## T11 — Runtime composition of the cognitive pipeline (context + epistemic)

### 1. Decision

**Evidence baseline: `9fa4f95057063bfb1fa9d5582227c13d8267c6fa`. This is an implementation brief, not an implementation, admission receipt, or executed proof.**

1. **OWNER DEFAULT (proposed):** place the concrete orchestration in `crates/governor/eliot-governor/src/context_composition.rs`, composed by the existing `bins/eliotd/src/lib.rs::DaemonComposition`, under the existing Governor integration owner **#18**; this fills the edge map's explicitly external composition slot without creating another daemon, semantic owner, or Smart-to-Store dependency (`crates/smart/cognitive-edge-map.toml:6@9fa4f950`; `crates/governor/AGENTS.md:29@9fa4f950`).
2. Obtain seven **immutable, revision/fence-bound owner projections** through Governor read/task/problem/session/module owners and Kernel-authorized named reads, construct cues from admitted binding projections, then invoke candidates → admission → assembly without moving final membership or truth decisions into adapters (`docs/architecture/I01-08-exact-ownership-and-call-paths.md:36@9fa4f950`; `crates/smart/eliot-context-admission/module.toml:21@9fa4f950`; `crates/smart/eliot-cue-index/src/build.rs:15@9fa4f950`).
3. Keep the current public agent surface **`eliot.packet`**, with explicit-intent `eliot.query` for read/expansion operations; neither the MCP bridge nor the daemon binary chooses the task, invents a Session, reranks the admitted view, or bypasses Kernel binding (`crates/surfaces/eliot-mcp/src/contract.rs:65@9fa4f950`; `crates/surfaces/AGENTS.md:44@9fa4f950`; `docs/architecture/I01-08-exact-ownership-and-call-paths.md:24@9fa4f950`).
4. Build on an authenticated, uniquely task-bound SessionStart/task activation, explicit packet refresh, relevant invalidation, or a governed Dreamer bundle request; prepare reactive deltas away from the hot boundary, with Host delivery through a supported hook or the next ELIOT response, not a model call on PreToolUse (`docs/architecture/I07-10-reactive-delivery.md:3@9fa4f950`; `docs/architecture/I07-19-reactive-context-sequence.md:3@9fa4f950`; `crates/smart/cognitive-edge-map.toml:33@9fa4f950`).
5. **The epistemic writer is Governor semantic admission**, using the existing `SemanticCommand = NamedMutationRequest` / `SemanticCommandKind = NamedMutationOperation` vocabulary and `ApplyEpistemicRevision`, followed by the existing PreparedTransition → Kernel → atomic Store → receipt/readback path; a resolver result, read request, model answer, or Dreamer candidate is not itself an admitted position (`crates/governor/eliot-canonical/src/lib.rs:364@9fa4f950`; `docs/architecture/I01-08-exact-ownership-and-call-paths.md:48@9fa4f950`; `crates/smart/eliot-epistemic-contracts/src/admitted.rs:1@9fa4f950`).
6. **OWNER DEFAULT (proposed):** use the existing inert `EpistemicPositionCandidate` and `EpistemicTransition` as the semantic payload vocabulary of the activated mutation, with an explicit Store parameter schema and position-revision CAS rather than a new epistemic command enum or generic status setter (`crates/smart/eliot-epistemic-contracts/src/candidate.rs:1@9fa4f950`; `crates/smart/eliot-epistemic-contracts/src/transition.rs:1@9fa4f950`; `crates/storage/eliot-store-api/src/operation_parameters.rs:14@9fa4f950`).
7. Independent leaf implementation/proof and per-edge contract fixtures precede serialized integrator admission; runtime wiring follows admission rather than becoming a prerequisite for package proof, and admission alone never counts as a working pipeline (`crates/smart/cognitive-edge-map.toml:7@9fa4f950`; `crates/smart/eliot-context-admission/module.toml:33@9fa4f950`; `workstreams/ACTIVE.toml:50@9fa4f950`).
8. Retire only the legacy Context/recall branches whose actual callers have migrated and whose #7/#8/#1261 discriminators pass on the new path; do not delete the entire engine/app, retain a second active compiler, or silently fall back to legacy success when the new path reports incomplete (`crates/eliot-app/AGENTS.md:31@9fa4f950`; `crates/eliot-app/AGENTS.md:49@9fa4f950`; `crates/smart/eliot-context-admission/module.toml:31@9fa4f950`).
9. Keep the owner's single-account deployment assumption from the task, without adding account/authentication products and without removing local process/session/fence checks; useful D3a/D3b operation must not wait for D3c graphs, broad Dreamer execution, or a full-workspace testing campaign (`docs/architecture/I01-08-exact-ownership-and-call-paths.md:24@9fa4f950`; `docs/architecture/I17-11-delivery-depth-d3-grounded-smart-understanding.md:3@9fa4f950`; `START.md:6@9fa4f950`).

### 2. Evidence

- `crates/smart/cognitive-edge-map.toml:6@9fa4f950` — composition is outside Smart — permits a runtime composition implementation, not algorithm-to-algorithm dependency shortcuts.
- `crates/smart/cognitive-edge-map.toml:7@9fa4f950` — producer, consumer, version/fence, omission/failure and replacement fixtures precede admission — separates package/contract proof from later live runtime proof.
- `crates/governor/AGENTS.md:29@9fa4f950` — semantic application behind `eliotd`, integration owner #18 — resolves the unnamed runtime owner.
- `crates/governor/AGENTS.md:41@9fa4f950` — current semantic position and context/evidence admission belong here — runtime orchestration must reuse these owners.
- `docs/architecture/I01-08-exact-ownership-and-call-paths.md:5@9fa4f950` — one logical Governor with split checks — no Smart, Host, or Store semantic writer.
- `docs/architecture/I01-08-exact-ownership-and-call-paths.md:46@9fa4f950` — exact, expiring NamedReadCapability — no generic read bypass or write through a read capability.
- `crates/governor/eliot-read/src/lib.rs:435@9fa4f950` — existing ReadApi/ReadService boundary — extend this read owner instead of adding a competing query service.
- `crates/storage/eliot-store-api/src/operation_parameters.rs:3@9fa4f950` — four activated reads and two mutations only — enum presence does not make cognitive reads executable.
- `crates/governor/eliot-canonical/src/lib.rs:364@9fa4f950` — semantic command aliases already exist — adding a second command vocabulary is unnecessary.
- `crates/smart/eliot-epistemic/src/lib.rs:17@9fa4f950` — local resolver algebra differs from the extracted wire contracts — an alias/import change is not a lossless runtime adapter.
- `crates/smart/eliot-epistemic-contracts/src/admitted.rs:76@9fa4f950` — admitted view binds an external receipt and its own position revision — do not manufacture admission or substitute TaskRevision.
- `crates/smart/eliot-epistemic-contracts/src/transition.rs:3@9fa4f950` — inert transition, explicit before/after support, no promotion without fresh evidence — retain existing semantic vocabulary and validate closure.
- `docs/architecture/I12-12-current-epistemic-position-resolver.md:5@9fa4f950` — filter current evidence, preserve rivals and uncertainty — no vote/prose/recency shortcut to truth.
- `crates/smart/eliot-context-candidates/src/lib.rs:1@9fa4f950` — seven owner projections, not retrieval/admission — acquire outside the candidate cell.
- `crates/smart/eliot-context-admission/module.toml:21@9fa4f950` — admission decides include/handle/revalidate/suppress/quarantine — assembly may not repair membership.
- `crates/smart/eliot-context-assembly/src/assemble.rs:81@9fa4f950` — actual assembler takes admitted set, recipe, quality, policy and measurement callback — reuse its real API, not a fictional assembly service.
- `crates/smart/eliot-context-contracts/src/error.rs:262@9fa4f950` — Complete and Incomplete are separate variants — typed incompleteness must survive to the caller.
- `crates/smart/eliot-reactive-context-plan/src/plan.rs:30@9fa4f950` — six immutable planning inputs — an ActiveUnderstandingView is not directly interchangeable with ContextPlanningView.
- `crates/smart/eliot-reactive-context-plan/module.toml:29@9fa4f950` — no delivery, receipt issuance or Session mutation — downstream delivery owns those effects.
- `docs/architecture/I07-19-reactive-context-sequence.md:15@9fa4f950` — Critical items sticky, ordinary items deduplicated — deduplication uses actual session/delivery evidence.
- `crates/surfaces/eliot-mcp/src/host_gateway.rs:27@9fa4f950` — KernelHostRequestPort already binds the host request path — preserve host correlation versus canonical request identity.
- `docs/architecture/I12-13-context-compiler.md:1@9fa4f950` — whole units, safety floor, independent reserves, vector quality and selection integrity — neither token cutting nor an all-passed scorecard is acceptable wiring.
- `docs/architecture/I17-11-delivery-depth-d3-grounded-smart-understanding.md:3@9fa4f950` — D3a exact-record orientation and D3b zero-edge operation — missing graphs do not justify postponing the useful path.
- `workstreams/core-daemons/AGENTS.md:41@9fa4f950` — Dreamer is excluded from that workstream — a Dreamer consumer integration needs an explicitly separate scope, not covert expansion of a core-daemon patch.

Issue bodies #816, #626, #604, #38, #19, #830, #704, #7, #8, #1261 and #806 were also read. GitHub issue bodies are mutable and are **not** represented as files frozen by the Git commit; the file citations above are the pinned source/normative evidence. The controller must freeze the accepted issue-body versions when dispatching the implementation units.

### 3. Current code reality

| Boundary | What exists at the pin | What T11 must not assume |
|---|---|---|
| Runtime root | `bins/eliotd/src/lib.rs::DaemonComposition`; `daemon_runtime.rs` has bounded activation/heartbeat/shutdown machinery | A responsive daemon or ControlBoard does not establish a packet caller |
| Governor composition | `crates/governor/eliot-governor/src/composition.rs` has Kernel transition/recovery ports and the existing owner set | RecoveryOwner snapshots are not a generic semantic Store-query bypass; do not register a seventeenth cognitive state owner |
| Read owner | `eliot-read::ReadApi`, `ReadService<C: CanonicalStoreClient>`, exact fence/dependency checks | The concrete generic bound currently exposes a wider client than a runtime read-only adapter needs |
| Store | `operation_catalogue.rs` and `operation_parameters.rs` activate GetRevisionHeads, GetOrderingHeads, GetScopeRevisionView, ResolveWriteReceipt, CaptureObservation and AppendAuditEvent | GetCurrentEpistemicPosition/GetEvidencePack/GetUnderstandingProjectionInputs and ApplyEpistemicRevision still need schemas, handlers and callers |
| Epistemic | `eliot-epistemic::resolve` returns its local six-state result; extracted contracts include candidate, transition, admitted receipt and wire CurrentEpistemicPosition | `ContractPosition` is an alias to a different type, not proof that `resolve` returns an externally admitted view |
| Cue index | `build_cue_snapshot`/`rebuild_cue_snapshot` take AdmittedCueBindingProjection records and optional relation edges | Output is a CueSnapshotBuildCandidate; its validator does not authenticate admission or publish a snapshot |
| Candidates | `construct_context_candidates`; typed cue/epistemic/evidence/attention inputs and opaque versioned owner projections | Missing task/negative-memory/affordance schemas cannot be filled with made-up complete values |
| Admission / assembly | `admit_context(&AdmissionInput)`; `assemble_active_view` is a real bounded renderer with exact measurement and quality checks | Incomplete admission must not be passed into a renderer as a successful empty set |
| Reactive | `plan_pending_context_injection` returns a plan/no-injection/error from six immutable inputs | It neither delivers nor issues receipts; lossless AUV → ContextPlanningView projection is another required adapter |
| Measurement / admission | A-15 contracts are admitted; context leaves remain prototypes/excluded pending integration | The exact Smart tree contains no `eliot-context-measurement` directory, although #704 owns it and #830 requires it in the nine-package wave |
| Public surface | `ToolRequest::Packet` is `eliot.packet`; `ToolRequest::Query` is explicit-intent `eliot.query`; KernelHostRequestPort owns forwarding | Do not introduce an `eliot.context` tool, send caller-minted Session metadata, or mistake Accepted for delivered/committed |
| Legacy | `crates/eliot-engine/src/context.rs::ContextCompiler`; legacy `crates/eliot-app/src/mcp_stdio.rs` and `mcp_stdio/dispatch.rs` | Their presence does not establish that every installed client still uses them, or authorize wholesale deletion |

The measurement absence was checked by exact-path reads and the pinned Smart tree `6e7aa38834601e2f8ca92270ab08d7382b9bee6e`, not inferred from a failed Rust import. No repository-wide grep or Rust execution is claimed here. Sources for the additional gaps: `crates/governor/eliot-read/src/lib.rs:435@9fa4f950`; `crates/smart/eliot-epistemic/src/lib.rs:17@9fa4f950`; `crates/smart/eliot-cue-index/src/lib.rs:1@9fa4f950`; `crates/smart/eliot-reactive-context-plan/src/plan.rs:30@9fa4f950`.

**Projection acquisition — OWNER DEFAULT (proposed).** The operation-to-projection mapping below is a concrete placement choice, not a claim that these Store operations are activated already. Governor owners determine meaning; Store returns exact bounded records and source heads. Existing typed contracts are used where present; otherwise the candidate cell's existing opaque, versioned owner-projection slot carries the complete owner object without semantic reinterpretation.

| Candidate provider role | Runtime source / acquisition | Required preservation |
|---|---|---|
| TaskFrame | Governor WorkScope/task selection plus activated GetTaskState | Unique selected task, acceptance digest, task revision, workspace identity; ambiguity is not newest-task selection |
| CriticalAttention/Conflict | Governor problem owner plus GetAttentionAndProblems | Sticky obligations, conflicts, waiver/resolution identities, exact denominator |
| CurrentEpistemicPosition | GetCurrentEpistemicPosition after canonical admission; resolve/expand supporting material through GetEvidencePack | External receipt, PositionId/PositionRevision, source heads, claims, assumptions, rivals and unknowns |
| Cue activation | Cue normalizer → binding/index inputs obtained through a closed GetUnderstandingProjectionInputs projection selector → activation | Only authenticated admitted bindings; scope/profile/fence/source revisions; zero edges permitted |
| Negative memory | GetUnderstandingProjectionInputs, closed negative-memory projection selector | Retractions, prohibitions, stale/partial/known-empty distinctions and resolvable source handles |
| Evidence/source assurance | GetEvidencePack with Governor/source-assurance owner projection | Exact bytes/handles, provenance, verifier standing, source independence and proof ceiling |
| Affordances | Governor authority/module projections plus GetCapabilityEvidenceState when durable evidence is required | Available operations and constraints are offers, not new effect grants |

All seven bind one compatible read closure. Capture dependency heads before acquisition and verify them afterward; bounded churn produces a typed stale/incomplete result, not a silently mixed snapshot or infinite retry. `KnownEmpty` requires an authoritative completed lookup. Transport failure, absent implementation and a bounded partial scan are not empty. Runtime adapters never treat opaque source prose as instructions. This follows the existing read checks and candidate boundary, rather than a new snapshot authority (`crates/governor/eliot-read/src/lib.rs:485@9fa4f950`; `crates/smart/eliot-context-candidates/src/inputs.rs:1@9fa4f950`).

### 4. Critical documentation errors (if any)

**none**

The missing runtime caller, unactivated Store operations, local/wire epistemic mismatch and absent measurement package are implementation/admission gaps, not two mutually impossible normative statements. #830's full-workspace commands versus this task's scoped-gate instruction require an explicit owner-scoped execution clarification, not deletion of a normative invariant. Likewise, the core-daemons Dreamer exclusion is a scope boundary, not permission to silently widen #18's current work unit.

### 5. Slice plan

#### Dispatch and admission rules

**Ownership:** #18 remains the runtime semantic integration owner, #19 the Store owner, #38 the epistemic implementation/contract owner, #830 the serialized root-admission owner, and the existing leaf issues retain their algorithm scopes. A slice below is a bounded integration acceptance unit. Before dispatch, the controller gives it one exact open issue/subissue and one writer per path; cross-owner supporting changes land under their existing owners. Do not use metadata-only #816 or leaf-only #604/#626 as authorization to edit daemon/Store code, and do not reuse completed #13/#15 as active writers. Supporting contract/admission PRs do not close a runtime slice on their own (`workstreams/core-daemons/AGENTS.md:110@9fa4f950`; `crates/smart/eliot-context-admission/module.toml:49@9fa4f950`).

**Order:** T11.1 → T11.2 → T11.3 → T11.4 → T11.5 → T11.6 → T11.7. These integration slices are **serialized**: shared `lib.rs`, Cargo manifests, operation tables and bridge dispatch paths are never concurrently writable. Independent existing leaf work may proceed in parallel only in its already exclusive package scope. New file placements/signatures below are **OWNER DEFAULT (proposed)** unless explicitly called existing.

**Admission prerequisites, not runtime completion:**

1. Keep the already admitted A-10/A-15/epistemic contracts as their sole public owners; revalidate exact source and contract revisions rather than trusting old issue status.
2. Finish the actual #704 measurement implementation in `crates/smart/eliot-context-measurement`, with its existing owner-local proof; do not copy STU into Governor, assembly or a test helper to bypass that dependency.
3. Collect the current nine-package #830 denominator: normalizer, binding, index, activation, candidates, admission, assembly, reactive plan, measurement. Each leaf must be independently implemented/proved; an already admitted exact member is a verified no-op, not a denominator deletion.
4. For every crossed edge, exercise the real producer against the public contract, real consumer validation, wrong schema/fence, omission/failure, and replacement via the same neutral contract. This is contract-edge proof before admission, not an invented requirement for an already running whole pipeline. Topology #816 must reflect these compile/runtime distinctions without importing sibling implementations.
5. #830 alone applies the coherent membership/exclusion/lock/generated-index change after integrator review. No partial eight-of-nine admission, forged status field, or admission-before-leaf-proof cycle.
6. **OWNER DEFAULT (proposed, scoped-gate amendment for this task):** the controller records that #830's compilation gate is the exact nine packages and affected direct consumers, not a full-workspace rebuild; preserve existing substantive leaf/admission obligations, frozen identities and independent review. Until that scope is accepted, report the original gate as unexecuted rather than pretending scoped commands satisfy an unchanged issue body.

The package/admission work above may proceed while T11.1–T11.2 establish useful read/write paths. It is not another daemon and does not become a condition for unrelated existing operations. The fixture/admission basis is `crates/smart/cognitive-edge-map.toml:5@9fa4f950`; `crates/smart/eliot-context-contracts/module.toml:21@9fa4f950`; `crates/smart/eliot-context-admission/module.toml:55@9fa4f950`.

#### T11.1 — One real cognitive named read through the daemon

**Owning issue:** #18 integration; #19 owns the Store portion. **Depends on:** none of the new Context implementation admissions.

**Owned paths:** `crates/storage/eliot-store-api/src/lib.rs`, `operation_parameters.rs`, `operation_catalogue.rs`; the named-read handlers in `crates/storage/eliot-store-memory/src/` and `crates/storage/eliot-store-surreal-adapter/src/`; `crates/governor/eliot-read/src/lib.rs`; new `bins/eliotd/src/kernel_context_read_client.rs`; `bins/eliotd/src/lib.rs`; the existing Kernel/bridge named-read dispatch seam. Freeze exact adapter/dispatch files from the current tree before assigning the writer; no broad storage/process rewrite.

**Interfaces:** extract a read-only capability from the existing storage client in its existing owner; keep `ReadApi` and its request/result types. No fake full CanonicalStoreClient implementation whose write methods always return unavailable.

```rust
// UNCOMPILED — proposed in eliot-store-api; existing request/result types.
#[allow(async_fn_in_trait)]
pub trait CanonicalReadClient {
    async fn execute_named(
        &self, request: NamedReadRequest,
    ) -> Result<NamedReadResponse, StoreError>;
    async fn revision_heads(
        &self, keys: Vec<RevisionKey>,
    ) -> Result<Vec<RevisionHead>, StoreError>;
}
// CanonicalStoreClient inherits this capability; remove duplicate declarations.
// ReadService<C> requires C: CanonicalReadClient, not the write capability.
```

Implement the read-only Kernel-backed adapter, with a fresh operation/scope/fence-bound read capability and exact response validation; reuse ReadService's existing consistency algorithm, not a second implementation in the binary. Activate **GetEvidencePack only** in this slice with bounded exact selectors, a versioned result and actual handlers. A capability is never a raw query, a recovery-owner selector, or a write grant.

**Acceptance:** through the running bridge/daemon, capture one real observation using the existing capture path and retrieve its exact record/provenance via `eliot.query` with an explicit verification/read intent. One small real-Store behavior test verifies record identity and fence; changing the fence or exceeding the declared bound must not return a successful current view. Exercise the real Surreal edge before claiming production support; the memory adapter is reference proof only.

**Deferred:** full projection collection, CEP computation/admission, vector/graph retrieval. **Unblocks:** the real input-read dependency of #19/#604/#626 and the read-only half of #7. Basis: `docs/architecture/I01-08-exact-ownership-and-call-paths.md:36@9fa4f950`; `crates/governor/eliot-read/src/lib.rs:435@9fa4f950`; `crates/storage/AGENTS.md:29@9fa4f950`.

#### T11.2 — Epistemic proposal → admitted write → current-position readback

**Owning issue:** #18 integration, with #38 resolver adaptation and #19 transaction execution in their existing scopes. **Depends on:** T11.1.

**Owned paths:** new `crates/governor/eliot-governor/src/epistemic_composition.rs`; new `crates/governor/eliot-canonical/src/epistemic_revision.rs` and exports; `crates/smart/eliot-epistemic/src/` for an owner-local adapter; Store parameter/catalogue and actual mutation/read handlers; daemon wiring and focused integration test. Contract corrections belong to `eliot-epistemic-contracts`, not a local Governor/Store duplicate.

**Interfaces:** reuse EpistemicPositionCandidate, EpistemicTransition, AdmittedReceipt, PositionId and PositionRevision. Keep the donor resolver API for its existing consumers; the owner-local adaptation is private and exchanges only the existing contract types. The command encoder is not the semantic admission decision:

```rust
// UNCOMPILED — proposed helper in eliot-canonical::epistemic_revision.
pub fn epistemic_revision_command(
    transition: &eliot_epistemic_contracts::EpistemicTransition,
    candidate: &eliot_epistemic_contracts::EpistemicPositionCandidate,
) -> Result<eliot_store_api::NamedMutationRequest, crate::CanonicalError>;
```

The runtime producer receives a real observed evidence/verifier change, obtains its complete bounded owner read closure, invokes the resolver and owner-local candidate adaptation, validates the contract's closed support/coverage/claim/verifier relationships, then calls the encoder and existing canonical preparation path. An incomplete input remains an explicit candidate/unknown/investigation condition; a read does not secretly trigger a write. The local six-state resolver result cannot be serialized/cast into the wire admitted view, and local Assumed cannot become Supported.

Declare a closed ApplyEpistemicRevision parameter schema with the exact candidate/transition payloads, expected **position** revision/absence condition, immutable evidence/proof references and schema identities. Bind these to the envelope's operation, request, scope, task revision, fence, manifest, CAS and digest. Check the operation's existing transition/effect classification; do not borrow CaptureObservation's Candidate ceiling to authorize a stronger revision. Store executes only the admitted mutation, atomically preserving history, projection, receipt and outbox. Assemble an AdmittedReceipt from actual commit/readback facts; never pre-mint it to satisfy a candidate decoder or create a circular receipt digest.

Activate GetCurrentEpistemicPosition with the actual consumer and handler. Return the admitted wire CEP only after validating its external receipt/projection; the current-position read is not an independent truth resolver inside Store.

**Acceptance:** one observed, evidence-backed change travels through Governor → Kernel → real Store → exact CEP readback. Repeat the identical operation and obtain the same receipt/revision; a stale predecessor or changed same-ID payload fails without a second revision. Preserve the existing shared negative transaction tests and unknown-outcome reconciliation; do not blindly retry after a lost response.

**Deferred:** automatic global promotion, model generation, broad revision families unrelated to this transition. **Unblocks:** #38's runtime consumption, #19 ApplyEpistemicRevision and #604's real CEP input. Basis: `crates/smart/eliot-epistemic/src/lib.rs:17@9fa4f950`; `crates/smart/eliot-epistemic-contracts/src/admitted.rs:76@9fa4f950`; `docs/architecture/I01-08-exact-ownership-and-call-paths.md:48@9fa4f950`.

#### T11.3 — Seven-provider reconstruction with a real zero-edge cue index

**Owning issue:** #18 integration; #19 for additional named reads; existing cue leaf owners and #830 for prerequisites. **Depends on:** T11.2 and accepted cue admission from the complete #830 wave.

**Owned paths:** new `crates/governor/eliot-governor/src/context_inputs.rs` and `cue_composition.rs`; remaining cognitive read schemas/handlers listed in section 3; root wiring; one reconstruction behavior test. No changes to candidate selection policy.

**Interfaces:** keep the seven current candidate input families and `ReadApi::query`; expose them through the existing `eliot.query` **ContextReconstruction** intent, clearly as input reconstruction, not an admitted ActiveUnderstandingView. Reuse these index entrypoints without publishing their result as an authenticated snapshot merely because construction succeeded:

```rust
// UNCOMPILED — existing owner entrypoint, not a new runtime interface.
pub fn build_cue_snapshot(
    scope_id: &eliot_cue_contracts::WorkScopeId,
    snapshot_id: eliot_cue_contracts::SnapshotId,
    profile: eliot_cue_contracts::NormalizationProfile,
    state_fence: eliot_contracts::StateFence,
    projections: &[eliot_cue_contracts::AdmittedCueBindingProjection],
    relation_edges: &[eliot_cue_contracts::RelationEdge],
    registry_revision: Option<&str>,
) -> Result<eliot_cue_contracts::CueSnapshotBuildCandidate,
            eliot_cue_contracts::CueContractError>;
```

Resolve admitted binding receipts/source existence before index construction; validate the built candidate against the same source closure before exposing the derived read projection. Keep any cache under the existing read owner, keyed by scope, source revisions, normalization profile and fence, with no durable semantic authority. New cue-binding candidates enter the existing governed admission path; they are not accepted by calling an index constructor. Zero edges means an authoritative empty edge set, not a swallowed provider error.

**Acceptance:** a task-bound ContextReconstruction request returns all seven role dispositions and an activation from one real admitted exact binding with **zero graph edges**. A second read after source/fence invalidation rebuilds or reports stale; it never serves a previous generation as current. At least one unsupported/missing role is reported distinctly from authoritative KnownEmpty. Use real records written through the canonical path, not test-only injected admission receipts.

**Deferred:** relation expansion/large graph optimization, additional owner schema redesign. **Unblocks:** #604's producer closure, #816's real provider edges, and #626's eventual input path. Basis: `crates/smart/eliot-cue-index/src/build.rs:15@9fa4f950`; `crates/smart/eliot-context-candidates/src/inputs.rs:1@9fa4f950`.

#### T11.4 — `eliot.packet` executes candidates → admission → assembly

**Owning issue:** #18 integration; #604/#608/#626 remain the respective algorithm owners. **Depends on:** T11.3 and completed #704/#830 prerequisites.

**Owned paths:** new `crates/governor/eliot-governor/src/context_composition.rs`; composition/exports/Cargo dependency declarations in that crate; `bins/eliotd/src/lib.rs` and bounded request-dispatch wiring; existing bridge/MCP response projection and one packet behavior test. Concrete Smart dependencies stay at this external composition layer, never between Smart producers/consumers.

**Interfaces:** introduce one Governor service entrypoint; use existing A-15 outcomes. Its local error wraps existing typed read/candidate/assembly failures and does not become another public Context schema.

```rust
// UNCOMPILED — proposed Governor composition service API.
impl<R: eliot_read::ReadApi> ContextComposition<R> {
    pub async fn build_context(
        &self,
        request: &eliot_contracts::RequestMetadata,
        binding: &eliot_context_contracts::ContextBinding,
    ) -> Result<
        eliot_context_contracts::ContextOutcome<
            eliot_context_contracts::ActiveUnderstandingView>,
        ContextCompositionError,
    >;
}
```

The trusted binding comes from Kernel/Governor task selection, not MCP arguments. Obtain the seven projections, current versioned recipe, qualified route/reserves and actual quality evidence; invoke the existing constructor and admit_context. Branch on Incomplete **before** assembly. For Complete, call the actual assemble_active_view API with its exact admitted set, recipe, quality, policy and the sole #704 measurement implementation. Preserve member equality, exact bytes, order, all omission/selection receipts, independent reserves and every quality dimension. An unknown mandatory quality dimension is not an all-passed scorecard. Exact UTF-8 bytes/estimated STU/observed provider tokens remain distinct; no local byte/3 fallback or fabricated observation.

Project the exact result through existing `ToolRequest::Packet` / KernelHostRequestPort. Packet response framing must remain inside the measured/reserved route envelope; do not re-render or trim the admitted body in MCP.

**Acceptance:** a real packet request returns the assembled view consumed by the agent and Governor decision path. In one small behavior test, a canonically Supported record and a weak/candidate record remain correctly distinguished even when the real FTS query returns no hits; exact admitted membership survives rendering. A mandatory stale/gap variant returns DECISION_CONTEXT_INCOMPLETE with recovery/expansion handles, not legacy success. Slow acquisition must not starve heartbeat/cancellation/shutdown.

**Deferred:** D3c graphs, empirical route qualification campaigns, broad quality-policy optimization. **Unblocks:** #604/#626 runtime use, #816 candidate/admission/assembly edges, #1261's new-path discriminator. Basis: `crates/smart/eliot-context-assembly/src/assemble.rs:81@9fa4f950`; `docs/architecture/I12-13-context-compiler.md:1@9fa4f950`.

#### T11.5 — Task-bound bootstrap and reactive delivery with receipts

**Owning issue:** #18 integration and #8 attach regression; delivery/surface portions require the existing Host/bridge owner, not a Smart leaf writer. **Depends on:** T11.4.

**Owned paths:** new `crates/governor/eliot-governor/src/context_delivery.rs`; daemon event dispatch; existing Host Event/reactive delivery handler and `bins/eliot-agent-bridge/src/lib.rs`; the corresponding `eliot-mcp` host gateway/response adapter and one integration scenario. Freeze exact Host files before assigning the cross-owner patch; do not introduce a new queue database, scheduler or durable cursor owner.

**Interfaces:** no new public MCP tool or Session token. Add a lossless A-15 AUV → ContextPlanningView adapter at the existing contract/projection boundary, then call the existing planner:

```rust
// UNCOMPILED — existing planner; all six inputs must be real projections.
pub fn plan_pending_context_injection(
    view: &eliot_context_contracts::ContextPlanningView,
    cue_activation: &eliot_reactive_context_plan::ReactiveCueActivation,
    session_snapshot: &eliot_context_contracts::SessionDeliverySnapshot,
    critical_attention: &eliot_context_contracts::CriticalAttentionProjection,
    integration_coverage: &eliot_context_contracts::IntegrationCoverageProfile,
    policy: &eliot_reactive_context_plan::ReactiveDeliveryPolicy,
) -> eliot_reactive_context_plan::ReactiveContextPlanResult;
```

Trigger after successful task-bound attach, task activation or relevant invalidation. No unique task yields TASK_SELECTION_REQUIRED with bounded handles, not a historical-task guess or a fabricated complete packet. Convert only an actual InertDeliveryRequest to the existing B-RCTX/Host request; preserve operation/content/fence/coverage identity. Tool-only hosts receive pending context on the next ELIOT response and remain advisory. The delivery owner records an observed receipt; Accepted, serialization and stdio flush are not evidence of user/agent visibility or use. Critical obligations remain sticky; ordinary dedup uses the real SessionDeliverySnapshot and invalidates with source/fence changes.

**Acceptance:** one fresh session receives its selected task's bounded bootstrap; a later real exact cue causes delivery with zero graph edges and no hot model. Replaying the same event does not create a second logical observation/injection; a critical unresolved item remains eligible. Bind host correlation through response and observed completion; explicitly retain unknown external/UI coverage required by #7.

**Deferred:** unsupported external action interception, unobserved UI/use claims, background learning. **Unblocks:** #8, the delivery portion of #7, A-19r/B-RCTX and the D3b Context path. Basis: `docs/architecture/I07-10-reactive-delivery.md:3@9fa4f950`; `docs/architecture/I07-19-reactive-context-sequence.md:15@9fa4f950`; `crates/smart/eliot-reactive-context-plan/module.toml:29@9fa4f950`.

#### T11.6 — Governed Dreamer bundle request borrows the same view

**Owning issue:** **#18 runtime integration follow-up, OWNER DEFAULT (proposed): a separately authorized cognitive-runtime scope**, linked to #816 and the existing Dreamer bundle/cycle owners; **not** the core-daemons workstream and not scope expansion of pure-controller #806. **Depends on:** T11.4 and actual independent admission of the bundle implementation; it need not block T11.5 or ordinary agent packets.

**Owned paths:** new `crates/governor/eliot-governor/src/dreamer_context_adapter.rs` and its wiring/test, plus only the existing governed request dispatcher integration. No changes to Dreamer algorithms, process lifecycle, model routing or controller state semantics. The controller must record this explicit scope allocation before implementation; this brief does not silently grant it.

**Interfaces:** reuse `ContextComposition::build_context` and `eliot-dreamer-bundle::{AssemblyRequest, SuppliedAssemblyItem, plan_bundle, finalize_bundle}`; **no public interface change** to the Dreamer leaf or a new Context compiler. The adapter obtains a task/job/fence-bound view through the same service and supplies it as an immutable bundle item under the existing Dreamer contracts. Dreamer's bundle budget may differ from an agent response budget, but it cannot silently alter the supplied AUV's admitted membership or claim missing items were supplied.

**Acceptance:** one actual governed bundle request in the daemon obtains a newly built/revalidated AUV and the real bundle consumer retains its digest, scope/fence and provenance; changed fence or incomplete required view yields the existing typed partial/blocked result. This test executes bundle assembly, not a canned bundle or fake model. No model invocation is needed to prove this edge.

**Deferred:** model execution, job scheduling, full DreamCycle, D3c and Dreamer self-learning. **Unblocks:** #816's assembly → dreamer-bundle edge without making Dreamer a prerequisite for #7/#8. Basis: `crates/smart/cognitive-edge-map.toml:33@9fa4f950`; `crates/smart/eliot-dreamer-bundle/src/lib.rs:1@9fa4f950`; `workstreams/core-daemons/AGENTS.md:41@9fa4f950`.

#### T11.7 — Retire the migrated legacy Context route

**Owning issue:** #18 extraction; #7/#8/#1261 own their exact regression discriminators. **Depends on:** T11.4–T11.5, and T11.6 only for a demonstrated Dreamer caller of the branch being retired.

**Owned paths:** only proven old Context/recall call sites in `crates/eliot-app/src/mcp_stdio.rs`, `mcp_stdio/dispatch.rs` and their directly identified handlers; migrated portions of `crates/eliot-engine/src/context.rs`; exact compatibility/consumer tests and directly obsolete exports/dependencies. Resolve the complete caller inventory on the implementation base before deleting a symbol; do not reserve either whole crate for unrelated cleanup.

**Interfaces:** no new public interface. Current clients use `eliot.packet` and explicit-intent `eliot.query`. A necessary temporary compatibility adapter translates the old request to those current owners while preserving scope, task selection, intent, correlation and typed failure; it owns no compiler, state cache, fallback ranking or write path. Navigation/recall is not silently converted into a supported epistemic claim.

**Acceptance:** repeat the new-path packet test with the legacy Context dispatch disabled/removed, and run the focused actual #7/#8/#1261 regression suites. Prove a fresh constrained-route session receives the intended task or explicit selection request; a Supported record is reachable without FTS luck; request/response/readback correlation remains intact. Production readiness must not depend on the retired compiler. Retain unrelated legacy consumers until separately migrated, recording exact remaining symbols rather than claiming whole-engine retirement.

**Deferred:** complete removal of eliot-app/eliot-engine and unrelated migrations. **Unblocks:** the requested legacy Context dependency and closes the corresponding T11 retirement discriminator, not all historical integration issues. Basis: `crates/eliot-app/AGENTS.md:49@9fa4f950`; `crates/eliot-engine/src/context.rs:168@9fa4f950`; `crates/surfaces/AGENTS.md:44@9fa4f950`.

#### Verification at each handoff

Use current supported owner descriptors, existing substantive package tests and one small new real behavior test per vertical slice. Scoped locked check/test/Clippy cover touched packages and affected direct consumers; do not require a full-workspace rebuild. Do not reduce an existing proof denominator, change an oracle to make the implementation pass, or count a reference-memory test as the real Surreal/Host edge. Store receipt changes retain the required existing idempotency/fence/unknown-outcome checks.

Before source mutation the implementing manager must run the repository documentation reader for its exact actual paths and read the verified bundle. Return source/contract/issue-body identities, exact changed paths, commands actually executed, real caller → implementation → consumer evidence and remaining ceilings. Root admission, module proof, real Edge proof and #11 Product Pulse remain separate; skipped Windows/provider/UI evidence remains explicitly unexecuted. No workflow changes or automatic Actions triggers are part of T11 (`START.md:6@9fa4f950`; `crates/storage/AGENTS.md:69@9fa4f950`; `workstreams/core-daemons/AGENTS.md:92@9fa4f950`).

### 6. Risks

**The first slice must expose the real runtime-read boundary early.** ReadService currently requires CanonicalStoreClient, while the daemon is meant to use Kernel-bound capabilities; T11.1 must establish the actual typed wire/handler/consumer path and an acyclic contract dependency graph. A fake local store, generic recovery payload, always-unavailable adapter or a read-only API with hidden write authority is a rejected implementation, not a partial success.

**This is not wiring alone.** At the pin, the measurement implementation is absent, most cognitive named reads are inactive, the resolver's result is not the wire CEP, and a cue snapshot builder produces a candidate rather than authenticated publication. T11.1–T11.3 and the separate #704/#830 prerequisites deliberately expose those gaps before managers attempt to satisfy assembly with fake complete inputs.

**Receipt bootstrapping and revision confusion can corrupt epistemic ownership.** A candidate has no admission receipt; TaskRevision is not PositionRevision; an observation's existence does not prove a Supported/Verified claim. T11.2 must validate actual evidence closure and read back an externally admitted position without circular digest construction, status laundering or blind retry.

**Quality/measurement can become a false-success shortcut.** Existing assembly rejects unknown quality and incompatible measurement. The correct remedy is real owner evidence and measurement, not all-true vectors, guessed route limits, relabeled STU or empty mandatory slots. A typed incomplete packet is safe but does not satisfy the positive-path acceptance; the slice must also demonstrate a real complete supported scenario.

**Issue scope and gate drift are real.** #816 is metadata-only, #626/#604 are leaf-only, #830 has a fixed nine-package denominator, and core-daemons excludes Dreamer. The controller must freeze the explicit allocations/amendments above before code dispatch. A newer main may already implement part of the plan; revalidate and reuse it instead of duplicating owners or reintroducing retired code.

**Delivery proof can be overstated.** A live packet, planner result, accepted operation, flushed response, observed delivery and actual influence/use are distinct. T11.5 and #7 preserve unknown host/UI coverage rather than turning it into success. No supported claim or finish is created merely because a packet was emitted.

**Verification status of this brief:** pinned source/issue review and static document checks only. All Rust snippets are **UNCOMPILED** design signatures. No Rust build, package test, real daemon/Surreal/Host run, generated documentation-reader receipt, `just quick`, or Product Pulse was executed for this brief. No runtime issue is closed by committing this document; implementation completion is the working paths and scoped evidence above.
