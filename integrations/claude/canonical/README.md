# Canonical Claude connector metadata

This directory owns the small shared purpose and prompt inventory for both Claude
surfaces. `../eliot` remains the generated Claude Code plugin preserved from L7.
`../claude-desktop/mcpb` is the distinct Claude Desktop extension source.

Neither surface grants a permanent role. The live Governor session, task role lease,
work/action lease, verifier evidence, and current revision determine authority.
