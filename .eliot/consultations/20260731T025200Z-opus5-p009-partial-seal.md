# Bounded Opus 5 consultation: P009 / run006 partial seal

You are the senior reviewer. Do not use tools and do not ask questions. We need one
concrete collective decision after two fail-closed seal attempts. Prefer the
smallest safe repair; do not weaken authority, provenance, runtime binding, or
immutability.

Repository and contract:

- Rust repo: `eliot-memory-os`, branch `codex/cognitive-completion-v2`.
- Recovery Task 02R2 run: `cq-core-20260730-006`.
- Fresh provider calls dispatched for run006: zero.
- `provider-plan.json`: absent.
- run005/run003 accepted evidence must remain immutable.
- P009 exception is allowed only for:
  `cq-core-20260729-003 / 6f04e449-ecab-4555-8bd0-4a6bd762c1b4 / U03 / codex_worker`.
- All four U03 roles are reused; only U06/U11 are fresh (8 calls, max 9).

Implemented P009 repair:

1. `LegacyEvidenceAdmissionRecord` binds exact tuple, missing field
   `source_call.canonical_schema_sha256`, current Worker schema,
   immutable run005 role-evidence plan hash, receipt-derived historical runtime
   binding hash, and `fresh_provider_authority=false`.
2. `LegacyProviderRuntimeBinding` binds source commit/session/receipt ref,
   provider executable and hash, prompt hash, raw stream hashes, receipt hash,
   `zero_model_preflight_available=false`, `fresh_role_authority=false`.
3. Missing schema is rejected for every other tuple. Current-schema and all
   existing artifact/receipt/deterministic/contamination/candidate-diff checks
   remain.
4. Focused allow/reject and 8-fresh+4-reused tests pass.
5. Claude, Antigravity and OpenCode production headless prerequisites pass.

Seal attempt 1:

- Failed after 23.721 s:
  `legacy evidence admission differs from the immutable run005 role-evidence plan`.
- Root cause: hashing immutable v0 run005 JSON through the current expanded Rust
  struct serialized defaulted empty fields and changed hash material.
- Minimal repair made: exact `LegacyCoreRoleEvidencePlanV0` deserialization and
  historical hash verification. run005 remained unchanged. Focused tests pass.

Seal attempt 2:

- Failed after 3.838 s:
  `sealed output already exists with different content:
  ...\runtime\063d32a3-7779-44e8-90c7-b6a8cc83b44f-provider-runtime.json`.
- Exact mechanism: seal attempt 1 had already written per-call runtime JSON
  files before later P009 role-plan verification failed. It never wrote
  `provider-plan.json` and dispatched no provider.
- Attempt 2 re-ran runtime preparation. The runtime contains freshly generated
  `ELIOT_AGENT_SESSION_ID`, `ELIOT_ROLE_LEASE_ID`, and `ELIOT_WORK_ITEM_ID`, so
  content and runtime hash differ even with the same call.
- The incomplete attempt-1 file is operational pre-provider state, not
  qualification evidence, but `write_new_or_same_json` correctly refuses to
  overwrite it.
- Current attempt-1 runtime points at the current release Governor SHA-256
  `876f566935c67b40218a7d32a0f4441d2a9f7cf2302759dff1878f60f17f7651`.
- P009 source still needs a commit and final release rebuild, which will change
  that executable hash before provider execution.

Related implementation sequence today:

`seal_provider_plan` prepares external-agent runtime per fresh call and writes
`runtime/<call>-provider-runtime.json` immediately, then later loads/verifies
the core role evidence plan and ultimately writes `provider-plan.json`.

Questions to decide:

A. Is the root defect a partial-publication/resumability bug in seal ordering?
B. What is the minimal safe ordering/transaction shape? In particular, should
   all role-evidence checks run before any runtime file write, then runtime
   contracts be rendered to memory/staging and published only with the final
   provider plan?
C. For the existing incomplete run006 (zero provider calls, no provider plan),
   may the exact attempt-created runtime files be removed/replaced under an
   explicit typed recovery record after the P009 commit/final release build, or
   must a new run007 be created? State the evidence needed for your choice.
D. Should a retry ever reuse an existing runtime contract containing a prior
   live role lease, or must it regenerate scopes after final binary deployment?
E. Give the smallest focused tests required before a third real seal.
F. Review the P009 legacy admission design above for any authority/provenance
   hole that must be fixed before continuing.

Return:

1. root-cause verdict;
2. chosen recovery path;
3. minimal code changes;
4. exact tests/gates;
5. explicit do-not-do list;
6. whether run006 may continue or must be replaced.
