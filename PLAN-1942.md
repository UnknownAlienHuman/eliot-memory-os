# PLAN-1942 — Reactive producers (MGR-B, Wave2 item 4)

- Branch: `work/1942-reactive-producers` from `origin/main` `a236b0c2`
  (fetched `origin --prune` first; `#223` content already in main via
  `01b01b9c`/`#2398`-family integration — `work/223-projection-freeze`
  tip `7e6f7aa6` itself is NOT an ancestor, so reconcile against live main).
- Worktree: `C:\Development\Rust\projects\eliot-swarm\MGR-B-1942`
- `CARGO_TARGET_DIR`: `C:\Development\Rust\projects\eliot-swarm\targets\MGR-B`
- Issue: #1942 `[I7-audit] Persist reactive injection receipts and
  sticky-item resolution state` (OPEN). Owner per body: Context/attention
  and `bins/eliot-agent-bridge` delivery owners.

## Reading attestation

Read before mutation (brief-mandated):

- `gh issue view 1942` body + all comments, incl. audit v2 (PARTIAL:
  receipts/sticky MET; six real owner producers MISSING; owner join +
  P1/P2 + influence updates MISSING).
- `HANDOFF.md` §1 row D (reactive projection producers) + §2
  (dependency table: D feeds no cognition; missing owner = withheld).
- `CONTROL/o1-a066-full-review.md` in full: P1-1 steady-state commit
  wedge repair (retain first-attempt envelope per key and replay
  byte-identically, OR track committed keys to skip re-submission;
  never re-derive expectations for an attempted key; do NOT weaken the
  triple rule, do NOT mint new operation identities), P2-1 stale-marker
  repair (`&`-settable marker set on the commit path, surfaced in
  `status()`, cleared by the owning refresh).
- Fragments `docs/architecture/I07-19-reactive-context-sequence.md`
  (16 lines, full) and `I07-18-readresources-surface.md` (19 lines,
  full): sequence observed-event → … → Delivery/Injection Receipt →
  later influence/use/outcome update; sticky critical / session-deduped
  normal; revisioned resources.
- Root `AGENTS.md`, `bins/AGENTS.md`, `crates/AGENTS.md`, `WORKFLOW.md`
  (full): composition binaries must not gain task/memory/policy/
  scheduling/canonical-write/store semantics; no production `todo!`;
  one writer per file; docs bundle before mutation.
- `T11.md` §T11.5 (reactive delivery with receipts): lossless A-15 AUV
  → ContextPlanningView adapter at the contract boundary, then call the
  existing planner; only an actual `InertDeliveryRequest` converts to a
  B-RCTX/Host request (Host/bridge owner, NOT this lane); the delivery
  owner records the observed receipt.
- `crates/smart/eliot-reactive-context-plan/module.toml` (full):
  stateless, `allowed_effects = []`, non-goals exclude delivery,
  receipt issuance, session mutation, attention resolution;
  `providers = [context assembly, cue activation, Governor session
  projection]`; `depends_on` closed (no impl deps on assembly/cue-activation).
- O1 source diff `origin/main...8f4fc606` (40 files): port ONLY #1942
  needs, adapted to main.
- Docs routes (PASS, receipts under `.eliot/plan1942/`):
  - `bins/eliotd/src/experience_runtime.rs` → read
    `sha256:3bc4772b…`, route `sha256:2722ed5c…`, bundle `0010e770…` (23 items)
  - `crates/smart/eliot-reactive-context-plan/src/reactive_owner_producers.rs`
    → read `sha256:1ade54eb…`, route `sha256:4e7395fd…`, bundle `f7d8fd93…`
    (35 items, routes `generic-source`, `memory-context`)
  - `bins/eliotd/src/reactive_feed.rs` → read `sha256:e70091b3…`,
    route `sha256:6f188a3e…`, bundle `6ae2ffdb…` (23 items)
  - `bins/eliotd/src/lib.rs` → read `sha256:814d9382…`,
    route `sha256:4c3ca134…`, bundle `797d7782…` (23 items)
  - Bundles opened; required items read (AGENTS ×3, ARCHITECTURE_CONTRACT,
    DEPENDENCY_POLICY, READING_PROTOCOL, ACTIVE.toml, A00-01/02/03/04/06,
    A02-03, A04/A05/A06/A07 (A07 + I13 are single-line placeholders —
    no normative content), A10-04, A14-01..04/08, I00-03/04/05/13/14,
    I02-17/20, I12-09/10, I13, I16, I18).
  - `COMMON-RULES.md`, `WORK-QUEUE` item 13, `MGR-B BRIEF` Wave2 text:
    NOT present in this worker's visible scope; the manager brief in the
    task prompt is used as the lane authority instead (recorded here).

## Code reality on current main (verified by read, not inferred)

- `commit_experience_event_records` (`bins/eliotd/src/experience_runtime.rs:704`,
  `&mut DaemonComposition`) documents convergent retry ("already-durable
  receipts stay durable … so retry is convergent") but IMPLEMENTS none:
  no committed-key tracking; a retry re-derives ingress and re-submits
  the same keys under fresh expected heads → `IdentityConflict` →
  batch abort → `Failed`, forever. P1-1 is LATENT on main (no production
  caller yet — verified zero callers outside the module + re-export).
  Fix belongs here (repair option b: track committed keys, skip
  re-submission), touching only `eliotd`.
- P2-1's O1-branch shape (`&self` commit, dropped refresh, zero-caller
  `refresh_dependent_view`) does NOT exist on main: `commit_canonical_and_refresh`,
  `commit_experience_bank_record`, `commit_experience_feedback_record`,
  `finish_attempt` (`lib.rs:425-539`) are `&mut`, run the real
  `GovernorComposition::refresh_from_kernel()` (rebuilds owners from
  Kernel recovery, `composition.rs:4177`), set `view_stale` on refresh
  failure, and `status()` surfaces `stale`/non-ready. No code change
  there; conformance is asserted, not duplicated. Remaining P2 in #1942
  scope = reactive retained projections served stale under healthy
  status → fixed by construction in the new feed (no cross-call
  retention; per-evaluation fence/digest/revision validation; stale →
  typed non-plan outcome, never silently current).
- Six planner inputs (`plan_pending_context_injection`, `plan.rs:32-39`)
  have production callers ONLY in tests (`work_unit_612.rs`, support
  `reactive_plan.rs` fixtures) plus the bridge transport consuming the
  plan OUTPUT (`settled_plan_transport.rs`). No `reactive_owner_*`
  files exist anywhere on main (verified). Bridge already owns session
  binding/ledger/receipts/stickiness/dedup — the feed must stop at the
  inert plan (bins/AGENTS.md + module.toml non-goals).
- Owner functions verified real (no guessing): `assemble_active_view`
  (assembly), `evaluate_activation` (cue-activation),
  `ContextPlanningView::new` (re-proves both byte payloads against the
  view/admitted digests), `ReactiveCueActivation::validate_against`,
  `ReactiveDeliveryPolicy::{validate, canonical_digest}`,
  `SessionDeliverySnapshot::canonical_digest`,
  `CriticalAttentionProjection::validate`,
  `IntegrationCoverageProfile` (contracts) validation, Governor
  `eliot-integration-coverage` owner (separate type — project, do NOT
  alias/bridge-duplicate).
- Layering (edge-map + module.toml `depends_on`): producers take owner
  ARTIFACTS, never call assembly/admission/cue impl. No new
  dependencies for `eliot-reactive-context-plan`. `eliotd` gains one
  path dep (`eliot-reactive-context-plan`, precedent:
  `eliot-cognitive-quality` path dep). No third-party deps
  (DEPENDENCY_POLICY clean).

## Changes (this lane only; no widening)

1. `bins/eliotd/src/lib.rs` (P1): `DaemonComposition.committed_experience:
   BTreeMap<String, WriteReceipt>` (key → owner receipt), init in
   `start()`, `committed_experience_receipt(&self, key)` accessor,
   `note_experience_committed(&mut self, key, receipt)` recorder with
   bounded growth (cap; oldest-first eviction is NOT needed — keys are
   per-record deterministic and few; document bound instead).
2. `bins/eliotd/src/experience_runtime.rs` (P1): in
   `commit_experience_event_records`, compute each record's commit key
   first; if retained, reuse the receipt WITHOUT re-deriving ingress
   or re-submitting; else submit and retain on success. Update docs:
   retry converges by construction; triple rule untouched; no new
   operation identities.
3. `crates/smart/eliot-reactive-context-plan/src/reactive_owner_producers.rs`
   (new): six pure producers over live owner artifacts —
   `produce_planning_view` (lossless AUV→CPV via `::new`),
   `produce_cue_activation` (exact A10 pair + target→atom bindings vs
   the view), `produce_session_snapshot`, `produce_attention_projection`,
   `produce_coverage_profile` (adopt + cross-check fence/session/task
   binding vs the view), `produce_delivery_policy` (validate + digest).
   Missing artifact is NOT an error here — absence is expressed in
   suppliers (Withheld, never defaulted).
4. `crates/smart/eliot-reactive-context-plan/src/reactive_owner_suppliers.rs`
   (new): `ReactiveOwnerSupply` (six `Option` artifact bundles),
   `ReactiveMissingOwner` inventory, `missing_owners()`,
   `assemble()` → `AssembledReactiveInputs` or the missing list.
5. `bins/eliotd/src/reactive_feed.rs` (new):
   `drive_reactive_delivery_once(supply) -> ReactiveFeedOutcome`
   (`Planned` / `NoInjection` / `Withheld { missing }` /
   `Failed(error)` with identities preserved): Withheld on any missing
   owner; else assemble → `plan_pending_context_injection`; map the
   three plan results 1:1. Pure per-evaluation sequencing (no loop, no
   timer — scheduling semantics are forbidden in the binary); retains
   nothing across calls (P2 by construction); fence/digest mismatches
   → `Failed`, never planned.
6. Registrations: `mod` + `pub use` in both `lib.rs`; `eliotd`
   `Cargo.toml` += `eliot-reactive-context-plan` path dep. No other
   manifests. No Kernel/Host/Governor/Store edits.

## Explicitly NOT ported from O1 (out of lane or owned elsewhere)

- `experience_audit.rs` (+ 8f4 outcome/heads corroboration): O1 trigger
  file; trigger wiring belongs to the O1/M2 continuation.
- `owner_feed.rs`, `owner_publish.rs`, daemon_runtime trigger hunks:
  daemon trigger lane.
- `session_envelope.rs` × 6 + Kernel/Host `lib.rs` hunks: MGR-A
  (Kernel/Host roots) + session-envelope owners.
- Workscope `caller/guard/identity/issuance/resolver`,
  `scope_identity_admission.rs`, Governor `composition.rs` hunks:
  Governor/workscope owners.
- `scope_observe.rs` CLI, `attempt_execution_chain.rs`,
  `experience_audit.rs`, `testd_terminal_completion` hunks: other lanes.
- D1/D2/O2 branches (`work/1942-d1/d2/o2-*`, `f606f35b`): separate
  writers; not consumed (one writer per file).
- `docs/architecture/route-rules.toml` hunk: docs lane.

## MGR-A requests (lines needed, not taken)

- None required for this slice: no Kernel/Host root reads beyond the
  existing `DaemonKernelClient`/`context_read_client` seam, which is
  already consumed read-only by `experience_runtime.rs`. If the trigger
  lane later needs session-envelope Kernel/Host producers, that is O1's
  request to file, not this slice's.

## Gate (brief-mandated; no test campaigns)

- `cargo fmt` (touched crates surface)
- `cargo check --locked -p eliotd` (+ reactive plan crate), offline
- `cargo clippy --offline -p <crate> --all-targets --no-deps -- -D warnings`
  (brief text: `clippy --all-targets --no-deps -- -D warnings`),
  compared with main on the same surface
- Record exits. No `cargo test` runs (brief: "No cargo test campaigns";
  issue allows only smallest acceptance proof — deferred to the
  integration owner with the pending trigger).
- Commit `feat(...)(#1942)` + `Ported-from:
  work/1942-o1-session-envelopes@8f4fc606` + `Refs #1942`, push `-u
  origin work/1942-reactive-producers`. `REPORT-1942.md`, Russian
  progress comment (no close), `status.md` append — where `status.md`
  lives is resolved at report time (not in this tree scope).

## Residual unknowns

- Live session/attention/policy owner read endpoints in the daemon
  trigger do not exist on main yet (Governor session projection is
  named as provider in module.toml but has no callable serving the
  snapshot shape); the feed therefore idles `Withheld` until the
  owning lanes serve them. That is the designed honest state, not a
  gap in this slice.
- Influence/use/outcome updates (issue Work bullet 4) ride the
  delivery owner's observed receipts (bridge lane) — not this slice.
