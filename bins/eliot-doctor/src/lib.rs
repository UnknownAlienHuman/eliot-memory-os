#![forbid(unsafe_code)]

//! Doctor's binary crate intentionally exposes only the canonical contract.
//! Admission and effects belong to the authenticated Kernel client.

pub use eliot_doctor_core::*;
