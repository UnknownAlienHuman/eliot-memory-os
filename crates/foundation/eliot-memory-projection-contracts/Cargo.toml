[package]
name = "eliot-memory-projection-contracts"
version = "0.1.0"
edition = "2024"
rust-version = "1.94"
license = "MIT"
publish = false
description = "Owner-neutral bounded canonical memory projection and applicability-set contracts (CC-008)"

[lib]
path = "src/lib.rs"

[package.metadata.eliot]
prototype = true
source_status = "IMPLEMENTED_PROTOTYPE"
workspace_admission = "pending_agent_implementation_and_proof"
functional_cell = "foundation.memory.projection-contracts"
source_layer = "C0"
agent_order = 44

[dependencies]
eliot-contracts = { path = "../eliot-contracts", version = "0.1.0" }
eliot-evidence = { path = "../eliot-evidence", version = "0.1.0" }
eliot-receipts = { path = "../eliot-receipts", version = "0.1.0" }
schemars = "1.2.1"
serde = { version = "1.0.228", features = ["derive"] }
thiserror = "2.0.18"

[dev-dependencies]
serde_json = "1.0.150"

[lints]
workspace = true
