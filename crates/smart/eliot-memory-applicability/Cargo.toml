[package]
name = "eliot-memory-applicability"
version = "0.1.0"
edition = "2024"
rust-version = "1.94"
license = "MIT"
publish = false
description = "Purely evaluate bounded canonical memory projection records for current applicability."

[lib]
path = "src/lib.rs"

[package.metadata.eliot]
prototype = true
source_status = "NOT_IMPLEMENTED"
workspace_admission = "pending_contract_package_provider_consumer_edge_and_product_proof"
functional_cell = "smart.memory.applicability"
source_layer = "C1"
runtime_layer = "R5"
runtime_bundle = "eliotd"
agent_order = 47

[dependencies]
eliot-contracts = { path = "../../foundation/eliot-contracts" }
eliot-evidence = { path = "../../foundation/eliot-evidence" }
eliot-memory-projection-contracts = { path = "../../foundation/eliot-memory-projection-contracts" }
