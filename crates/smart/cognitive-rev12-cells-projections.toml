schema_version = 3
authority = "eliot-cognitive-function-design-rev12"
status = "STATIC_FUNCTION_AND_W8_FIELD_CONTRACT_DESIGN_ONLY"
orders = "68..75"
cell_count = 8
field_contract_index = "crates/smart/cognitive-rev12-field-contracts.toml"
owner_resolution = "ContextProviderProjectionRequest is owned once by eliot-context-contracts and used by every Context provider; no provider-specific public request type may duplicate it."

[[cell]]
order = 68
id = "smart.memory.projection.contracts"
stage = "W8_CANONICAL_PROJECTION_BRIDGES"
physical_crate = "eliot-memory-projection-contracts"
hot_path = false
responsibility = "Own provider-neutral bounded canonical-memory read, query, selection and context-projection contracts without defining storage or lifecycle."
field_contract = "crates/smart/eliot-memory-projection-contracts/projection.contract.toml"
public_functions = ["pub fn validate_memory_read_snapshot(snapshot: &CanonicalMemoryReadSnapshot) -> Result<(), MemoryProjectionContractError>", "pub fn validate_memory_query_intent(intent: &MemoryQueryIntent, cues: &CueActivationResult) -> Result<(), MemoryProjectionContractError>", "pub fn validate_memory_selection_policy(policy: &MemorySelectionPolicy) -> Result<(), MemoryProjectionContractError>", "pub fn validate_memory_candidate_set(set: &MemoryCandidateSet) -> Result<(), MemoryProjectionContractError>", "pub fn validate_memory_context_projection_request(request: &MemoryContextProjectionRequest, candidates: &MemoryCandidateSet) -> Result<(), MemoryProjectionContractError>"]
public_types = ["MemoryReadCoverage", "MemoryRecordKind", "MemoryAccessibility", "PermittedInfluence", "MemoryCoverageDenominator", "CanonicalMemoryReadRequest", "MemoryProjectionRecord", "CanonicalMemoryReadSnapshot", "MemoryQueryKind", "MemoryQueryIntent", "MemorySelectionDimension", "SelectionDimensionResult", "MemorySelectionAssessment", "PartialCoverageBehavior", "MemorySelectionPolicy", "MemoryCandidateRepresentation", "MemoryCandidate", "MemorySelectionDisposition", "MemorySelectionTraceEntry", "MemorySelectionTrace", "MemoryCandidateSet", "MemoryContextProjectionRequest"]
algorithm = ["validate exact Governor read operation identity, WorkScope, State Fence, canonical revision, receipt and frozen denominator", "require every record to match request scope and fence", "keep status, lifecycle, accessibility and permitted influence independent", "bind intent to one CueActivationResult and immutable selection policy", "bound candidate and payload representations", "canonicalize order and digest"]
invariants = ["contract owns no storage or canonical write client", "Complete requires exact recheckable denominator", "searched state requires a read receipt", "Unavailable or Blocked names missing owner/reason", "retrieval cannot reinforce support", "accessibility and influence cannot be inferred from existence"]
complexity = "O(n log n) validation over policy-bounded records; no I/O or model call."
degradation = "Partial or stale canonical read remains an explicit coverage state; guessed records are forbidden."
errors = ["ReadIdentityInvalid", "ScopeMismatch", "FenceMismatch", "CanonicalRevisionMissing", "ReadReceiptMissing", "DenominatorMissing", "MissingOwnerReason", "DuplicateHandle", "ProjectionDimensionCollapsed", "LimitExceeded"]
tests = ["empty-complete differs from unavailable", "Complete without expected count rejected", "wrong-scope record rejected", "searched state without receipt rejected", "Unavailable without owner/reason rejected", "support/accessibility/influence remain separate", "record order does not change digest"]
source_files = ["crates/smart/eliot-memory-projection-contracts/src/contracts.rs", "crates/smart/eliot-memory-projection-contracts/tests/contracts.rs"]
product_pulse = "D3B_CANONICAL_PROJECTION_PULSE_01"
architecture_refs = ["ARCH-SEC-02", "ARCH-MEM-04", "ARCH-CTX-04", "ARCH-EPI-04", "ARCH-SCOPE-01"]
implementation_refs = ["I5.20 named reads", "I12 memory projection", "I7.19 reactive context", "I2.20"]
owned_mutable_state = "NONE"
allowed_effects = []
proof_ceiling = "STATIC_FIELD_CONTRACT_ONLY"

[[cell]]
order = 69
id = "smart.memory.candidate_query"
stage = "W8_CANONICAL_PROJECTION_BRIDGES"
physical_crate = "eliot-memory-projection"
hot_path = true
responsibility = "Select a bounded deterministic memory candidate set from one already materialized Governor read snapshot and current cue/query intent."
field_contracts = ["crates/smart/eliot-memory-projection-contracts/projection.contract.toml"]
public_functions = ["pub fn query_memory_candidates(snapshot: &CanonicalMemoryReadSnapshot, intent: &MemoryQueryIntent, cues: &CueActivationResult, policy: &MemorySelectionPolicy) -> Result<MemoryCandidateSet, MemoryCandidateQueryError>"]
algorithm = ["validate snapshot, intent, cue result, policy and one State Fence", "exclude denied influence while retaining suppression trace", "select exact cue-linked records before broader filters", "protect negative memory, rivals, unknowns, minority and counterevidence", "evaluate applicability, freshness, provenance, decision relevance and activation precision separately", "emit bounded result, frozen denominator, omissions and full trace"]
invariants = ["query does not mark delivered, used or helpful", "retrieval count does not change support", "activation score is not epistemic weight", "unknown applicability is not zero relevance", "exact negative memory cannot be displaced by popularity"]
complexity = "O(n log n + c log n) over a request-bounded materialized snapshot; no storage read, model call or session mutation."
degradation = "Partial snapshot yields partial candidates and explicit missing coverage; mixed fences fail closed."
errors = ["SnapshotInvalid", "IntentInvalid", "PolicyInvalid", "FenceMismatch", "CueSnapshotMismatch", "InfluenceProjectionMissing", "SelectionLimitInvalid"]
tests = ["exact cue precedes broad candidate", "query is side-effect free", "negative memory retained", "minority/counterevidence retained", "partial denominator visible", "trace contains every considered record", "permutation deterministic"]
source_files = ["crates/smart/eliot-memory-projection/src/candidate_query.rs", "crates/smart/eliot-memory-projection/tests/candidate_query.rs"]
product_pulse = "D3B_CANONICAL_PROJECTION_PULSE_01"
architecture_refs = ["ARCH-CTX-02", "ARCH-CTX-04", "ARCH-MEM-04"]
implementation_refs = ["I5.20 read model", "I12.14 retrieval", "I7.19 reactive sequence"]
owned_mutable_state = "NONE"
allowed_effects = []
proof_ceiling = "STATIC_FUNCTION_DESIGN_ONLY_WITH_FROZEN_INPUT_CONTRACTS"

[[cell]]
order = 70
id = "smart.memory.context_projection"
stage = "W8_CANONICAL_PROJECTION_BRIDGES"
physical_crate = "eliot-memory-projection"
hot_path = true
responsibility = "Translate a bounded MemoryCandidateSet into whole owner-attributed ProviderAtoms without granting admission or leaking inaccessible payloads."
field_contracts = ["crates/smart/eliot-memory-projection-contracts/projection.contract.toml", "crates/smart/eliot-context-contracts/provider-contribution.contract.toml"]
public_functions = ["pub fn project_memory_context(request: &MemoryContextProjectionRequest, candidates: &MemoryCandidateSet) -> Result<ProviderContribution, MemoryContextProjectionError>"]
algorithm = ["validate provider request, candidate and trace digests and fence", "map each candidate to whole payload, exact excerpt or exact handle according to accessibility and disclosure", "preserve epistemic status, assertability, negative-memory, rivals, unknowns, cues and provenance", "represent decision delta as Known, BoundedRange, Unknown or NotApplicable", "emit provider coverage, omissions and frozen denominator"]
invariants = ["projection cannot admit itself", "inaccessible payload is never copied", "handle-only representation remains expandable", "record status and influence ceiling are unchanged", "each role is a whole addressable unit", "ProviderContribution contains no admission or delivery state"]
complexity = "O(n log n + selected representation bytes); no canonical read, model call or admission decision."
degradation = "Optional unrenderable items become handle-only or explicit omissions; a required protected item without a safe form blocks projection."
errors = ["CandidateSetMismatch", "TraceMismatch", "FenceMismatch", "DisclosureDenied", "AccessibilityUnknown", "RequiredRepresentationMissing", "ProjectionLimitExceeded"]
tests = ["inaccessible payload not leaked", "status preserved", "unknown decision delta preserved", "protected handle retained", "complete coverage requires denominator", "no admission disposition"]
source_files = ["crates/smart/eliot-memory-projection/src/context_projection.rs", "crates/smart/eliot-memory-projection/tests/context_projection.rs"]
product_pulse = "D3B_CANONICAL_PROJECTION_PULSE_01"
architecture_refs = ["ARCH-CTX-01", "ARCH-CTX-04", "ARCH-MEM-03"]
implementation_refs = ["I7.11 Decision Safety Floor", "I12 Context Compiler inputs"]
owned_mutable_state = "NONE"
allowed_effects = []
proof_ceiling = "STATIC_FUNCTION_DESIGN_ONLY_WITH_FROZEN_INPUT_CONTRACTS"

[[cell]]
order = 71
id = "smart.learning.contracts.experience"
stage = "W8_CANONICAL_PROJECTION_BRIDGES"
physical_crate = "eliot-learning-contracts"
hot_path = false
responsibility = "Own immutable attempt, failure, trace, artifact, evaluator, effect and experience-projection contracts shared by learning and diagnosis."
field_contract = "crates/smart/eliot-learning-contracts/experience.contract.toml"
public_functions = ["pub fn validate_experience_projection_request(request: &SystemExperienceProjectionRequest) -> Result<(), LearningContractError>", "pub fn validate_canonical_experience_read_snapshot(request: &SystemExperienceProjectionRequest, snapshot: &CanonicalExperienceReadSnapshot) -> Result<(), LearningContractError>", "pub fn validate_failure_observation(identity: &ExperienceIdentity, failure: &FailureObservation) -> Result<(), LearningContractError>", "pub fn validate_system_experience_projection(request: &SystemExperienceProjectionRequest, projection: &SystemExperienceProjection) -> Result<(), LearningContractError>"]
public_types = ["CampaignId", "AttemptId", "ExperienceIdentity", "ExperienceOwnerKind", "ExperienceCoverageState", "ExperienceCoverageDenominator", "ExperienceCoverage", "OutcomeStatus", "OutcomeAttribution", "ObservedOutcome", "FailureTriggerClass", "FailureClass", "FailureObservation", "AttemptExperience", "SystemExperienceProjectionRequest", "CanonicalExperienceReadSnapshot", "SystemExperienceProjection"]
algorithm = ["validate exact task, attempt, scope, owner revisions and State Fence", "require explicit denominator-bearing coverage for task, trace, artifact, evaluator and effect owners", "separate sequence, association, supported contribution, intervention support and unknown attribution", "preserve exact deterministic trigger separately from semantic near-match", "bind snapshot/projection counts to request limits", "canonicalize order and compute immutable digest"]
invariants = ["contract owns no experience store", "success/failure count is not causality", "unknown effect outcome remains unknown", "FailureObservation creates no hard block or lifecycle action", "projection cannot promote a Skill or policy", "owner coverage cannot be filled from transcript prose"]
complexity = "O(n log n) contract validation over bounded projections."
degradation = "Incomplete coverage is valid only as Partial, Stale or Blocked with named missing owners/reasons."
errors = ["ProjectionIdentityInvalid", "AttemptLineageMismatch", "ScopeMismatch", "FenceMismatch", "CoverageInvalid", "AttributionCollapsed", "FailureTriggerCollapsed", "LimitExceeded"]
tests = ["Complete owner without denominator rejected", "blocked owner without reason rejected", "unattributed success remains observation", "unknown effect retained", "near-match has no hard block", "failed attempt retained", "request limit bypass rejected", "order-independent digest"]
source_files = ["crates/smart/eliot-learning-contracts/src/experience.rs", "crates/smart/eliot-learning-contracts/tests/experience.rs"]
product_pulse = "D4_EXPERIENCE_PROJECTION_PULSE_01"
architecture_refs = ["ARCH-LEARN-01", "ARCH-LEARN-02", "ARCH-UND-02", "ARCH-RES-05", "ARCH-EPI-04"]
implementation_refs = ["I12.24 task-local learning", "I16.23 self-quality evidence", "I13 Failure Capsule"]
owned_mutable_state = "NONE"
allowed_effects = []
proof_ceiling = "STATIC_FIELD_CONTRACT_ONLY"

[[cell]]
order = 72
id = "smart.experience.projection"
stage = "W8_CANONICAL_PROJECTION_BRIDGES"
physical_crate = "eliot-experience-projection"
hot_path = false
responsibility = "Build SystemExperienceProjection from exact owner-produced canonical read projections without becoming a second mutable experience owner."
field_contracts = ["crates/smart/eliot-learning-contracts/experience.contract.toml"]
public_functions = ["pub fn project_system_experience(request: &SystemExperienceProjectionRequest, snapshot: &CanonicalExperienceReadSnapshot) -> Result<SystemExperienceProjection, ExperienceProjectionError>"]
algorithm = ["validate request digest, exact task/attempt/scope identity and fence", "join hypothesis/strategy with trace, artifact, evaluator, effect and FailureObservation records by exact identity", "separate observed sequence, outcome attribution, confounders and unknowns", "retain failed attempts and contrary outcomes", "emit frozen owner denominators and digest"]
invariants = ["no mutable experience state", "success is not mechanism proof", "missing effect receipt remains Unknown", "FailureObservation cannot create a block", "projection cannot create learning delta", "eliot-system-experience remains donor-only"]
complexity = "O(n log n) over bounded owner projections; no storage I/O or model call."
degradation = "Incomplete owner coverage yields a partial projection and blocks causal claims while preserving raw handles."
errors = ["RequestMismatch", "SnapshotInvalid", "AttemptLineageMismatch", "ScopeMismatch", "FenceMismatch", "OutcomeUnattributed", "ReceiptMismatch", "CoverageInsufficient", "LimitExceeded"]
tests = ["unattributed success observed only", "failed attempt and failure observation retained", "unknown external outcome visible", "request limits enforced", "projection deterministic", "no mutable store API"]
source_files = ["crates/smart/eliot-experience-projection/src/projection.rs", "crates/smart/eliot-experience-projection/tests/projection.rs"]
product_pulse = "D4_EXPERIENCE_PROJECTION_PULSE_01"
architecture_refs = ["ARCH-CORE-00", "ARCH-LEARN-01", "ARCH-LEARN-02"]
implementation_refs = ["I12.24 learning state", "I9 development diagnosis"]
owned_mutable_state = "NONE"
allowed_effects = []
proof_ceiling = "STATIC_FUNCTION_DESIGN_ONLY_WITH_FROZEN_INPUT_CONTRACTS"

[[cell]]
order = 73
id = "smart.self_model.context_projection"
stage = "W8_CANONICAL_PROJECTION_BRIDGES"
physical_crate = "eliot-dreamer-self-query"
hot_path = true
responsibility = "Project accepted Architecture, Implementation targets and exact current-conformance evidence into bounded self-model ProviderAtoms without self-certification."
field_contracts = ["crates/smart/eliot-context-contracts/provider-contribution.contract.toml"]
public_functions = ["pub fn project_self_model_context(request: &ContextProviderProjectionRequest, architecture: &ArchitectureBrief, implementation: &ImplementationBrief) -> Result<ProviderContribution, SelfModelContextProjectionError>"]
algorithm = ["validate provider class SelfModel, normative pair and brief digests", "separate constitutional, target, implemented, operational, experiential and epistemic states", "select decision-relevant whole atoms within request bounds", "retain exact section/evidence handles and omissions", "emit owner coverage and one fence"]
invariants = ["Architecture precedence remains explicit", "Implementation target is not current support", "prose-only evidence cannot become capability support", "missing conformance becomes Unknown", "projection cannot certify or admit itself", "no provider-specific request type"]
complexity = "O(n log n + selected bytes) over materialized briefs; no document read, repository scan or model call."
degradation = "Stale or missing current evidence produces target-only atoms plus explicit unknowns and blockers."
errors = ["ProviderClassMismatch", "NormativePairMismatch", "BriefDigestMismatch", "FenceMismatch", "CurrentSupportOverclaim", "RequiredInvariantMissing", "ProjectionTooLarge"]
tests = ["generic provider request only", "target/current separated", "wrong pair rejected", "prose-only support rejected", "missing conformance unknown", "omission expandable"]
source_files = ["crates/smart/eliot-dreamer-self-query/src/context_projection.rs", "crates/smart/eliot-dreamer-self-query/tests/context_projection.rs"]
product_pulse = "D3B_CANONICAL_PROJECTION_PULSE_01"
architecture_refs = ["ARCH-SELF-01", "ARCH-UND-01", "ARCH-CTX-01"]
implementation_refs = ["I12.23 self-knowledge", "I7.11 Context Safety Floor"]
owned_mutable_state = "NONE"
allowed_effects = []
proof_ceiling = "STATIC_FUNCTION_DESIGN_ONLY_WITH_FROZEN_INPUT_CONTRACTS"

[[cell]]
order = 74
id = "smart.context.provider_registry_contracts"
stage = "W8_CANONICAL_PROJECTION_BRIDGES"
physical_crate = "eliot-context-contracts"
hot_path = false
responsibility = "Own the closed Context provider registry, expected-provider denominator, whole provider contribution, role coverage and generic provider-projection request contracts."
field_contract = "crates/smart/eliot-context-contracts/provider-contribution.contract.toml"
public_functions = ["pub fn validate_context_provider_registry(registry: &ContextProviderRegistrySnapshot) -> Result<(), ContextContractError>", "pub fn validate_expected_provider_set(expected: &ExpectedContextProviderSet) -> Result<(), ContextContractError>", "pub fn validate_context_provider_request(request: &ContextProviderProjectionRequest) -> Result<(), ContextContractError>", "pub fn validate_provider_contribution(request: &ContextProviderProjectionRequest, contribution: &ProviderContribution) -> Result<(), ContextContractError>", "pub fn provider_contribution_digest(contribution: &ProviderContribution) -> Result<String, ContextContractError>"]
public_types = ["ProviderId", "ContextProviderClass", "ProviderRoleRequirement", "ProviderAvailabilityClass", "ContextRole", "ContextAtomClass", "ContextLossPolicy", "DecisionDelta", "ProviderAtomRepresentation", "ProviderAtom", "ProviderCoverageDisposition", "ProviderCoverageDenominator", "ProviderCoverage", "ProviderOmissionReason", "ProviderOmission", "ContextProviderDescriptor", "ContextProviderRegistrySnapshot", "ExpectedContextProvider", "ExpectedProviderSetDisposition", "ExpectedContextProviderSet", "ContextProviderProjectionRequest", "ProviderContribution"]
algorithm = ["validate exact seven-class provider set and stable identity", "bind generation, contract identity and supported roles", "require one registry revision and fence", "validate mandatory/optional/advisory providers and role coverage", "validate contribution atoms, omissions and exact denominator against one generic request", "canonicalize and digest registry, expected set and contribution"]
invariants = ["registry owns no provider implementation", "missing mandatory provider is not empty-complete", "one provider cannot silently claim another role", "Complete coverage requires exact denominator", "all providers use the one generic request type", "ProviderContribution has no admission, delivery, use, authority or finish field"]
complexity = "O((p + r + a + o) log n) for policy-bounded providers, roles, atoms and omissions."
degradation = "Unavailable optional/advisory provider stays explicit; missing mandatory provider yields Blocked; partial contribution preserves omissions and expansion handles."
errors = ["UnknownProviderClass", "ProviderDuplicate", "RoleUnsupported", "RegistryStale", "FenceMismatch", "MandatoryProviderMissing", "DenominatorMissing", "ContributionMismatch", "UnsafeRepresentation"]
tests = ["all legacy/extra provider classes rejected", "duplicate rejected", "missing mandatory visible", "Complete without expected count rejected", "role overclaim rejected", "order-independent digest", "single request type used by all providers", "contribution has no admission disposition"]
source_files = ["crates/smart/eliot-context-contracts/src/provider_registry.rs", "crates/smart/eliot-context-contracts/src/provider_contribution.rs", "crates/smart/eliot-context-contracts/tests/provider_registry.rs", "crates/smart/eliot-context-contracts/tests/provider_contribution.rs"]
product_pulse = "D3B_CANONICAL_PROJECTION_PULSE_01"
architecture_refs = ["ARCH-MOD-03", "ARCH-CTX-01", "ARCH-CTX-04", "ARCH-EPI-04"]
implementation_refs = ["I12 Context Compiler", "I7.11 provider coverage", "I2.20"]
owned_mutable_state = "NONE"
allowed_effects = []
proof_ceiling = "STATIC_FIELD_CONTRACT_ONLY"

[[cell]]
order = 75
id = "smart.epistemic.context_projection"
stage = "W8_CANONICAL_PROJECTION_BRIDGES"
physical_crate = "eliot-epistemic"
hot_path = true
responsibility = "Translate CurrentEpistemicPosition into an owner-attributed ProviderContribution while preserving every epistemic role."
field_contracts = ["crates/smart/eliot-context-contracts/provider-contribution.contract.toml"]
public_functions = ["pub fn project_epistemic_context(request: &ContextProviderProjectionRequest, position: &CurrentEpistemicPosition) -> Result<ProviderContribution, EpistemicContextProjectionError>"]
algorithm = ["validate provider class EpistemicPosition, position digest, expected provider identity and fence", "map position roles to whole ProviderAtoms without merging status", "retain assumptions, rivals, conflict participants, stale/superseded handles, unknowns and investigation requirements", "protect load-bearing conflict, unknown and counterevidence atoms", "emit provider coverage, omissions and exact denominator"]
invariants = ["Assumed never becomes Supported", "conflict is not flattened into one warning string", "minority and counterevidence remain addressable", "projection cannot rank against other providers or admit itself", "source handles and fence are preserved", "no provider-specific request type"]
complexity = "O(n log n + selected bytes) over bounded CurrentEpistemicPosition; no canonical read, model call or I/O."
degradation = "Partial position produces partial contribution with missing owners and unknowns; invalid fence fails closed."
errors = ["ProviderClassMismatch", "PositionDigestMismatch", "FenceMismatch", "ProviderIdentityMismatch", "RequiredRoleUnrepresentable", "ProjectionLimitExceeded"]
tests = ["generic provider request only", "assumption remains assumption", "rival/counterevidence retained", "local conflict structured", "partial coverage visible", "no admission disposition"]
source_files = ["crates/smart/eliot-epistemic/src/context_projection.rs", "crates/smart/eliot-epistemic/tests/context_projection.rs"]
product_pulse = "D3B_CANONICAL_PROJECTION_PULSE_01"
architecture_refs = ["ARCH-EPI-01", "ARCH-CTX-01", "ARCH-UND-01"]
implementation_refs = ["I12 Current Epistemic Position", "I7.11 Decision Safety Floor"]
owned_mutable_state = "NONE"
allowed_effects = []
proof_ceiling = "STATIC_FUNCTION_DESIGN_ONLY_WITH_FROZEN_INPUT_CONTRACTS"
