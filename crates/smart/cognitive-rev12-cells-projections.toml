schema_version = 2
authority = "eliot-cognitive-function-design-rev12"
status = "STATIC_FUNCTION_DESIGN_ONLY"
orders = "68..75"
cell_count = 8
owner_resolution = "ContextProviderProjectionRequest is owned once by eliot-context-contracts and used by every Context provider; no provider-specific public request type may duplicate it."

[[cell]]
order = 68
id = "smart.memory.projection.contracts"
stage = "W8_CANONICAL_PROJECTION_BRIDGES"
physical_crate = "eliot-memory-projection-contracts"
hot_path = false
responsibility = "Own provider-neutral bounded canonical-memory read, candidate, selection and context-projection contracts without defining storage or lifecycle."
public_functions = ["pub fn validate_memory_read_snapshot(snapshot: &CanonicalMemoryReadSnapshot) -> Result<(), MemoryProjectionContractError>", "pub fn validate_memory_candidate_set(set: &MemoryCandidateSet) -> Result<(), MemoryProjectionContractError>"]
public_types = ["CanonicalMemoryReadRequest", "CanonicalMemoryReadSnapshot", "MemoryProjectionRecord", "MemoryQueryIntent", "MemorySelectionPolicy", "MemoryCandidate", "MemoryCandidateSet", "MemorySelectionDimension", "MemorySelectionTrace", "MemoryContextProjectionRequest"]
algorithm = ["validate exact Governor read operation identity, WorkScope, State Fence, canonical revision and frozen denominator", "require immutable handles rather than store references", "keep status, lifecycle, accessibility and permitted influence as independent dimensions", "bound candidate and payload representations", "canonicalize order and digest"]
invariants = ["contract owns no storage or canonical write client", "missing denominator differs from empty complete result", "retrieval cannot reinforce support", "accessibility and influence cannot be inferred from existence"]
complexity = "O(n log n) validation over policy-bounded records; no I/O or model call."
degradation = "Partial or stale canonical read remains an explicit coverage state; guessed records are forbidden."
errors = ["ReadIdentityInvalid", "FenceMismatch", "CanonicalRevisionMissing", "DenominatorMissing", "DuplicateHandle", "ProjectionDimensionCollapsed", "LimitExceeded"]
tests = ["empty-complete differs from unavailable", "support/accessibility/influence remain separate", "duplicate handle rejected", "record order does not change digest"]
source_files = ["crates/smart/eliot-memory-projection-contracts/src/contracts.rs", "crates/smart/eliot-memory-projection-contracts/tests/contracts.rs"]
product_pulse = "D3B_CANONICAL_PROJECTION_PULSE_01"
architecture_refs = ["ARCH-SEC-02", "ARCH-MEM-04", "ARCH-CTX-04"]
implementation_refs = ["I5.20 named reads", "I12 memory projection", "I7.19 reactive context"]
owned_mutable_state = "NONE"
allowed_effects = []
proof_ceiling = "STATIC_FUNCTION_DESIGN_ONLY"

[[cell]]
order = 69
id = "smart.memory.candidate_query"
stage = "W8_CANONICAL_PROJECTION_BRIDGES"
physical_crate = "eliot-memory-projection"
hot_path = true
responsibility = "Select a bounded deterministic memory candidate set from one already materialized Governor read snapshot and current cue/query intent."
public_functions = ["pub fn query_memory_candidates(snapshot: &CanonicalMemoryReadSnapshot, intent: &MemoryQueryIntent, cues: &CueActivationResult, policy: &MemorySelectionPolicy) -> Result<MemoryCandidateSet, MemoryCandidateQueryError>"]
algorithm = ["validate snapshot, intent, cue result and one State Fence", "exclude denied influence while retaining suppression trace", "select exact cue-linked records before broader filters", "protect negative memory, rivals, unknowns, minority and counterevidence", "evaluate applicability, freshness, provenance, decision relevance and activation precision separately", "emit bounded result, frozen denominator, omissions and trace"]
invariants = ["query does not mark delivered or used", "retrieval count does not change support", "activation score is not epistemic weight", "unknown applicability is not zero relevance", "exact negative memory cannot be displaced by popularity"]
complexity = "O(n log n + c log n) over a request-bounded materialized snapshot; no storage read, model call or session mutation."
degradation = "Partial snapshot yields partial candidates and explicit missing coverage; mixed fences fail closed."
errors = ["SnapshotInvalid", "IntentInvalid", "FenceMismatch", "CueSnapshotMismatch", "InfluenceProjectionMissing", "SelectionLimitInvalid"]
tests = ["exact cue precedes broad candidate", "query is side-effect free", "negative memory retained", "partial denominator visible", "permutation deterministic"]
source_files = ["crates/smart/eliot-memory-projection/src/candidate_query.rs", "crates/smart/eliot-memory-projection/tests/candidate_query.rs"]
product_pulse = "D3B_CANONICAL_PROJECTION_PULSE_01"
architecture_refs = ["ARCH-CTX-02", "ARCH-CTX-04", "ARCH-MEM-04"]
implementation_refs = ["I5.20 read model", "I12.14 retrieval", "I7.19 reactive sequence"]
owned_mutable_state = "NONE"
allowed_effects = []
proof_ceiling = "STATIC_FUNCTION_DESIGN_ONLY"

[[cell]]
order = 70
id = "smart.memory.context_projection"
stage = "W8_CANONICAL_PROJECTION_BRIDGES"
physical_crate = "eliot-memory-projection"
hot_path = true
responsibility = "Translate a bounded MemoryCandidateSet into whole owner-attributed ContextAtoms without granting admission or leaking inaccessible payloads."
public_functions = ["pub fn project_memory_context(request: &MemoryContextProjectionRequest, candidates: &MemoryCandidateSet) -> Result<ProviderContribution, MemoryContextProjectionError>"]
algorithm = ["validate request, candidate digest and fence", "map each record to whole payload, bounded representation or exact handle according to accessibility and disclosure", "preserve epistemic status, assertability, negative-memory, rivals, unknowns, cues and provenance", "represent decision delta as Known, BoundedRange or Unknown", "emit provider coverage and frozen denominator"]
invariants = ["projection cannot admit itself", "inaccessible payload is never copied", "handle-only representation remains expandable", "record status and influence ceiling are unchanged", "each role is a whole addressable unit"]
complexity = "O(n log n + selected representation bytes); no canonical read, model call or admission decision."
degradation = "Optional unrenderable items become handle-only or explicit omissions; a required protected item without a safe form blocks projection."
errors = ["CandidateSetMismatch", "FenceMismatch", "DisclosureDenied", "AccessibilityUnknown", "RequiredRepresentationMissing", "ProjectionLimitExceeded"]
tests = ["inaccessible payload not leaked", "status preserved", "unknown decision delta preserved", "protected handle retained", "no admission disposition"]
source_files = ["crates/smart/eliot-memory-projection/src/context_projection.rs", "crates/smart/eliot-memory-projection/tests/context_projection.rs"]
product_pulse = "D3B_CANONICAL_PROJECTION_PULSE_01"
architecture_refs = ["ARCH-CTX-01", "ARCH-CTX-04", "ARCH-MEM-03"]
implementation_refs = ["I7.11 Decision Safety Floor", "I12 Context Compiler inputs"]
owned_mutable_state = "NONE"
allowed_effects = []
proof_ceiling = "STATIC_FUNCTION_DESIGN_ONLY"

[[cell]]
order = 71
id = "smart.learning.contracts.experience"
stage = "W8_CANONICAL_PROJECTION_BRIDGES"
physical_crate = "eliot-learning-contracts"
hot_path = false
responsibility = "Own immutable attempt, trace, artifact, evaluator, effect and experience-projection contracts shared by learning and diagnosis."
public_functions = ["pub fn validate_experience_projection_request(request: &SystemExperienceProjectionRequest) -> Result<(), LearningContractError>", "pub fn validate_system_experience_projection(projection: &SystemExperienceProjection) -> Result<(), LearningContractError>"]
public_types = ["SystemExperienceProjectionRequest", "CanonicalExperienceReadSnapshot", "AttemptExperience", "OutcomeAttribution", "FailureObservation", "ExperienceCoverage", "SystemExperienceProjection"]
algorithm = ["validate exact task, attempt, owner revisions and State Fence", "require explicit coverage for task, trace, artifact, evaluator and effect owners", "separate sequence, association, supported contribution, intervention support and unknown attribution", "bound evidence handles and canonicalize order", "compute immutable projection digest"]
invariants = ["contract owns no experience store", "success/failure count is not causality", "unknown effect outcome remains unknown", "projection cannot promote a Skill or policy", "owner coverage cannot be filled from transcript prose"]
complexity = "O(n log n) contract validation over bounded projections."
degradation = "Incomplete coverage is valid only as Partial or Blocked with named missing owners."
errors = ["ProjectionIdentityInvalid", "AttemptLineageMismatch", "FenceMismatch", "CoverageInvalid", "AttributionCollapsed", "LimitExceeded"]
tests = ["unattributed success remains observation", "unknown effect retained", "partial owner coverage visible", "order-independent digest"]
source_files = ["crates/smart/eliot-learning-contracts/src/experience.rs", "crates/smart/eliot-learning-contracts/tests/experience.rs"]
product_pulse = "D4_EXPERIENCE_PROJECTION_PULSE_01"
architecture_refs = ["ARCH-LEARN-01", "ARCH-LEARN-02", "ARCH-UND-02"]
implementation_refs = ["I12.24 task-local learning", "I16.23 self-quality evidence"]
owned_mutable_state = "NONE"
allowed_effects = []
proof_ceiling = "STATIC_FUNCTION_DESIGN_ONLY"

[[cell]]
order = 72
id = "smart.experience.projection"
stage = "W8_CANONICAL_PROJECTION_BRIDGES"
physical_crate = "eliot-experience-projection"
hot_path = false
responsibility = "Build SystemExperienceProjection from exact owner-produced canonical read projections without becoming a second mutable experience owner."
public_functions = ["pub fn project_system_experience(request: &SystemExperienceProjectionRequest, snapshot: &CanonicalExperienceReadSnapshot) -> Result<SystemExperienceProjection, ExperienceProjectionError>"]
algorithm = ["validate exact task, attempt, canonical revision and fence", "join hypothesis/strategy with trace, artifact, evaluator and effect records by exact identity", "separate observed sequence, outcome attribution, confounders and unknowns", "retain failed attempts and contrary outcomes", "emit frozen denominator and digest"]
invariants = ["no mutable experience state", "success is not mechanism proof", "missing effect receipt remains Unknown", "projection cannot create learning delta", "eliot-system-experience remains donor-only"]
complexity = "O(n log n) over bounded owner projections; no storage I/O or model call."
degradation = "Incomplete owner coverage yields a partial projection and blocks causal claims while preserving raw handles."
errors = ["SnapshotInvalid", "AttemptLineageMismatch", "FenceMismatch", "OutcomeUnattributed", "ReceiptMismatch", "CoverageInsufficient"]
tests = ["unattributed success observed only", "failed attempt retained", "unknown external outcome visible", "projection deterministic", "no mutable store API"]
source_files = ["crates/smart/eliot-experience-projection/src/projection.rs", "crates/smart/eliot-experience-projection/tests/projection.rs"]
product_pulse = "D4_EXPERIENCE_PROJECTION_PULSE_01"
architecture_refs = ["ARCH-CORE-00", "ARCH-LEARN-01", "ARCH-LEARN-02"]
implementation_refs = ["I12.24 learning state", "I9 development diagnosis"]
owned_mutable_state = "NONE"
allowed_effects = []
proof_ceiling = "STATIC_FUNCTION_DESIGN_ONLY"

[[cell]]
order = 73
id = "smart.self_model.context_projection"
stage = "W8_CANONICAL_PROJECTION_BRIDGES"
physical_crate = "eliot-dreamer-self-query"
hot_path = true
responsibility = "Project accepted Architecture, Implementation targets and exact current-conformance evidence into bounded self-model ContextAtoms without self-certification."
public_functions = ["pub fn project_self_model_context(request: &SelfModelContextProjectionRequest, architecture: &ArchitectureBrief, implementation: &ImplementationBrief) -> Result<ProviderContribution, SelfModelContextProjectionError>"]
public_types = ["SelfModelContextProjectionRequest"]
algorithm = ["validate normative pair and brief digests", "separate constitutional, target, implemented, operational, experiential and epistemic states", "select decision-relevant atoms within policy", "retain exact section/evidence handles and omissions", "emit owner coverage and one fence"]
invariants = ["Architecture precedence remains explicit", "Implementation target is not current support", "prose-only evidence cannot become capability support", "missing conformance becomes Unknown", "projection cannot certify itself"]
complexity = "O(n log n + selected bytes) over materialized briefs; no document read, repository scan or model call."
degradation = "Stale or missing current evidence produces target-only atoms plus explicit unknowns and blockers."
errors = ["NormativePairMismatch", "BriefDigestMismatch", "FenceMismatch", "CurrentSupportOverclaim", "RequiredInvariantMissing", "ProjectionTooLarge"]
tests = ["target/current separated", "wrong pair rejected", "prose-only support rejected", "missing conformance unknown", "omission expandable"]
source_files = ["crates/smart/eliot-dreamer-self-query/src/context_projection.rs", "crates/smart/eliot-dreamer-self-query/tests/context_projection.rs"]
product_pulse = "D3B_CANONICAL_PROJECTION_PULSE_01"
architecture_refs = ["ARCH-SELF-01", "ARCH-UND-01", "ARCH-CTX-01"]
implementation_refs = ["I12.23 self-knowledge", "I7.11 Context Safety Floor"]
owned_mutable_state = "NONE"
allowed_effects = []
proof_ceiling = "STATIC_FUNCTION_DESIGN_ONLY"

[[cell]]
order = 74
id = "smart.context.provider_registry_contracts"
stage = "W8_CANONICAL_PROJECTION_BRIDGES"
physical_crate = "eliot-context-contracts"
hot_path = false
responsibility = "Own the closed Context provider registry, expected-provider denominator, role coverage and generic provider-projection request contracts."
public_functions = ["pub fn validate_context_provider_registry(registry: &ContextProviderRegistrySnapshot) -> Result<(), ContextContractError>", "pub fn validate_expected_provider_set(expected: &ExpectedContextProviderSet) -> Result<(), ContextContractError>", "pub fn validate_context_provider_request(request: &ContextProviderProjectionRequest) -> Result<(), ContextContractError>"]
public_types = ["ContextProviderClass", "ContextProviderDescriptor", "ContextProviderRegistrySnapshot", "ExpectedContextProvider", "ExpectedContextProviderSet", "ContextProviderProjectionRequest", "ProviderRoleRequirement", "ProviderAvailabilityClass"]
algorithm = ["validate closed provider class and stable identity", "bind generation, contract identity and supported roles", "require one registry revision and fence", "validate mandatory/optional providers and role coverage", "canonicalize and digest expected set"]
invariants = ["registry owns no provider implementation", "missing mandatory provider is not empty-complete", "one provider cannot silently claim another role", "merge receives an exact denominator", "all providers use the one generic request type"]
complexity = "O(p log p + r log r) for policy-bounded providers and roles."
degradation = "Unavailable optional provider stays explicit; missing mandatory provider yields Partial or Blocked per ContextRecipe."
errors = ["UnknownProviderClass", "ProviderDuplicate", "RoleUnsupported", "RegistryStale", "FenceMismatch", "MandatoryProviderMissing"]
tests = ["duplicate rejected", "missing mandatory visible", "role overclaim rejected", "order-independent digest", "single request type used by all providers"]
source_files = ["crates/smart/eliot-context-contracts/src/provider_registry.rs", "crates/smart/eliot-context-contracts/tests/provider_registry.rs"]
product_pulse = "D3B_CANONICAL_PROJECTION_PULSE_01"
architecture_refs = ["ARCH-MOD-03", "ARCH-CTX-01", "ARCH-CTX-04"]
implementation_refs = ["I12 Context Compiler", "I7.11 provider coverage"]
owned_mutable_state = "NONE"
allowed_effects = []
proof_ceiling = "STATIC_FUNCTION_DESIGN_ONLY"

[[cell]]
order = 75
id = "smart.epistemic.context_projection"
stage = "W8_CANONICAL_PROJECTION_BRIDGES"
physical_crate = "eliot-epistemic"
hot_path = true
responsibility = "Translate CurrentEpistemicPosition into an owner-attributed ProviderContribution while preserving every epistemic role."
public_functions = ["pub fn project_epistemic_context(request: &ContextProviderProjectionRequest, position: &CurrentEpistemicPosition) -> Result<ProviderContribution, EpistemicContextProjectionError>"]
algorithm = ["validate position digest, expected provider identity and fence", "map position roles to whole ContextAtoms without merging status", "retain assumptions, rivals, conflict participants, stale/superseded handles, unknowns and investigation requirements", "protect load-bearing conflict, unknown and counterevidence atoms", "emit provider coverage and exact denominator"]
invariants = ["Assumed never becomes Supported", "conflict is not flattened into one warning string", "minority and counterevidence remain addressable", "projection cannot rank against other providers or admit itself", "source handles and fence are preserved"]
complexity = "O(n log n + selected bytes) over bounded CurrentEpistemicPosition; no canonical read, model call or I/O."
degradation = "Partial position produces partial contribution with missing owners and unknowns; invalid fence fails closed."
errors = ["PositionDigestMismatch", "FenceMismatch", "ProviderIdentityMismatch", "RequiredRoleUnrepresentable", "ProjectionLimitExceeded"]
tests = ["assumption remains assumption", "rival/counterevidence retained", "local conflict structured", "partial coverage visible", "no admission disposition"]
source_files = ["crates/smart/eliot-epistemic/src/context_projection.rs", "crates/smart/eliot-epistemic/tests/context_projection.rs"]
product_pulse = "D3B_CANONICAL_PROJECTION_PULSE_01"
architecture_refs = ["ARCH-EPI-01", "ARCH-CTX-01", "ARCH-UND-01"]
implementation_refs = ["I12 Current Epistemic Position", "I7.11 Decision Safety Floor"]
owned_mutable_state = "NONE"
allowed_effects = []
proof_ceiling = "STATIC_FUNCTION_DESIGN_ONLY"
