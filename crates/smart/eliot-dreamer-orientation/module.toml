schema_version = 4
module_id = "smart.dreamer.orientation"
crate = "eliot-dreamer-orientation"
status = "PROTOTYPE"
wave = "cognitive-micromodules-wave-01"
agent_order = 6
plane = "Smart"
runtime_layer = "R6"
delivery_depth = "D3a"
source_layer = "C1"
runtime_bundle = "eliot-dreamer"
state_class = "stateless"
hot_path = false
lifecycle_owner = "smart.dreamer.orientation"
loc_soft_ceiling = 10000
loc_policy = "review ceiling only; causal cohesion, proof, owner, build/test closure and replacement boundary decide topology"
product_objective = "A current goal receives a bounded source-linked problem-oriented packet before self-query, curation or advanced planning expansion."
causal_property = "basic problem-oriented DreamPacket synthesis"
old_failure_or_missing_capability = "The legacy generator mixes basic Orientation with self-query briefs, clarification and unrelated subtype semantics."
discriminator = "Generic project Orientation closes without ArchitectureBrief or ImplementationBrief dependencies and preserves rivals, unknowns and probes."
primary_responsibility = "Convert one validated Orientation draft plus a bounded bundle and Current Epistemic Position handles into a candidate-only DreamPacket."
owned_mutable_state = []
allowed_effects = []
inputs = ["DreamJobInput class=Orientation", "DreamInputBundle", "ValidatedDreamDraft", "CurrentEpistemicPosition handles"]
outputs = ["DreamPacket candidate", "orientation coverage and quality report", "invalidation conditions"]
depends_on = ["eliot-epistemic", "eliot-dreamer-contracts", "eliot-dreamer-bundle", "eliot-dreamer-candidate-validation"]
providers = ["Governor-admitted bundle", "Current Epistemic Position projection", "Dreamer common contracts"]
consumers = ["Main Agent or Human Dreamer surface", "D3a Orientation Product Pulse"]
non_goals = ["Architecture/self query projection", "Clarification question selection", "source acquisition", "epistemic resolution", "task action or finish decision"]
invariants = ["resolved evidence and epistemic fields come only from admitted projections", "rivals and dissent are not smoothed away", "unknowns, missing coverage and invalidation conditions remain visible", "generic Orientation has no ELIOT self-scope prerequisite"]
failure_behavior = "Return partial, rejected or unsupported Orientation with exact missing providers; never fabricate evidence or silently request a self-query."
proof_surface = ["model-authored confirmation attempt", "rival and unknown preservation", "generic project orientation", "missing epistemic position"]
proof_ceiling = "Module proof only; real provider/consumer Edge proof and Product Pulse remain separate"
product_pulse = "D3A_ORIENTATION_PULSE_01"
replacement_boundary = "implementation behind the declared public contract; package ownership creates no runtime or semantic authority"
merge_rejoin_condition = "merge back when no independent consumer, proof, dependency or replacement seam remains"
split_trigger = "a second causal responsibility, mutable owner, independent proof surface or independent rollback boundary appears"

[[donor]]
path = "crates/smart/eliot-dreamer-core/src/lib.rs"
disposition = "extract candidate-only packet builder"

[[donor]]
path = "ELIOT_IMPLEMENTATION I9.3-I9.5"
disposition = "authoritative Orientation and DreamPacket contract"

[[donor]]
path = "ELIOT_ARCHITECTURE A7.3"
disposition = "Dreamer Orientation channel boundary"

[agent_task]
objective = "Close the first useful D3a Orientation path and stop expansion if its Product Pulse has no user or agent delta."
write_scope = ["crates/smart/eliot-dreamer-orientation/**"]
deliverables = ["typed Orientation handler", "rival/unknown/lineage fixtures", "provider-consumer Edge fixtures"]
acceptance = ["candidate-only DreamPacket", "all material statements retain admitted lineage", "no self-query or clarification semantics"]
forbidden = ["canonical writes outside the named Governor transition path", "truth/support/authority/policy/finish/effect promotion", "provider or model SDKs in pure cognitive cores", "vendor or storage types in public contracts", "unbounded synchronous work", "silent truncation", "oracle or verifier weakening in the same work unit", "implicit scope widening", "active-generation self-modification"]
stop_condition = "Stop and return ContractChallenge if Orientation requires source acquisition, self-query projection, clarification delivery, canonical transition or another handler's responsibility."
challenge_path = "return ContractChallenge to the integration owner when owner, discriminator, proof ceiling or dependency boundary is wrong"
integration_owner = "cognitive-wave-integrator"
implementation_language = "Rust 2024"
required_verification = ["module/package proof at the declared proof ceiling", "real provider/consumer fixture for every changed edge", "the named Product Pulse or Edge proof before promotion", "exact donor reuse/rewrite/reject trace"]
writeback = "return implementation artifact, exact proof evidence, donor disposition, unresolved ContractChallenges and merge/remove recommendation"
workspace_admission = "forbidden until implementation, package proof, affected Edge proof, current-main ancestry and integration-owner review"

# Machine-checked completion gate, read by `scripts/verify-work-unit.py`.
# `cargo fmt|test|clippy` all pass on an empty crate, so they cannot tell a
# finished cell from an unstarted one. This table can.
#
# These are the floors every cell shares. The owning work unit tightens them
# with `required_exports` and `required_tests` taken from its own brief, so a
# reviewer decides on one exit code instead of reading prose criteria.
[acceptance]
min_source_lines = 200
min_public_items = 6
min_tests = 5
require_deny_unknown_fields = true
forbid_unsafe = true
required_exports = []
required_tests = []
forbidden_patterns = ['serde_json::Value', 'unimplemented!', 'todo!']
