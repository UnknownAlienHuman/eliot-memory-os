schema_version = 3
module_id = "smart.dreamer.orientation"
crate = "eliot-dreamer-orientation"
status = "PROTOTYPE"
wave = "cognitive-micromodules-wave-01"
agent_order = 9
plane = "Smart"
runtime_layer = "R6"
delivery_depth = "D3a"
source_layer = "C1"
runtime_bundle = "eliot-dreamer"
state_class = "stateless"
hot_path = false
lifecycle_owner = "smart.dreamer.orientation"
loc_soft_ceiling = 10000
loc_policy = "review ceiling only; causal cohesion, proof, owner, build/test closure and replacement boundary decide topology"
product_objective = "Dreamer returns bounded, source-linked candidates that improve orientation or maintenance without becoming an owner or authority."
causal_property = "problem-oriented DreamPacket synthesis"
old_failure_or_missing_capability = "the legacy eliot-dreamer-core combines problem-oriented DreamPacket synthesis with unrelated job contracts, validation or subtype semantics"
discriminator = "the handler proves problem-oriented DreamPacket synthesis through its public contract while source state and authority remain unchanged"
primary_responsibility = "Convert a validated Orientation draft and optional authority-separated self briefs into a DreamPacket with evidence, rivals, hidden relations, gaps and probes."
owned_mutable_state = []
allowed_effects = []
inputs = ["Orientation job", "DreamInputBundle", "ValidatedDreamDraft", "CurrentEpistemicPosition handles", "optional ArchitectureBrief/ImplementationBrief candidates"]
outputs = ["DreamPacket candidate", "orientation coverage/quality report", "invalidation conditions"]
depends_on = ["eliot-dreamer-contracts", "eliot-dreamer-candidate-validation", "eliot-dreamer-architecture-brief", "eliot-dreamer-implementation-brief"]
providers = ["Governor-admitted handles", "Dreamer common contracts"]
consumers = ["Main Agent/Human Dreamer surface", "D3a Product Pulse"]
non_goals = ["source acquisition", "epistemic resolution", "task action/finish decision", "self-certification"]
invariants = ["resolved evidence sections come only from Governor inputs", "rivals are not smoothed away", "self briefs remain visibly separate"]
failure_behavior = "Return partial, rejected or unsupported problem-oriented DreamPacket synthesis with exact gaps; never fabricate evidence or promotion."
proof_surface = ["model-authored confirmation attempt", "rival/unknown preservation", "generic project orientation", "ELIOT self-scope orientation"]
proof_ceiling = "Module proof only; real provider/consumer edge and Product Pulse remain separate"
product_pulse = "D3A_ORIENTATION_PULSE_01"
replacement_boundary = "implementation behind the declared public contract; package ownership creates no runtime or semantic authority"
merge_rejoin_condition = "merge back when no independent consumer, proof, dependency or replacement seam remains"
split_trigger = "a second causal responsibility, mutable owner, independent proof surface or independent rollback boundary appears"

[[donor]]
path = "crates/smart/eliot-dreamer-core/src/lib.rs"
disposition = "extract packet builder"

[[donor]]
path = "ELIOT_IMPLEMENTATION I9.3-I9.5"
disposition = "authoritative contract"

[agent_task]
objective = "Close the first useful D3a orientation path; stop expansion if its Product Pulse has no user/agent delta."
write_scope = ["crates/smart/eliot-dreamer-orientation/**"]
deliverables = ["typed problem-oriented DreamPacket synthesis handler", "negative/adversarial fixtures", "provider/consumer contract fixtures"]
acceptance = ["candidate-only output", "all material statements retain admitted lineage", "no state mutation or external effect"]
forbidden = ["canonical writes outside the named Governor transition path", "truth/support/authority/policy/finish/effect promotion", "provider or model SDKs in pure cognitive cores", "vendor or storage types in public contracts", "unbounded synchronous work", "silent truncation", "oracle or verifier weakening in the same work unit", "implicit scope widening", "active-generation self-modification"]
stop_condition = "Stop if problem-oriented DreamPacket synthesis requires source acquisition, authority, canonical transition or another handler's semantic responsibility."
challenge_path = "return ContractChallenge to the integration owner when owner, discriminator, proof ceiling or dependency boundary is wrong"
integration_owner = "cognitive-wave-integrator"
implementation_language = "Rust 2024"
required_verification = ["module/package proof at the declared proof ceiling", "real provider/consumer fixture for every changed edge", "the named Product Pulse before promotion", "exact donor reuse/rewrite/reject trace"]
writeback = "return implementation artifact, exact proof evidence, donor disposition, unresolved ContractChallenges and merge/remove recommendation"
workspace_admission = "forbidden until implementation, package proof, affected edge proof, current-main rebase and integration-owner review"
