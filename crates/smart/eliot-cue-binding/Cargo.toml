[package]
name = "eliot-cue-binding"
version = "0.1.0"
edition = "2024"
rust-version = "1.94"
license = "MIT"
publish = false
description = "Derive bounded cue-binding candidates from an admitted observation, touched resources and an optional expected-reuse hint."

[lib]
path = "src/lib.rs"

[package.metadata.eliot]
prototype = true
source_status = "IMPLEMENTED"
workspace_admission = "pending_agent_implementation_and_proof"
functional_cell = "smart.cue.binding"
source_layer = "C1"
runtime_layer = "R5"
runtime_bundle = "eliotd"

[dependencies]
eliot-contracts = { path = "../../foundation/eliot-contracts", version = "0.1.0" }
eliot-evidence = { path = "../../foundation/eliot-evidence", version = "0.1.0" }
eliot-cue-contracts = { path = "../eliot-cue-contracts", version = "0.1.0" }
eliot-cue-normalizer = { path = "../eliot-cue-normalizer", version = "0.1.0" }
eliot-observation = { path = "../../governor/eliot-observation", version = "0.1.0" }
eliot-change-monitor = { path = "../../governor/eliot-change-monitor", version = "0.1.0" }
eliot-observation-contracts = { path = "../../foundation/eliot-observation-contracts", version = "0.1.0" }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = { version = "1.0.150", features = ["float_roundtrip"] }
schemars = { version = "1.2.1", features = ["derive", "uuid1"] }
thiserror = "2.0.18"

[lints]
workspace = true