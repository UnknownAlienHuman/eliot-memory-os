schema_version = 4
module_id = "smart.dreamer.orchestration_plan"
crate = "eliot-dreamer-orchestration-plan"
status = "PROTOTYPE"
wave = "cognitive-micromodules-wave-02"
agent_order = 43
plane = "Smart"
runtime_layer = "R6"
delivery_depth = "D3a"
source_layer = "C1"
runtime_bundle = "eliot-dreamer"
state_class = "stateless"
hot_path = false
lifecycle_owner = "smart.dreamer.orchestration_plan"
loc_soft_ceiling = 10000
loc_policy = "review ceiling only; causal cohesion, proof, owner, build/test closure and replacement boundary decide topology"
product_objective = "One objective becomes a bounded CognitiveWorkPlanCandidate while TaskGraphCompiler, Governor and Agent Coordinator retain execution authority."
causal_property = "CognitiveWorkPlanCandidate synthesis"
old_failure_or_missing_capability = "Free-form orchestration advice can silently become a scheduler, shared chat control plane or invented route availability."
discriminator = "The handler emits a bounded plan candidate with explicit alternatives, budgets, independence and verifier paths while launching nothing."
primary_responsibility = "Translate one Human or Main-Agent objective into bounded work units, competence and route classes, context budgets, independence, synthesis and verifier paths."
owned_mutable_state = []
allowed_effects = []
inputs = ["DreamJobInput class=OrchestrationPlanning", "objective and acceptance", "capability, route and budget evidence"]
outputs = ["CognitiveWorkPlanCandidate", "work-unit packets", "route and independence alternatives"]
depends_on = ["eliot-dreamer-contracts", "eliot-dreamer-candidate-validation"]
providers = ["Governor-admitted Dreamer bundle", "capability and route projections"]
consumers = ["TaskGraphCompiler", "Governor", "Agent Coordinator", "Main Agent or Human"]
non_goals = ["TaskGraph admission", "agent launch", "budget creation", "task finish", "shared free-form control plane"]
invariants = ["one writer per deliverable is the default", "route availability and competence are never invented", "descendant and budget limits are explicit", "one strong agent, multiple scouts, one question or no model job remain visible alternatives"]
failure_behavior = "Return unsupported or over-budget CognitiveWorkPlanCandidate with exact missing capabilities, verifier or route evidence; never launch work."
proof_surface = ["one strong agent better", "several scouts", "missing verifier", "over-budget fanout", "invented route"]
proof_ceiling = "Module proof only; real provider/consumer Edge proof and Product Pulse remain separate"
product_pulse = "D3A_ADVISORY_DIAGNOSIS_PLANNING_PULSE_01"
replacement_boundary = "implementation behind the declared public contract; package ownership creates no runtime or semantic authority"
merge_rejoin_condition = "merge back when no independent consumer, proof, dependency or replacement seam remains"
split_trigger = "a second causal responsibility, mutable owner, independent proof surface or independent rollback boundary appears"

[[donor]]
path = "ELIOT_IMPLEMENTATION I9.3 Agent orchestration planning"
disposition = "authoritative candidate and execution-owner boundary"

[[donor]]
path = "ELIOT_ARCHITECTURE A8 and A13.7"
disposition = "work-unit and agent-independence boundary"

[agent_task]
objective = "Implement one candidate-only CognitiveWorkPlan handler; deterministic compilation, admission and launch remain external."
write_scope = ["crates/smart/eliot-dreamer-orchestration-plan/**"]
deliverables = ["CognitiveWorkPlanCandidate core", "route/budget/independence fixtures", "TaskGraphCompiler Edge fixtures"]
acceptance = ["bounded candidate only", "no admission or launch", "alternatives and verifier paths explicit"]
forbidden = ["canonical writes outside the named Governor transition path", "truth/support/authority/policy/finish/effect promotion", "provider or model SDKs in pure cognitive cores", "vendor or storage types in public contracts", "unbounded synchronous work", "silent truncation", "oracle or verifier weakening in the same work unit", "implicit scope widening", "active-generation self-modification"]
stop_condition = "Stop if planning requires admitting TaskGraph nodes, launching agents, creating budget or inventing route capacity."
challenge_path = "return ContractChallenge to the integration owner when owner, discriminator, proof ceiling or dependency boundary is wrong"
integration_owner = "cognitive-wave-integrator"
implementation_language = "Rust 2024"
required_verification = ["module/package proof at the declared proof ceiling", "real provider/consumer fixture for every changed edge", "the named Product Pulse or Edge proof before promotion", "exact donor reuse/rewrite/reject trace"]
writeback = "return implementation artifact, exact proof evidence, donor disposition, unresolved ContractChallenges and merge/remove recommendation"
workspace_admission = "forbidden until implementation, package proof, affected Edge proof, current-main ancestry and integration-owner review"
