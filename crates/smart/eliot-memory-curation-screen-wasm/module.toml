schema_version = 4
module_id = "smart.memory.curation-screen-wasm"
crate = "eliot-memory-curation-screen-wasm"
status = "PROTOTYPE"
wave = "cognitive-micromodules-wave-01"
agent_order = 20
plane = "Smart"
runtime_layer = "R6"
delivery_depth = "D3a"
source_layer = "C1"
runtime_bundle = "eliot-dreamer"
state_class = "stateless"
hot_path = false
lifecycle_owner = "smart.memory.curation-screen-wasm"
loc_soft_ceiling = 10000
loc_policy = "review ceiling only; causal cohesion, proof, owner, build/test closure and replacement boundary decide topology"
product_objective = "The accepted native deterministic memory-curation screen is invocable as a thin typed wasm32-wasip2 guest without a second screen owner."
causal_property = "thin typed WASM adaptation of the deterministic memory-curation screen leaf"
old_failure_or_missing_capability = "Packaging the screen required either a speculative private ABI, duplicate native screening logic, or a second eligibility decision in guest glue."
discriminator = "One valid typed guest invocation calls native A-20 #588 exactly once through the real screen and preserves its A-19c CurationScreenResult; rejected ABI envelopes call it zero times; every native field, disposition, digest and bound is preserved with no duplicate algorithm and no invented handler."
primary_responsibility = "Adapt the exact accepted memory-curation-screen world to one exhaustive typed call of the native screen over the real A-19c/A-20 contracts."
owned_mutable_state = []
allowed_effects = []
inputs = ["GuestRequest (typed envelope over the native CurationScreenRequest, SourceSnapshot and ProtectionEvidence)", "canonical request bytes at the `screen` boundary"]
outputs = ["GuestResponse (native CurationScreenResult or exhaustive typed error)", "component descriptor and import gate verdicts"]
depends_on = ["eliot-contracts", "eliot-memory-curation-contracts", "eliot-memory-curation-screen"]
providers = ["Governor-admitted source snapshot via the native owner", "bins/eliot-wasm-host memory-curation-screen.wit (read-only)", "rust-toolchain.toml pinned target (read-only)"]
consumers = ["E-HOST/E-RT local-experimental execution (#758/#760, read-only surfaces)"]
non_goals = ["typed `memory-curation-screen` world definition (owner #756)", "Host/runtime/engine implementation (#758/#760)", "toolchain pinning (#870)", "source paging/continuation beyond the native single call (owner #588)", "screening, protection, eligibility, A-31/handler semantics, canonical mutation, authority/effect, Finish"]
invariants = ["exactly one native A-20 call per valid typed invocation, zero per rejected invocation", "no wildcard, Other, Value or whole-payload JSON/bytes bridge", "native semantic digests distinct from component/artifact hashes", "no ambient WASI capability in the pure-world import gate", "no second eligibility decision in guest glue"]
failure_behavior = "Reject wrong world/shape/version/descriptor before native execution; degraded, blocked, partial and stale domain states go to A-20 and retain the actual native outcome; never promote to success."
proof_surface = ["16-case proof matrix (WORK_UNIT_CASE 642/1..16)", "native/component full-field and semantic-digest parity", "built-artifact import inspection", "standalone ModuleTestCapsule through the real E-HOST facade"]
proof_ceiling = "Module proof only; real provider/consumer Edge proof and Product Pulse remain separate"
product_pulse = "D3A_CURATION_PULSE_01"
replacement_boundary = "implementation behind the declared public contract; package ownership creates no runtime or semantic authority"
merge_rejoin_condition = "merge back when no independent consumer, proof, dependency or replacement seam remains"
split_trigger = "a second causal responsibility, mutable owner, independent proof surface or independent rollback boundary appears"

[[donor]]
path = "crates/smart/eliot-memory-curation-screen/src/screen.rs"
disposition = "reuse the native A-20 screen behind the guest envelope; no logic copied"

[[donor]]
path = "crates/smart/eliot-memory-curation-contracts/src/result.rs"
disposition = "reuse the accepted A-19c CurationScreenResult behind the guest envelope; no vocabulary copied"

[[donor]]
path = "bins/eliot-wasm-host/wit/typed/memory-curation-screen.wit"
disposition = "read-only ABI source compiled into the descriptor at build time; not copied by hand"

[[donor]]
path = "docs/architecture/I14-19-wasm-components.md"
disposition = "authoritative component boundary; typed memory-curation-screen world still owned by #756"

[[donor]]
path = "crates/smart/eliot-dreamer-orientation-wasm"
disposition = "structural precedent only (#632): envelope/descriptor/export/proof shape; no logic copied"

[agent_task]
objective = "Ship one thin exhaustive native-call adapter with zero ambient capability as a component-ready cdylib."
write_scope = ["crates/smart/eliot-memory-curation-screen-wasm/**"]
deliverables = ["typed guest envelope and exhaustive conversion", "memory-curation-screen-bound descriptor and pure-world import gate", "16-case parity/capsule proof suite"]
acceptance = ["one valid typed invocation calls native A-20 exactly once", "rejected calls never reach native", "all native dispositions, findings, eligibility, coverage, frontier, digests and bounds preserved"]
forbidden = ["canonical writes outside the named Governor transition path", "truth/support/authority/policy/finish/effect promotion", "provider or model SDKs in pure cognitive cores", "vendor or storage types in public contracts", "unbounded synchronous work", "silent truncation", "oracle or verifier weakening in the same work unit", "implicit scope widening", "active-generation self-modification"]
stop_condition = "Stop and return ContractChallenge if the guest requires a private ABI, duplicate screen logic, a second eligibility decision, or any Host/runtime/toolchain/screen-owner edit."
challenge_path = "return ContractChallenge to the integration owner when owner, discriminator, proof ceiling or dependency boundary is wrong"
integration_owner = "cognitive-wave-integrator"
implementation_language = "Rust 2024"
required_verification = ["standalone fmt/test/clippy", "all 16 Issue #642 cases discovered and passed", "wasm32-wasip2 release build", "workspace admission remains separate under this issue's controller"]
writeback = "return implementation artifact, exact proof evidence, donor disposition, unresolved ContractChallenges and merge/remove recommendation"
workspace_admission = "forbidden until implementation, package proof, affected Edge proof, current-main ancestry and integration-owner review"

[acceptance]
require_deny_unknown_fields = true
forbid_unsafe = true
required_exports = ["GuestRequest", "GuestResponse", "GuestError", "CallLedger", "ComponentDescriptor", "handle_request_typed", "screen", "descriptor", "check_wasm_imports"]
required_tests = ["proof.rs hosts all 16 WORK_UNIT_CASE 642/1..16 cases"]
forbidden_patterns = ['serde_json::Value', 'unimplemented!', 'todo!', 'std::process', 'std::fs', 'tokio', 'reqwest', 'surrealdb']
