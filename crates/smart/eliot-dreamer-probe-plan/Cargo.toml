[package]
name = "eliot-dreamer-probe-plan"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Bounded candidate-only discriminative probe planning for ELIOT Dreamer"

[lib]
path = "src/lib.rs"

[dependencies]
eliot-dreamer-contracts = { path = "../eliot-dreamer-contracts", version = "0.1.0" }
# Serialization infrastructure only: canonical digests and closed wire shapes.
# No rival-model, evaluation, epistemic, provider, or serde_json families.
serde.workspace = true
schemars = { version = "1.2.1", features = ["derive"] }

[package.metadata.eliot]
prototype = false
workspace_admission = "admitted via #836 (T8-A4) root workspace membership"
functional_cell = "smart.dreamer.probe_plan"
source_layer = "C1"
runtime_layer = "R6"
runtime_bundle = "eliot-dreamer"
agent_order = 17

[lints]
workspace = true
