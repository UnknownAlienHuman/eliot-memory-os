# `eliot-dreamer-probe-plan` implementation contract

Owning issue: [#610 — A-17b discriminative probe planner](https://github.com/UnknownAlienHuman/eliot-memory-os/issues/610).

## Mandatory documentation

Before changing this package, execute the repository `docs_read.py` route for `crates/smart/eliot-dreamer-probe-plan`, read the complete verified bundle, and read the direct files required by #610.

## Responsibility

Implement the sole deterministic pure converter from explicit material unknowns and live rival disagreements into a bounded ordered set of inert discriminative probe candidates.

## Required properties

- Reuse exact A-03 objective, result-matrix, affordance, rival and evidence contracts.
- Validate task, attempt, scope, fence, bundle, draft, rival, affordance, policy, digest and denominator identities before planning.
- Every ready probe has a finite possible-result schema where at least two valid outcomes produce different rival or gap updates.
- Preserve information, cost, latency, context, resources, privacy, consent, authority, effects, reversibility, feasibility and Human attention independently.
- Availability is not permission. Effectful or privacy-sensitive probes remain blocked until an external owner supplies exact admission.
- Execute no provider, tool, model, agent, process, network, Store, reservation, evidence acquisition, rival resolution, effect or Finish path.
- Merge only exact equivalents with complete lineage. Unknown or incomparable values cannot prove dominance.
- Emit one explicit disposition for every objective and candidate, including blocked, unprobeable, partial and omitted/frontier cases.

## Completion gate

- All 42 `WORK_UNIT_CASE: 610/1..42` tests execute and pass.
- `cargo fmt`, `cargo test --all-targets`, `cargo clippy --all-targets -- -D warnings`, `cargo doc --no-deps`, and `git diff --check` pass for the standalone package.
- The crate remains excluded until #836 performs serialized workspace admission.
