schema_version = 4
module_id = "smart.cue.activation-wasm"
crate = "eliot-cue-activation-wasm"
status = "PROTOTYPE"
wave = "cognitive-micromodules-wave-01"
agent_order = 14
plane = "Smart"
runtime_layer = "R5"
delivery_depth = "D3a"
source_layer = "C1"
runtime_bundle = "eliotd"
state_class = "stateless"
hot_path = false
lifecycle_owner = "smart.cue.activation-wasm"
loc_soft_ceiling = 10000
loc_policy = "review ceiling only; causal cohesion, proof, owner, build/test closure and replacement boundary decide topology"
product_objective = "The accepted native bounded cue activation evaluator is invocable as a thin typed wasm32-wasip2 guest without a second activation owner."
causal_property = "thin typed WASM adaptation of the cue activation leaf"
old_failure_or_missing_capability = "Packaging the evaluator required either a speculative private ABI, duplicate native traversal logic, or a reconstructed direct-hit snapshot in the guest."
discriminator = "One valid typed guest invocation calls native A-14a exactly once over the supplied immutable build candidate; rejected ABI/input calls it zero times; every native field, disposition, digest and bound is preserved with no duplicate algorithm and no invented snapshot."
primary_responsibility = "Adapt the exact accepted cue-activation `activate` export to one exhaustive typed call of the native A-14a evaluator over the caller-supplied immutable snapshot build candidate."
owned_mutable_state = []
allowed_effects = []
inputs = ["GuestRequest (typed envelope over the native candidate, request and profile)", "canonical request bytes at the `activate` boundary"]
outputs = ["GuestResponse (native evaluation or exhaustive typed error)", "component descriptor and import gate verdicts"]
depends_on = ["eliot-contracts", "eliot-cue-contracts", "eliot-cue-activation"]
providers = ["Governor-admitted bundle via the native owner", "bins/eliot-wasm-host cue-activation.wit (read-only)", "rust-toolchain.toml pinned target (read-only)"]
consumers = ["E-HOST/E-RT local-experimental execution (#758/#760, read-only surfaces)"]
non_goals = ["typed `cue-activation` world definition (owner #756)", "Host/runtime/engine implementation (#758/#760)", "toolchain pinning (#870)", "normalization, binding admission, snapshot build, index/graph query, inferred edges, model/provider, Context delivery, memory mutation, Finish"]
invariants = ["exactly one native A-14a call per valid typed invocation, zero per rejected invocation", "no wildcard, Other, Value or whole-payload JSON/bytes bridge", "native semantic digests distinct from component/artifact hashes", "no ambient WASI capability in the pure-world import gate", "direct activations carry no path; derived paths stay non-empty and contiguous from a direct seed"]
failure_behavior = "Reject wrong version/shape/world before native execution; retain the actual native or component error; never promote to success. Representable stale/partial/unknown domain inputs retain native behavior, never a guest-recomputed rejection."
proof_surface = ["13-case proof matrix (WORK_UNIT_CASE 640/1..13)", "native/component full-field and semantic-digest parity", "built-artifact import inspection", "standalone ModuleTestCapsule through the real E-HOST facade"]
proof_ceiling = "Module proof only; real provider/consumer Edge proof and Product Pulse remain separate"
product_pulse = "D3A_ACTIVATION_PULSE_01"
replacement_boundary = "implementation behind the declared public contract; package ownership creates no runtime or semantic authority"
merge_rejoin_condition = "merge back when no independent consumer, proof, dependency or replacement seam remains"
split_trigger = "a second causal responsibility, mutable owner, independent proof surface or independent rollback boundary appears"

[[donor]]
path = "crates/smart/eliot-cue-activation/src/lib.rs"
disposition = "reuse the native A-14a evaluator behind the guest envelope; no logic copied"

[[donor]]
path = "bins/eliot-wasm-host/wit/typed/cue-activation.wit"
disposition = "read-only ABI source compiled into the descriptor at build time; not copied by hand"

[[donor]]
path = "docs/architecture/I14-19-wasm-components.md"
disposition = "authoritative component boundary; typed cue-activation world still owned by #756"

[[donor]]
path = "crates/smart/eliot-dreamer-orientation-wasm"
disposition = "structural precedent only (#632): envelope/descriptor/export/proof shape; no logic copied"

[agent_task]
objective = "Ship one thin exhaustive native-call adapter with zero ambient capability as a component-ready cdylib."
write_scope = ["crates/smart/eliot-cue-activation-wasm/**"]
deliverables = ["typed guest envelope and exhaustive conversion", "cue-activation-bound descriptor and pure-world import gate", "13-case parity/capsule proof suite"]
acceptance = ["one valid typed invocation calls native A-14a exactly once", "rejected calls never reach native", "all native dispositions, paths, digests and bounds preserved"]
forbidden = ["canonical writes outside the named Governor transition path", "truth/support/authority/policy/finish/effect promotion", "provider or model SDKs in pure cognitive cores", "vendor or storage types in public contracts", "unbounded synchronous work", "silent truncation", "oracle or verifier weakening in the same work unit", "implicit scope widening", "active-generation self-modification"]
stop_condition = "Stop and return ContractChallenge if the guest requires a private ABI, duplicate traversal logic, a reconstructed snapshot, or any Host/runtime/toolchain/cue-contract edit."
challenge_path = "return ContractChallenge to the integration owner when owner, discriminator, proof ceiling or dependency boundary is wrong"
integration_owner = "cognitive-wave-integrator"
implementation_language = "Rust 2024"
required_verification = ["standalone fmt/test/clippy", "all 13 Issue #640 cases discovered and passed", "wasm32-wasip2 release build", "workspace admission remains separate under this issue's controller"]
writeback = "return implementation artifact, exact proof evidence, donor disposition, unresolved ContractChallenges and merge/remove recommendation"
workspace_admission = "forbidden until implementation, package proof, affected Edge proof, current-main ancestry and integration-owner review"

[acceptance]
require_deny_unknown_fields = true
forbid_unsafe = true
required_exports = ["GuestRequest", "GuestResponse", "GuestError", "CallLedger", "ComponentDescriptor", "handle_request_typed", "activate", "descriptor", "check_wasm_imports"]
required_tests = ["proof.rs hosts all 13 WORK_UNIT_CASE 640/1..13 cases"]
forbidden_patterns = ['serde_json::Value', 'unimplemented!', 'todo!', 'std::process', 'std::fs', 'tokio', 'reqwest', 'surrealdb']
