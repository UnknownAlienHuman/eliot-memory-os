[package]
name = "eliot-learning-delta"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Convert one consequential before/observed/interpretation input into an AttemptLearningDelta candidate or explicit evidence-backed no-change disposition."

[lib]
path = "src/lib.rs"

[package.metadata.eliot]
prototype = false
source_status = "IMPLEMENTED"
workspace_admission = "admitted via #967 (T8-AL1) root workspace membership"
functional_cell = "smart.learning.delta"
source_layer = "C1"
runtime_layer = "R6"
runtime_bundle = "eliot-dreamer"
agent_order = 34

[dependencies]
eliot-contracts = { path = "../../foundation/eliot-contracts" }
eliot-evidence = { path = "../../foundation/eliot-evidence" }
eliot-learning-contracts = { path = "../eliot-learning-contracts" }
eliot-instrument-api = { path = "../../instrument/eliot-instrument-api" }
schemars = { version = "1.2.1", features = ["derive"] }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = { version = "1.0.150", features = ["float_roundtrip"] }
thiserror = "2.0.18"

[lints]
workspace = true
