schema_version = 4
module_id = "smart.dreamer.architecture_brief"
crate = "eliot-dreamer-architecture-brief"
status = "PROTOTYPE"
wave = "cognitive-micromodules-wave-01"
agent_order = 8
plane = "Smart"
runtime_layer = "R6"
delivery_depth = "D3a"
source_layer = "C1"
runtime_bundle = "eliot-dreamer"
state_class = "stateless"
hot_path = false
lifecycle_owner = "smart.dreamer.architecture_brief"
loc_soft_ceiling = 10000
loc_policy = "review ceiling only; causal cohesion, proof, owner, build/test closure and replacement boundary decide topology"
product_objective = "ELIOT self-query exposes constitutional meaning from exact accepted Architecture sources without mixing it with implementation or observed runtime."
causal_property = "authority-preserving ArchitectureBrief projection"
old_failure_or_missing_capability = "Architecture projection was treated as an optional Orientation section and could be blurred with Implementation prose."
discriminator = "The brief is independently reconstructible from exact accepted Architecture handles and rejects Implementation or runtime evidence as constitutional authority."
primary_responsibility = "Produce an ArchitectureBrief candidate preserving applicable ARCH anchors, intent, rationale, Hard Boundaries, unresolved questions and exact source identity."
owned_mutable_state = []
allowed_effects = []
inputs = ["DreamJobInput class=ArchitectureSelfQuery", "ValidatedDreamDraft", "accepted Architecture handles and pair identity"]
outputs = ["ArchitectureBriefCandidate", "normative coverage manifest", "invalidation conditions"]
depends_on = ["eliot-dreamer-contracts", "eliot-dreamer-candidate-validation"]
providers = ["Governor-admitted accepted Architecture source handles"]
consumers = ["Dreamer self-query surface", "ELIOT self-scope Edge proof"]
non_goals = ["Implementation interpretation", "Architecture change", "runtime conformance claim", "source acquisition", "basic Orientation"]
invariants = ["only exact accepted Architecture bytes define constitutional meaning", "Implementation and runtime evidence cannot override Architecture", "exact revision and digest are retained", "missing applicable scope remains visible"]
failure_behavior = "Return partial or unsupported ArchitectureBrief with exact source or coverage gaps; never substitute summaries or implementation prose."
proof_surface = ["wrong digest", "mixed revision", "Implementation override attempt", "coverage omission"]
proof_ceiling = "Module proof only; real provider/consumer Edge proof and Product Pulse remain separate"
product_pulse = "D3A_SELF_QUERY_EDGE_01"
replacement_boundary = "implementation behind the declared public contract; package ownership creates no runtime or semantic authority"
merge_rejoin_condition = "merge back when no independent consumer, proof, dependency or replacement seam remains"
split_trigger = "a second causal responsibility, mutable owner, independent proof surface or independent rollback boundary appears"

[[donor]]
path = "ELIOT_IMPLEMENTATION I9.3 Architecture/self query"
disposition = "authoritative contract"

[[donor]]
path = "ELIOT_ARCHITECTURE A6.7"
disposition = "self-knowledge and constitutional-authority boundary"

[agent_task]
objective = "Implement the Architecture side of self-query as an independent authority-preserving handler."
write_scope = ["crates/smart/eliot-dreamer-architecture-brief/**"]
deliverables = ["typed ArchitectureBrief projection", "digest/authority/coverage fixtures", "self-query Edge fixtures"]
acceptance = ["candidate-only output", "exact accepted Architecture lineage", "no Implementation or runtime authority mixing"]
forbidden = ["canonical writes outside the named Governor transition path", "truth/support/authority/policy/finish/effect promotion", "provider or model SDKs in pure cognitive cores", "vendor or storage types in public contracts", "unbounded synchronous work", "silent truncation", "oracle or verifier weakening in the same work unit", "implicit scope widening", "active-generation self-modification"]
stop_condition = "Stop if the requested result requires Architecture mutation, source acquisition, current-runtime certification or Implementation semantics."
challenge_path = "return ContractChallenge to the integration owner when owner, discriminator, proof ceiling or dependency boundary is wrong"
integration_owner = "cognitive-wave-integrator"
implementation_language = "Rust 2024"
required_verification = ["module/package proof at the declared proof ceiling", "real provider/consumer fixture for every changed edge", "the named Product Pulse or Edge proof before promotion", "exact donor reuse/rewrite/reject trace"]
writeback = "return implementation artifact, exact proof evidence, donor disposition, unresolved ContractChallenges and merge/remove recommendation"
workspace_admission = "forbidden until implementation, package proof, affected Edge proof, current-main ancestry and integration-owner review"

# Machine-checked completion gate, read by `scripts/verify-work-unit.py`.
# `cargo fmt|test|clippy` all pass on an empty crate, so they cannot tell a
# finished cell from an unstarted one. This table can.
#
# `source_issue` names the assignment this gate enforces. `test_matrix_cases` is
# the number of numbered cases in that issue's `Required test matrix` section,
# and `min_tests` may never fall below it -- otherwise the gate certifies work
# that ignores most of the matrix the assignment demands.
[acceptance]
source_issue = 649
source_unit = "A-08"
test_matrix_cases = 30
min_source_lines = 200
min_public_items = 6
min_tests = 30
require_deny_unknown_fields = true
forbid_unsafe = true
required_exports = []
required_tests = []
forbidden_patterns = ['serde_json::Value', 'unimplemented!', 'todo!']
