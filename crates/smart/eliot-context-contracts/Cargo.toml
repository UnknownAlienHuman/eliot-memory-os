[package]
name = "eliot-context-contracts"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
description = "Provider-neutral Context atoms, provider registry, expected denominator, contribution, admission and view contracts."

[lib]
path = "src/lib.rs"

[package.metadata.eliot]
layer = "C0"
purpose = "stable Context contract hub and closed provider denominator"
source_maintenance_owner = "cognitive-integration-owner"
functional_cell_refs = [
  "smart.context.contracts.candidate",
  "smart.context.contracts.admission",
  "smart.context.contracts.view_quality",
  "smart.context.provider_registry_contracts",
]
independent_proof_profile = "prototype-not-workspace-admitted"
contract_refs = [
  "crates/smart/cognitive-current.toml",
  "crates/smart/eliot-context-contracts/provider-contribution.contract.toml",
]
implementation_support = "TARGET"
evidence_execution_status = "NOT_EXECUTED"
workspace_admission = "forbidden until Rust implementation, ModuleTestCapsule, affected Edge Proof and integration-owner review"

[dependencies]
eliot-contracts = { path = "../../foundation/eliot-contracts" }
eliot-evidence = { path = "../../foundation/eliot-evidence" }
