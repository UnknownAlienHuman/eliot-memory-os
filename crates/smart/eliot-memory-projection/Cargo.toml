[package]
name = "eliot-memory-projection"
version = "0.1.0"
edition = "2024"
rust-version = "1.94"
license = "MIT"
publish = false
description = "Read-only deterministic memory candidate selection and Context provider projection."

[lib]
path = "src/lib.rs"

[package.metadata.eliot]
prototype = true
source_status = "NOT_IMPLEMENTED"
workspace_admission = "pending_agent_implementation_module_edge_and_product_proof"
source_layer = "C1"
runtime_layer = "R5"
runtime_bundle = "eliotd"
functional_cell_refs = ["smart.memory.candidate_query", "smart.memory.context_projection"]
proof_ceiling = "STATIC_FUNCTION_DESIGN_ONLY"
issue = 152

[dependencies]
eliot-contracts = { path = "../../foundation/eliot-contracts" }
eliot-evidence = { path = "../../foundation/eliot-evidence" }
eliot-context-contracts = { path = "../eliot-context-contracts" }
eliot-cue-contracts = { path = "../eliot-cue-contracts" }
eliot-memory-projection-contracts = { path = "../eliot-memory-projection-contracts" }
