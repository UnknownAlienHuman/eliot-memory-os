[package]
name = "eliot-learning-state-view"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Compile one immutable CampaignLearningStateView from exact existing owner revisions and a declared recipe."

[lib]
path = "src/lib.rs"

[package.metadata.eliot]
prototype = false
source_status = "IMPLEMENTED"
workspace_admission = "admitted via #967 (T8-AL1) root workspace membership"
functional_cell = "smart.learning.state_view"
source_layer = "C1"
runtime_layer = "R5"
runtime_bundle = "eliotd"
agent_order = 33

[dependencies]
eliot-contracts = { path = "../../foundation/eliot-contracts", version = "0.1.0" }
eliot-evidence = { path = "../../foundation/eliot-evidence", version = "0.1.0" }
eliot-learning-contracts = { path = "../eliot-learning-contracts", version = "0.1.0" }
eliot-reactive-context-plan = { path = "../eliot-reactive-context-plan", version = "0.1.0" }

[lints]
workspace = true
