[package]
name = "eliot-learning-state-view"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Compile one immutable CampaignLearningStateView from exact existing owner revisions and a declared recipe."

[lib]
path = "src/lib.rs"

[package.metadata.eliot]
prototype = false
source_status = "IMPLEMENTED"
workspace_admission = "admitted via #967 (T8-AL1) root workspace membership"
functional_cell = "smart.learning.state_view"
source_layer = "C1"
runtime_layer = "R5"
runtime_bundle = "eliotd"
agent_order = 33

[dependencies]
eliot-contracts = { path = "../../foundation/eliot-contracts" }
eliot-evidence = { path = "../../foundation/eliot-evidence" }
eliot-learning-contracts = { path = "../eliot-learning-contracts" }

[lints]
workspace = true
