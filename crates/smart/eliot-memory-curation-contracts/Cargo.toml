[package]
name = "eliot-memory-curation-contracts"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
description = "Versioned read-only memory curation screening and forward proposal contracts."

[lib]
path = "src/lib.rs"

[package.metadata.eliot]
layer = "C0"
purpose = "dimensioned memory curation evidence, protection and reversible proposal contracts"
source_maintenance_owner = "cognitive-integration-owner"
functional_cell_refs = ["smart.memory.curation.contracts"]
independent_proof_profile = "prototype-not-workspace-admitted"
contract_refs = [
  "crates/smart/cognitive-current.toml",
  "crates/smart/eliot-memory-curation-contracts/curation.contract.toml",
  "crates/smart/eliot-memory-projection-contracts/projection.contract.toml",
]
implementation_support = "TARGET"
evidence_execution_status = "NOT_EXECUTED"
workspace_admission = "forbidden until Rust implementation, ModuleTestCapsule, affected real Edge Proof and D3A_CURATION_PULSE_01"

[dependencies]
eliot-contracts = { path = "../../foundation/eliot-contracts" }
eliot-evidence = { path = "../../foundation/eliot-evidence" }
