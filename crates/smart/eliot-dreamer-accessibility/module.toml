schema_version = 2
module_id = "smart.dreamer.accessibility"
crate = "eliot-dreamer-accessibility"
status = "PROTOTYPE"
wave = "cognitive-micromodules-wave-01"
agent_order = 25
plane = "Smart"
runtime_layer = "R6"
delivery_depth = "D3a"
source_layer = "C1"
runtime_bundle = "eliot-dreamer"
state_class = "stateless"
hot_path = false
lifecycle_owner = "smart.dreamer.accessibility"
loc_soft_ceiling = 10000
loc_policy = "review ceiling only; causal responsibility, proof and replacement boundary decide split/merge"
primary_responsibility = "Propose one reversible accessibility/activation/forgetting change based on cue utility, stale reuse, gravity, cost, regret, and protected-evidence constraints without changing epistemic support."
owned_mutable_state = []
allowed_effects = []
inputs = ["DreamJobInput class=Curation", "ValidatedDreamDraft item kind=accessibility", "memory ecology, influence/use and protected-role evidence"]
outputs = ["AccessibilityCandidate", "utility/harm/cost evidence vector", "restore/review/extinction conditions"]
depends_on = ["eliot-dreamer-contracts", "eliot-dreamer-candidate-validation"]
consumers = ["eliot-dreamer-curation", "memory lifecycle decision owner"]
non_goals = ["belief revision", "privacy erasure", "record deletion", "automatic suppression"]
invariants = ["accessibility and epistemic support remain separate", "low use does not mean false", "frequent retrieval does not mean true", "protected/minority/counterexample evidence is not erased"]
failure_behavior = "Return no-change or review candidate; never suppress evidence from popularity, one complaint, or writer-supplied utility."
proof_surface = ["low-use protected record", "high-use harmful narrative", "memory gravity", "restore/review condition"]
replacement_boundary = "crate implementation behind the declared public contract; no runtime authority follows from package ownership"
split_trigger = "a second causal responsibility, mutable owner, independent proof surface, or independent rollback boundary appears"

[[donor]]
path = "crates/smart/eliot-memory-curation/src/lib.rs"
disposition = "extract_protection_only"
scope = "protected roles and low-utility non-authority"

[[donor]]
path = "ELIOT_IMPLEMENTATION I12.21 and Architecture A14.4"
disposition = "authoritative_contract"
scope = "memory ecology and forgetting separation"

[agent_task]
objective = "Implement reversible memory-ecology proposals while keeping belief revision and erasure outside the cell."
write_scope = ["crates/smart/eliot-dreamer-accessibility/**"]
deliverables = ["accessibility candidate builder", "multi-axis utility/harm vector", "protected/minority and gravity tests"]
acceptance = ["no lifecycle effect", "support unchanged", "restore and review path always present"]
forbidden = ["canonical writes", "authority, policy, truth, finish or effect decisions", "provider/model SDKs", "storage/vendor types in public contracts", "unbounded synchronous work", "silent truncation", "weakening another owner's verifier or oracle"]
stop_condition = "Stop if the request is privacy erasure or epistemic revision; route it to the corresponding owner."
integration_owner = "cognitive-wave-01-integrator"
implementation_language = "Rust 2024"
required_verification = ["package-local shape/behavior proof", "declared consumer/provider contract fixtures", "affected Product Pulse when listed"]
workspace_admission = "forbidden until src/lib.rs, package proof in an isolated candidate workspace, affected edge proof, current-main rebase and integration-owner review"
