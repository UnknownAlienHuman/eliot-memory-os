schema_version = 4
module_id = "smart.dreamer.development_diagnosis"
crate = "eliot-dreamer-development-diagnosis"
status = "PROTOTYPE"
wave = "cognitive-micromodules-wave-02"
agent_order = 40
plane = "Smart"
runtime_layer = "R6"
delivery_depth = "D3a"
source_layer = "C1"
runtime_bundle = "eliot-dreamer"
state_class = "stateless"
hot_path = false
lifecycle_owner = "smart.dreamer.development_diagnosis"
loc_soft_ceiling = 10000
loc_policy = "review ceiling only; causal cohesion, proof, owner, build/test closure and replacement boundary decide topology"
product_objective = "A bounded product gap yields falsifiable rival diagnoses and the minimum discriminating experiment rather than another undifferentiated repair."
causal_property = "development diagnosis candidate"
old_failure_or_missing_capability = "Repeated repairs can stay locally green while product identity and mechanism evidence remain unresolved."
discriminator = "The handler must preserve rival root causes, expose common-mode assumptions and produce one minimum discriminating experiment or explicit insufficiency."
primary_responsibility = "Diagnose one bounded development or product gap using repair history, discriminators, product identity and a validated ConflictAnalysisCandidate when available."
owned_mutable_state = []
allowed_effects = []
inputs = ["DreamJobInput class=DevelopmentDiagnosis", "Product Objective and gap", "repair, discriminator and runtime evidence", "optional ConflictAnalysisCandidate"]
outputs = ["DevelopmentDiagnosisCandidate", "minimum discriminating experiment", "owner, repair-scope and guardrail recommendation"]
depends_on = ["eliot-dreamer-contracts", "eliot-dreamer-candidate-validation", "eliot-dreamer-conflict-analysis"]
providers = ["Governor-admitted Dreamer bundle", "conflict-analysis sub-handler"]
consumers = ["Dreamer diagnosis surface", "Governor or Task Controller named disposition"]
non_goals = ["feature freeze", "problem closure", "status promotion", "repair execution", "verifier mutation"]
invariants = ["activity artifacts are not product delta", "rivals and common-mode assumptions stay visible", "a repeated repair requires a new hypothesis or justified controlled repetition", "product and runtime identities are exact"]
failure_behavior = "Return inconclusive or unsupported DevelopmentDiagnosisCandidate with exact missing evidence; never prescribe an executable repair as established truth."
proof_surface = ["local-green product-fail", "repeated same repair", "wrong product identity", "proxy metric", "missing conflict analysis"]
proof_ceiling = "Module proof only; real provider/consumer Edge proof and Product Pulse remain separate"
product_pulse = "D3A_ADVISORY_DIAGNOSIS_PLANNING_PULSE_01"
replacement_boundary = "implementation behind the declared public contract; package ownership creates no runtime or semantic authority"
merge_rejoin_condition = "merge back when no independent consumer, proof, dependency or replacement seam remains"
split_trigger = "a second causal responsibility, mutable owner, independent proof surface or independent rollback boundary appears"

[[donor]]
path = "ELIOT_IMPLEMENTATION I9.3 Development diagnosis"
disposition = "authoritative job contract"

[[donor]]
path = "ELIOT_ARCHITECTURE A14.6"
disposition = "measurement versus product-outcome boundary"

[agent_task]
objective = "Implement one falsifiable candidate-only DevelopmentDiagnosis handler after the first D3a and D3b Product Pulses."
write_scope = ["crates/smart/eliot-dreamer-development-diagnosis/**"]
deliverables = ["DevelopmentDiagnosisCandidate core", "rival/proxy/product-identity fixtures", "conflict-analysis Edge fixtures"]
acceptance = ["falsifiable candidate only", "minimum experiment or explicit insufficiency", "no repair execution or status promotion"]
forbidden = ["canonical writes outside the named Governor transition path", "truth/support/authority/policy/finish/effect promotion", "provider or model SDKs in pure cognitive cores", "vendor or storage types in public contracts", "unbounded synchronous work", "silent truncation", "oracle or verifier weakening in the same work unit", "implicit scope widening", "active-generation self-modification"]
stop_condition = "Stop if diagnosis requires changing objective, acceptance, verifier, active production generation or executing the proposed repair."
challenge_path = "return ContractChallenge to the integration owner when owner, discriminator, proof ceiling or dependency boundary is wrong"
integration_owner = "cognitive-wave-integrator"
implementation_language = "Rust 2024"
required_verification = ["module/package proof at the declared proof ceiling", "real provider/consumer fixture for every changed edge", "the named Product Pulse or Edge proof before promotion", "exact donor reuse/rewrite/reject trace"]
writeback = "return implementation artifact, exact proof evidence, donor disposition, unresolved ContractChallenges and merge/remove recommendation"
workspace_admission = "forbidden until implementation, package proof, affected Edge proof, current-main ancestry and integration-owner review"

# Machine-checked completion gate, read by `scripts/verify-work-unit.py`.
# `cargo fmt|test|clippy` all pass on an empty crate, so they cannot tell a
# finished cell from an unstarted one. This table can.
#
# `source_issue` names the assignment this gate enforces. `test_matrix_cases` is
# the number of numbered cases in that issue's `Required test matrix` section,
# and `min_tests` may never fall below it -- otherwise the gate certifies work
# that ignores most of the matrix the assignment demands.
[acceptance]
source_issue = 675
source_unit = "A-40"
test_matrix_cases = 51
min_source_lines = 200
min_public_items = 6
min_tests = 51
require_deny_unknown_fields = true
forbid_unsafe = true
required_exports = []
required_tests = []
forbidden_patterns = ['serde_json::Value', 'unimplemented!', 'todo!']
