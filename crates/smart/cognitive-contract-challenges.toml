schema_version = 6
status = "CURRENT_POINTER"
authority = "crates/smart/cognitive-current.toml"
open_contract_challenges = [
  "CC-PROVIDER-ADAPTERS: Governor-owned GoalTask/CriticalAttention/AuthorityAffordance provider adapters remain outside Smart",
  "CC-EDGE-PROOF: implementation workers cannot claim real provider-consumer proof",
  "CC-PRODUCT-PULSE: design contracts are not executed evidence",
  "CC-WAVE0-PROOF-IDENTITY: the isolated source workspaces have no committed dependency lock or repository-hosted exact-head run; author-reported local checks remain NOT_EXECUTED evidence until immutable dependency identity and a manual proof receipt exist",
]

[[resolved_contract_challenge]]
id = "CC-MEMORY-PROJECTION-SCHEMA"
status = "RESOLVED_BY_FIELD_CONTRACT_FREEZE"
contract_ref = "crates/smart/eliot-memory-projection-contracts/projection.contract.toml"
resolution = "MemoryQueryIntent, MemorySelectionPolicy, MemorySelectionTrace and MemoryContextProjectionRequest now have one field-level owner; dependent Rust remains unimplemented/unproven."

[[resolved_contract_challenge]]
id = "CC-FAILURE-OBSERVATION-SCHEMA"
status = "RESOLVED_BY_FIELD_CONTRACT_FREEZE"
contract_ref = "crates/smart/eliot-learning-contracts/experience.contract.toml"
resolution = "FailureObservation now preserves trigger/action/outcome/invariant/scope/reopen/extinction evidence and carries no hard-block or lifecycle authority."

[[resolved_contract_challenge]]
id = "CC-PROVIDER-CONTRIBUTION-SCHEMA"
status = "RESOLVED_BY_FIELD_CONTRACT_FREEZE"
contract_ref = "crates/smart/eliot-context-contracts/provider-contribution.contract.toml"
resolution = "ProviderContribution, ProviderRoleRequirement, whole ProviderAtoms, coverage/omissions and the exact seven-class provider set are frozen; all providers reuse ContextProviderProjectionRequest."
