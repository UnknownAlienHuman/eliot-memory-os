schema_version = 4
module_id = "smart.dreamer.research-synthesis"
crate = "eliot-dreamer-research-synthesis"
status = "ADMITTED"
wave = "cognitive-micromodules-wave-01"
agent_order = 6
plane = "Smart"
runtime_layer = "R6"
delivery_depth = "D3a"
source_layer = "C1"
runtime_bundle = "eliot-dreamer"
state_class = "stateless"
hot_path = false
lifecycle_owner = "smart.dreamer.research-synthesis"
loc_soft_ceiling = 10000
loc_policy = "review ceiling only; causal cohesion, proof, owner, build/test closure and replacement boundary decide topology"
product_objective = "The governed ResearchPack plus the exact A-03 validated grounded structured draft project to one candidate ResearchBrief with exact citations, retained rivals, unknowns, discriminative probes and an inert Concilium recommendation."
causal_property = "pure ResearchPack-to-ResearchBrief synthesis"
old_failure_or_missing_capability = "Native eliot-dreamer-research-synthesis was missing entirely (#995 OPEN); the thin #634 guest adapter stopped at the native boundary with an honest ContractChallenge and never emitted a brief of its own."
discriminator = "One deterministic pure call maps a validated job, governed ResearchPack, grounded structured draft and immutable policy to a candidate ResearchBrief; all 24 WORK_UNIT_CASE 995/1..24 cases execute and pass."
primary_responsibility = "Own the single bounded deterministic ResearchPack-to-ResearchBrief projection at candidate-only ceiling with no model call, acquisition, truth promotion, I/O, clock, randomness, mutable state, authority, effect or Finish."
owned_mutable_state = []
allowed_effects = []
inputs = ["SynthesisRequest (validated job binding, governed immutable ResearchPack, exact grounded structured draft, inherited input-validation receipt, immutable policy, independent bounds, cancellation view)"]
outputs = ["SynthesisOutcome (candidate ResearchBrief or honest Blocked/Abstained disposition, seven I9.7 preservation verdicts, exact inherited receipt, omissions with denominators, raw/semantic digests) or typed SynthesisError"]
depends_on = []
providers = ["Governor-admitted ResearchPack via #700 acquisition (values only)", "A-03 bundle/grounding/pre-handler validation via #593/#602/#595 (exact public values/receipts only)", "A-03 #578 and A-06e #580 accepted public contracts (read-only projections)"]
consumers = ["eliot-dreamer-research-wasm guest adapter (#634 downstream parity)", "Dreamer runtime composition (#702 downstream composition)"]
non_goals = ["source acquisition, parsing, indexing, provider execution (owner #700 / Researcher)", "grounding, entailment, bundle/input validation algorithms (owners #593/#602/#595)", "A-05 post-handler validation pass (the native handler owns its subtype checks)", "model calls, probe execution, Concilium launch, canonical writes, truth promotion, authority/effect/Finish"]
invariants = ["one disposition per input claim/counterclaim/source/rival/probe/coverage role", "only exact authorized handles occur in the brief; outside-manifest references fail closed", "repetition never inflates grade, authority or coverage; weakest evidence governs each claim", "unknown independence stays unknown; no weighted trust, majority or confidence winner", "unsupported numeric/time/version/causal precision is never promoted or hidden", "absence requires complete suitable authoritative coverage, never empty search results", "recommended probes are supplied discriminative questions or policy-authorized canonical transforms only", "Concilium recommendation is inert: it creates no meeting, message, job, budget or agent", "raw and semantic result digests stay distinct from delivery/artifact identity"]
failure_behavior = "Reject wrong job/schema/identity/binding/duplicate-content/firewall/budget violations with typed errors before projection; return evidence-backed abstention/partial/blocked/exhausted/unsupported/conflicted/unknown dispositions instead of invented synthesis; diagnostics bounded and redacted."
proof_surface = ["24-case proof matrix (WORK_UNIT_CASE 995/1..24)", "guest-parity disposition mapping for #634", "wasm-target portability compile fixtures (no clock/IO/threads/process/env, forbid unsafe)"]
proof_ceiling = "Module proof only; #634 component parity and #702 runtime composition remain separate"
product_pulse = "D3A_RESEARCH_PULSE_01"
replacement_boundary = "pure synthesis algorithm behind synthesize plus the frozen public model"
merge_rejoin_condition = "merge back when no independent consumer, proof, dependency or replacement seam remains"
split_trigger = "a second causal responsibility, mutable owner, independent proof surface or independent rollback boundary appears"

[agent_task]
objective = "Ship the single pure ResearchPack-to-ResearchBrief owner with a 24-case proof matrix as a standalone package."
write_scope = ["crates/smart/eliot-dreamer-research-synthesis/**"]
deliverables = ["pure synthesis operation with closed validation/binding/firewall/precision/coverage/probe/concilium/bound logic", "24-case proof suite", "guest-parity and portability fixtures"]
acceptance = ["one deterministic pure call maps the four bound inputs to a candidate brief", "every input role carries exactly one output disposition", "all 24 Issue #995 cases discovered and passed"]
forbidden = ["canonical writes outside the named Governor transition path", "truth/support/authority/policy/finish/effect promotion", "provider, model, store or runtime SDKs in the pure core", "vendor or storage types in public contracts", "unbounded synchronous work", "silent truncation", "oracle or verifier weakening in the same work unit", "implicit scope widening", "active-generation self-modification"]
stop_condition = "Stop and return ContractChallenge if the pack requires source acquisition, private contracts, duplicate provider/grounding/registry/authority algorithms, or any Host/runtime/toolchain edit."
challenge_path = "return ContractChallenge to the integration owner when owner, discriminator, proof ceiling or dependency boundary is wrong"
integration_owner = "cognitive-wave-integrator"
implementation_language = "Rust 2024"
required_verification = ["standalone fmt/test/clippy/doc via --manifest-path", "all 24 Issue #995 cases discovered and passed", "workspace admission remains separate under this issue's controller"]
writeback = "return implementation artifact, exact proof evidence, donor disposition, unresolved ContractChallenges and merge/remove recommendation"
workspace_admission = "admitted via #995 (A-RESEARCH-SYNTHESIS) root workspace membership"

[acceptance]
require_deny_unknown_fields = true
forbid_unsafe = true
required_exports = ["SynthesisRequest", "SynthesisOutcome", "ResearchBrief", "SynthesisError", "SynthesisDisposition", "synthesize", "request_digest", "request_semantic_digest", "request_canonical_len", "pack_content_digest", "draft_content_digest", "brief_semantic_digest", "brief_canonical_len", "guest_parity_disposition"]
required_tests = ["proof.rs hosts all 24 WORK_UNIT_CASE 995/1..24 cases"]
forbidden_patterns = ['serde_json::Value', 'unimplemented!', 'todo!', 'panic!', 'std::process', 'std::fs', 'std::net', 'std::thread', 'std::time', 'std::env', 'tokio', 'reqwest', 'surrealdb', 'post_handler', 'ModelClient', 'ProviderClient']
