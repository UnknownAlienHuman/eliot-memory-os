[package]
name = "eliot-cue-contracts"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
description = "Provider-neutral cue observation, normalization, binding, snapshot, direct-match, spread and activation contracts."

[lib]
path = "src/lib.rs"

[package.metadata.eliot]
layer = "C0"
purpose = "stable exact-cue and bounded activation contract hub"
source_maintenance_owner = "cognitive-integration-owner"
functional_cell_refs = ["smart.cue.contracts"]
independent_proof_profile = "prototype-not-workspace-admitted"
contract_refs = [
  "crates/smart/cognitive-current.toml",
  "crates/smart/eliot-cue-contracts/cue.contract.toml",
]
implementation_support = "TARGET"
evidence_execution_status = "NOT_EXECUTED"
workspace_admission = "forbidden until all cue cells have Rust implementation, ModuleTestCapsule, real Edge Proof and D3B_REACTIVE_CONTEXT_PULSE_01"

[dependencies]
eliot-contracts = { path = "../../foundation/eliot-contracts" }
eliot-evidence = { path = "../../foundation/eliot-evidence" }
