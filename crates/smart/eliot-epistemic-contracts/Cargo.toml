[package]
name = "eliot-epistemic-contracts"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
description = "Versioned store-neutral contracts for ELIOT Current Epistemic Position and resolver inputs."

[lib]
path = "src/lib.rs"

[package.metadata.eliot]
layer = "C0"
purpose = "Current Epistemic Position, admitted resolver input, explicit assumptions, rivals, conflicts and investigation contracts"
source_maintenance_owner = "cognitive-integration-owner"
functional_cell_refs = ["smart.epistemic.contracts"]
independent_proof_profile = "prototype-not-workspace-admitted"
contract_refs = [
  "crates/smart/cognitive-current.toml",
  "crates/smart/eliot-epistemic-contracts/current-position.contract.toml",
  "crates/smart/eliot-epistemic-contracts/resolution.contract.toml",
]
implementation_support = "TARGET"
evidence_execution_status = "NOT_EXECUTED"
workspace_admission = "forbidden until Rust implementation, ModuleTestCapsule, affected real Edge Proof and D3A_ORIENTATION_PULSE_01"

[dependencies]
eliot-contracts = { path = "../../foundation/eliot-contracts" }
eliot-evidence = { path = "../../foundation/eliot-evidence" }
schemars.workspace = true
serde.workspace = true
thiserror.workspace = true

[lints]
workspace = true
