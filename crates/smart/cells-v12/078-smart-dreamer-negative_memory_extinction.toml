schema_version = 2
authority = "eliot-cognitive-function-design-rev12"
order = 78
cell = "smart.dreamer.negative_memory_extinction"
physical_crate = "eliot-dreamer-memory-revision"
primary_property = "Propose narrowing, reopening or extinction of negative-memory activation after safe re-exposure while preserving original failure and support history."
field_contract = "crates/smart/eliot-dreamer-memory-revision/negative-memory-extinction.contract.toml"
public_functions = ["pub fn build_negative_memory_extinction_candidate(request: &NegativeMemoryExtinctionRequest, fingerprint: &FailureFingerprint, evidence: &[SafeReexposureEvidence]) -> Result<NegativeMemoryExtinctionCandidate, NegativeMemoryExtinctionError>"]
public_types = ["NegativeMemoryExtinctionRequest", "SafeReexposureEvidence", "NegativeMemoryExtinctionCandidate", "ExtinctionDisposition", "TriggerBoundaryRevision", "TriggerMatchClass", "SafeReexposureOutcome", "NegativeMemoryEvidenceCoverage"]
write_scope = ["crates/smart/eliot-dreamer-memory-revision/src/negative_memory_extinction.rs", "crates/smart/eliot-dreamer-memory-revision/tests/negative_memory_extinction.rs"]
algorithm = ["validate exact fingerprint/failure refs, scope, environment generation, fence and denominator", "separate exact deterministic trigger from semantic near-match", "evaluate re-exposure outcome, verifier independence, effect evidence, false-activation history and confounders", "derive KeepBlock/RequireProbe/NarrowTrigger/Reopen/ExtinguishActivation/NoChange within evidence ceiling", "retain every original failure observation and emit validity, expiry, rollback and required authority"]
minimum_tests = ["semantic near-match cannot hard-block", "single safe exposure cannot broad-reopen/extinguish", "stale/partial/confounded evidence gives RequireProbe/NoChange", "environment change permits only scoped candidate", "history and support preserved", "candidate cannot apply itself"]
non_goals = ["canonical memory write", "support change", "hard-block authority", "lifecycle transition", "tool/model call", "policy mutation", "Cargo/lib/re-export integration"]
architecture_refs = ["ARCH-MEM-02", "ARCH-MEM-04", "ARCH-RES-05", "ARCH-EPI-04"]
implementation_refs = ["I12.19", "I12.21", "I13.11", "I2.20"]
product_pulse = "D4_MEMORY_ECOLOGY_PULSE_01"
stop_condition = "Return a ContractChallenge rather than treating similarity as an exact trigger, discarding failure history, broadening beyond evidence, or returning a transition receipt."
proof_ceiling = "PACKAGE_MODULE_PROOF_ONLY_AFTER_RUST_AND_TEST_EXECUTION"
