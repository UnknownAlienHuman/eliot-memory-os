schema_version = 2
authority = "eliot-cognitive-function-design-rev12"
order = 76
cell = "meta.memory.ecology_assessment"
physical_crate = "eliot-memory-quality"
primary_property = "Evaluate memory usefulness, harm, transfer, accessibility and influence evidence without collapsing dimensions or changing lifecycle/support."
field_contract = "crates/meta/eliot-memory-quality/quality.contract.toml"
public_functions = ["pub fn assess_memory_ecology(request: &MemoryEcologyAssessmentRequest, evidence: &[MemoryEcologyEvidence]) -> Result<MemoryEcologyAssessment, MemoryEcologyAssessmentError>"]
public_types = ["MemoryEcologyAssessmentRequest", "MemoryEcologyEvidence", "MemoryEcologyAssessment", "MemoryEcologyDimension", "MemoryEcologyDimensionAssessment", "InfluenceStage", "InfluenceStageEvidence", "MemoryOutcomeClass", "MemoryCohort", "MemoryQualityCoverage", "MemoryEcologyAction", "MemoryEcologyActionCandidate"]
write_scope = ["crates/meta/eliot-memory-quality/src/ecology.rs", "crates/meta/eliot-memory-quality/tests/ecology.rs"]
algorithm = ["validate exact target/scope/fence/horizon and denominator", "separate retrieval, acknowledgement, expansion, observed use, verification, decision delta and outcome", "assess stale reuse, contradictions, false activation/block, negative transfer, costs, minority value and missing-context regret independently", "preserve current/obsolete/context-shift/near-match/minority/negative-memory cohorts", "return bounded reversible action candidates with evidence, expiry, rollback and authority reference"]
minimum_tests = ["unused supported fact not demoted", "retrieval/delivery without use gives no utility credit", "Complete without denominator rejected", "false activation and false block separate", "minority evidence protected", "Archive/Suppress cannot change support", "no transition receipt"]
non_goals = ["canonical read/write", "lifecycle/support/influence mutation", "Context compilation", "model call", "aggregate utility/confidence score", "Cargo/lib/re-export integration"]
architecture_refs = ["ARCH-MEM-04", "ARCH-LEARN-01", "ARCH-META-01", "ARCH-EPI-04"]
implementation_refs = ["I12.21", "I12.34", "I16.23", "I2.20"]
product_pulse = "D4_MEMORY_ECOLOGY_PULSE_01"
stop_condition = "Return a ContractChallenge rather than redefining the frozen fields, inventing an aggregate score, inferring utility from retrieval/use absence, or applying a lifecycle transition."
proof_ceiling = "PACKAGE_MODULE_PROOF_ONLY_AFTER_RUST_AND_TEST_EXECUTION"
