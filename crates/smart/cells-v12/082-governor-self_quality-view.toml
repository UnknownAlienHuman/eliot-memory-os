schema_version = 2
authority = "eliot-cognitive-function-design-rev12"
order = 82
cell = "governor.self_quality.view"
physical_crate = "eliot-cognitive-quality"
primary_property = "Compile a problem-oriented EliotSelfQualityView from exact owner projections without creating a second state owner or global intelligence score."
field_contract = "crates/meta/eliot-cognitive-quality/quality.contract.toml"
public_functions = ["pub fn compile_eliot_self_quality_view(request: &EliotSelfQualityViewRequest, projections: &[SelfQualityOwnerProjection]) -> Result<EliotSelfQualityView, EliotSelfQualityViewError>"]
public_types = ["EliotSelfQualityViewRequest", "SelfQualityOwnerKind", "SelfQualityOwnerProjection", "SelfQualityOwnerCoverage", "SelfQualityContour", "SelfQualityContourAssessment", "SelfQualityCounterMetric", "NamedCognitiveDimension", "EliotSelfQualityView", "CognitiveQualityCoverage"]
write_scope = ["crates/meta/eliot-cognitive-quality/src/self_quality_view.rs", "crates/meta/eliot-cognitive-quality/tests/self_quality_view.rs"]
algorithm = ["validate product/scope/fence and exact expected-owner denominator", "accept only immutable revisioned projections from observation, experience, feedback, product, runtime, Problems, Context, memory, Skill, tool, Dreamer, maintenance, effects and Product Pulse owners", "assemble dimensioned contours without cross-owner substitution", "retain open Problems, ImprovementCandidates, Human actions and countermetrics", "emit deterministic digest and explicit missing-owner coverage"]
minimum_tests = ["all expected owners represented or explicitly missing", "one owner cannot fill another", "activity volume without product/recovery delta exposed", "no global score field", "order-independent digest", "no mutable journal/experience state API"]
non_goals = ["canonical journal/experience state", "policy or lifecycle mutation", "intervention execution", "model call", "global intelligence score", "Cargo/lib/re-export integration"]
architecture_refs = ["ARCH-SELF-01", "ARCH-META-01", "ARCH-CORE-00", "ARCH-EPI-04"]
implementation_refs = ["I16.23", "I4.8", "I2.20"]
product_pulse = "D4_SELF_QUALITY_PULSE_01"
stop_condition = "Return a ContractChallenge rather than inventing a missing owner projection, collapsing contours to one score, or claiming mutable Governor lifecycle ownership inside the pure compiler."
proof_ceiling = "PACKAGE_MODULE_PROOF_ONLY_AFTER_RUST_AND_TEST_EXECUTION"
