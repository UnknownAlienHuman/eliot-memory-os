schema_version = 1
authority = "eliot-cognitive-function-design-rev12"
order = 70
cell = "smart.memory.context_projection"
physical_crate = "eliot-memory-projection"
primary_property = "Translate a bounded MemoryCandidateSet into whole owner-attributed ContextAtoms without granting admission or leaking inaccessible payloads."
public_functions = ["pub fn project_memory_context(request: &MemoryContextProjectionRequest, candidates: &MemoryCandidateSet) -> Result<ProviderContribution, MemoryContextProjectionError>"]
write_scope = ["crates/smart/eliot-memory-projection/src/context_projection.rs", "crates/smart/eliot-memory-projection/tests/context_projection.rs"]
minimum_tests = ["inaccessible payload not leaked", "status preserved", "unknown decision delta preserved", "protected handle retained", "no admission disposition"]
non_goals = ["canonical write or lifecycle transition", "policy, authority, scheduling, effect or finish ownership", "public type redefinition", "Cargo/lib/re-export integration", "proof above package-local design/implementation evidence"]
architecture_refs = ["ARCH-CTX-01", "ARCH-CTX-04", "ARCH-MEM-03"]
implementation_refs = ["I7.11 Decision Safety Floor", "I12 Context Compiler inputs"]
product_pulse = "D3B_CANONICAL_PROJECTION_PULSE_01"
stop_condition = "Return a ContractChallenge if the declared API requires another owner, hidden I/O/model work, an oracle change, or a wider mutable scope."
proof_ceiling = "STATIC_FUNCTION_DESIGN_ONLY until Rust implementation and package proof exist"
