schema_version = 2
authority = "eliot-cognitive-function-design-rev12"
order = 70
cell = "smart.memory.context_projection"
physical_crate = "eliot-memory-projection"
primary_property = "Translate MemoryCandidateSet into whole ProviderAtoms and ProviderContribution without leaking inaccessible payloads or granting admission."
field_contracts = ["crates/smart/eliot-memory-projection-contracts/projection.contract.toml", "crates/smart/eliot-context-contracts/provider-contribution.contract.toml"]
public_functions = ["pub fn project_memory_context(request: &MemoryContextProjectionRequest, candidates: &MemoryCandidateSet) -> Result<ProviderContribution, MemoryContextProjectionError>"]
write_scope = ["crates/smart/eliot-memory-projection/src/context_projection.rs", "crates/smart/eliot-memory-projection/tests/context_projection.rs"]
minimum_tests = ["inaccessible payload not leaked", "handle-only remains expandable", "status and influence preserved", "unknown decision delta preserved", "required protected item cannot disappear", "Complete contribution requires denominator", "no admission field or decision"]
non_goals = ["canonical read", "Context admission/ranking across providers", "delivery receipt", "authority", "Cargo/lib/re-export integration"]
architecture_refs = ["ARCH-CTX-01", "ARCH-CTX-04", "ARCH-MEM-03"]
implementation_refs = ["I7.11", "I12.13"]
product_pulse = "D3B_CANONICAL_PROJECTION_PULSE_01"
stop_condition = "Return a ContractChallenge rather than redefining ProviderContribution, bypassing disclosure/accessibility or inventing admission state."
proof_ceiling = "PACKAGE_MODULE_PROOF_ONLY_AFTER_RUST_AND_TEST_EXECUTION"
