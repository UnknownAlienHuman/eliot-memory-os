schema_version = 2
authority = "eliot-cognitive-function-design-rev12"
order = 72
cell = "smart.experience.projection"
physical_crate = "eliot-experience-projection"
primary_property = "Build one immutable SystemExperienceProjection from exact owner-produced read projections and frozen request limits."
field_contracts = ["crates/smart/eliot-learning-contracts/experience.contract.toml"]
public_functions = ["pub fn project_system_experience(request: &SystemExperienceProjectionRequest, snapshot: &CanonicalExperienceReadSnapshot) -> Result<SystemExperienceProjection, ExperienceProjectionError>"]
write_scope = ["crates/smart/eliot-experience-projection/src/projection.rs", "crates/smart/eliot-experience-projection/tests/projection.rs"]
minimum_tests = ["unattributed success observed only", "failed attempt and FailureObservation retained", "unknown external effect visible", "request limit bypass rejected", "partial owner denominator visible", "projection deterministic", "no mutable store API"]
non_goals = ["owner queries", "mutable experience state", "learning delta", "Skill/policy promotion", "Cargo/lib/re-export integration"]
architecture_refs = ["ARCH-CORE-00", "ARCH-LEARN-01", "ARCH-LEARN-02"]
implementation_refs = ["I12.24", "I9 development diagnosis"]
product_pulse = "D4_EXPERIENCE_PROJECTION_PULSE_01"
stop_condition = "Return a ContractChallenge if an owner projection or request-bound field is missing; never fill it from transcript/model prose."
proof_ceiling = "PACKAGE_MODULE_PROOF_ONLY_AFTER_RUST_AND_TEST_EXECUTION"
