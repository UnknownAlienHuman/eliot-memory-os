schema_version = 1
authority = "eliot-cognitive-function-design-rev12"
order = 72
cell = "smart.experience.projection"
physical_crate = "eliot-experience-projection"
primary_property = "Build SystemExperienceProjection from exact owner-produced canonical read projections without becoming a second mutable experience owner."
public_functions = ["pub fn project_system_experience(request: &SystemExperienceProjectionRequest, snapshot: &CanonicalExperienceReadSnapshot) -> Result<SystemExperienceProjection, ExperienceProjectionError>"]
write_scope = ["crates/smart/eliot-experience-projection/src/projection.rs", "crates/smart/eliot-experience-projection/tests/projection.rs"]
minimum_tests = ["unattributed success observed only", "failed attempt retained", "unknown external outcome visible", "projection deterministic", "no mutable store API"]
non_goals = ["canonical write or lifecycle transition", "policy, authority, scheduling, effect or finish ownership", "public type redefinition", "Cargo/lib/re-export integration", "proof above package-local design/implementation evidence"]
architecture_refs = ["ARCH-CORE-00", "ARCH-LEARN-01", "ARCH-LEARN-02"]
implementation_refs = ["I12.24 learning state", "I9 development diagnosis"]
product_pulse = "D4_EXPERIENCE_PROJECTION_PULSE_01"
stop_condition = "Return a ContractChallenge if the declared API requires another owner, hidden I/O/model work, an oracle change, or a wider mutable scope."
proof_ceiling = "STATIC_FUNCTION_DESIGN_ONLY until Rust implementation and package proof exist"
