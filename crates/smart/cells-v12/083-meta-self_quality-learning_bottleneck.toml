schema_version = 2
authority = "eliot-cognitive-function-design-rev12"
order = 83
cell = "meta.self_quality.learning_bottleneck"
physical_crate = "eliot-cognitive-quality"
primary_property = "Diagnose task-local learning bottlenecks while preserving update quality, activation, adherence, decision delta, outcome, retention and transfer as separate dimensions."
field_contract = "crates/meta/eliot-cognitive-quality/quality.contract.toml"
public_functions = ["pub fn diagnose_learning_bottleneck(view: &EliotSelfQualityView, evidence: &LearningBottleneckEvidence) -> Result<LearningBottleneckAssessment, LearningBottleneckError>"]
public_types = ["LearningBottleneckEvidence", "LearningBottleneckDimension", "LearningBottleneckDimensionEvidence", "LearningBottleneckAssessment", "LearningBottleneckKind", "LearningBottleneckCandidate", "CognitiveDimensionResult", "CognitiveQualityCoverage"]
write_scope = ["crates/meta/eliot-cognitive-quality/src/learning_bottleneck.rs", "crates/meta/eliot-cognitive-quality/tests/learning_bottleneck.rs"]
algorithm = ["validate common campaign/route/scope/fence, owner coverage and immediate/retention/transfer horizons", "inspect update quality, activation, adherence, decision delta, immediate outcome, retention and transfer independently", "map supported patterns to retrieval/cue/Context, route/instruction, update weakness, bad lesson, evaluator mismatch, confounder, overfitting or insufficient-evidence candidates", "preserve rival bottlenecks when evidence is not discriminative", "attach next discriminator and invalidation conditions"]
minimum_tests = ["high update quality plus low activation maps delivery bottleneck", "high activation plus low adherence maps route/instruction", "decision delta with worse outcome retained", "immediate gain plus retention/transfer loss maps overfitting", "missing dimension remains Unknown", "ambiguous rivals preserved", "no route/overlay mutation"]
non_goals = ["route or overlay mutation", "learning promotion", "evaluator replacement", "model call", "aggregate learning score", "Cargo/lib/re-export integration"]
architecture_refs = ["ARCH-LEARN-02", "ARCH-META-01", "ARCH-UND-02", "ARCH-EPI-04"]
implementation_refs = ["I16.23", "I12.24", "I2.20"]
product_pulse = "D4_SELF_QUALITY_PULSE_01"
stop_condition = "Return a ContractChallenge rather than forcing one bottleneck, filling missing dimensions with zero, or changing route/overlay/evaluator."
proof_ceiling = "PACKAGE_MODULE_PROOF_ONLY_AFTER_RUST_AND_TEST_EXECUTION"
