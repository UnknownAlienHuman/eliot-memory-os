schema_version = 2
authority = "eliot-cognitive-function-design-rev12"
order = 83
cell = "meta.self_quality.learning_bottleneck"
physical_crate = "eliot-cognitive-quality"
field_contract = "crates/meta/eliot-cognitive-quality/quality.contract.toml"
primary_property = "Diagnose the bottleneck in task-local learning while preserving update quality, activation, adherence, decision delta, outcome, retention and transfer as independent dimensions."
public_functions = ["pub fn diagnose_learning_bottleneck(view: &EliotSelfQualityView, evidence: &LearningBottleneckEvidence) -> Result<LearningBottleneckAssessment, LearningBottleneckError>"]
write_scope = ["crates/meta/eliot-cognitive-quality/src/learning_bottleneck.rs", "crates/meta/eliot-cognitive-quality/tests/learning_bottleneck.rs"]
minimum_tests = ["high update quality plus low activation maps to delivery/cue/context candidate", "high activation plus low adherence maps to route/instruction candidate", "decision delta with worse outcome remains harmful", "immediate gain with retention/transfer loss maps to overfit", "ambiguous evidence preserves multiple rivals and discriminator", "missing dimension remains Unknown", "no route/overlay mutation"]
non_goals = ["route/overlay/config mutation", "forced single diagnosis", "aggregate learning score", "policy/authority/effect/finish ownership", "Cargo/lib/re-export integration"]
architecture_refs = ["ARCH-LEARN-02", "ARCH-META-01", "ARCH-UND-02"]
implementation_refs = ["I16.23", "I12.24"]
product_pulse = "D4_SELF_QUALITY_PULSE_01"
stop_condition = "Return a ContractChallenge rather than redefining indexed fields, collapsing dimensions, or forcing a bottleneck without discriminating evidence."
proof_ceiling = "PACKAGE_MODULE_PROOF_ONLY_AFTER_RUST_AND_TEST_EXECUTION"
