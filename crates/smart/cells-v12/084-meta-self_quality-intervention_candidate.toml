schema_version = 1
authority = "eliot-cognitive-function-design-rev12"
order = 84
cell = "meta.self_quality.intervention_candidate"
physical_crate = "eliot-cognitive-quality"
primary_property = "Build an advisory SelfQualityInterventionCandidate with hypothesis, rivals, discriminator, proof ceiling, rollback and delayed outcome window; never execute or self-promote it."
public_functions = ["pub fn build_self_quality_intervention_candidate(view: &EliotSelfQualityView, bottleneck: &LearningBottleneckAssessment, policy: &SelfQualityInterventionPolicy) -> Result<SelfQualityInterventionCandidate, SelfQualityInterventionError>"]
write_scope = ["crates/meta/eliot-cognitive-quality/src/intervention.rs", "crates/meta/eliot-cognitive-quality/tests/intervention.rs"]
minimum_tests = ["configuration mutation absent", "same hypothesis recurrence forces mechanism review", "abstention valid", "rollback required", "candidate not receipt"]
non_goals = ["canonical write or lifecycle transition", "policy, authority, scheduling, effect or finish ownership", "public type redefinition", "Cargo/lib/re-export integration", "proof above package-local design/implementation evidence"]
architecture_refs = ["ARCH-META-01", "ARCH-INTENT-01", "ARCH-RES-05"]
implementation_refs = ["I16.23 SelfQualityInterventionReceipt boundary", "I14 maintenance trigger owner"]
product_pulse = "D4_SELF_QUALITY_PULSE_01"
stop_condition = "Return a ContractChallenge if the declared API requires another owner, hidden I/O/model work, an oracle change, or a wider mutable scope."
proof_ceiling = "STATIC_FUNCTION_DESIGN_ONLY until Rust implementation and package proof exist"
