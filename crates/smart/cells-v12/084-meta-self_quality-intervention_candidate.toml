schema_version = 2
authority = "eliot-cognitive-function-design-rev12"
order = 84
cell = "meta.self_quality.intervention_candidate"
physical_crate = "eliot-cognitive-quality"
primary_property = "Build an advisory self-quality intervention candidate with causal hypothesis, rivals, discriminator, countermetrics, rollback and delayed outcome windows; never execute or self-promote it."
field_contract = "crates/meta/eliot-cognitive-quality/quality.contract.toml"
public_functions = ["pub fn build_self_quality_intervention_candidate(view: &EliotSelfQualityView, bottleneck: &LearningBottleneckAssessment, policy: &SelfQualityInterventionPolicy) -> Result<SelfQualityInterventionCandidate, SelfQualityInterventionError>"]
public_types = ["SelfQualityInterventionPolicy", "SelfQualityInterventionCandidate", "SelfQualityInterventionKind", "DelayedOutcomeWindow", "LearningBottleneckAssessment", "EliotSelfQualityView"]
write_scope = ["crates/meta/eliot-cognitive-quality/src/intervention.rs", "crates/meta/eliot-cognitive-quality/tests/intervention.rs"]
algorithm = ["validate view/bottleneck/policy identities, owners, evidence and fence", "form one causal hypothesis, explicit rivals and a discriminating observation/action", "select Observe/Repair/Configure/Reroute/Experiment/Abstain/Escalate/MechanismReview within the external effect ceiling", "attach candidate change, required authority, verifier, countermetrics, rollback, validity scope and immediate/delayed outcome windows", "emit advisory candidate plus external writeback requirements"]
minimum_tests = ["candidate exposes no config/route/Skill/memory/evaluator mutation", "same-hypothesis recurrence forces MechanismReview", "Abstain/Escalate valid when no safe discriminator", "material action requires rollback", "countermetrics and both outcome windows required", "candidate is not receipt or self-promotion"]
non_goals = ["intervention execution", "configuration/route/Skill/memory/evaluator/policy mutation", "authority grant", "task finish", "scheduler/effect ownership", "Cargo/lib/re-export integration"]
architecture_refs = ["ARCH-META-01", "ARCH-INTENT-01", "ARCH-RES-05", "ARCH-EPI-04"]
implementation_refs = ["I16.23", "I14", "I2.20"]
product_pulse = "D4_SELF_QUALITY_PULSE_01"
stop_condition = "Return a ContractChallenge or Abstain/Escalate rather than creating an executable receipt, omitting rollback/discriminator, or repeating an unchanged failed hypothesis."
proof_ceiling = "PACKAGE_MODULE_PROOF_ONLY_AFTER_RUST_AND_TEST_EXECUTION"
