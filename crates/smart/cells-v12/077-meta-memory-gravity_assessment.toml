schema_version = 2
authority = "eliot-cognitive-function-design-rev12"
order = 77
cell = "meta.memory.gravity_assessment"
physical_crate = "eliot-memory-quality"
field_contract = "crates/meta/eliot-memory-quality/quality.contract.toml"
primary_property = "Detect Context dominance disproportionate to evidence and outcome utility and return reversible exposure candidates only."
public_functions = ["pub fn assess_memory_gravity(request: &MemoryGravityAssessmentRequest, exposures: &[MemoryContextExposure]) -> Result<MemoryGravityAssessment, MemoryGravityAssessmentError>"]
write_scope = ["crates/meta/eliot-memory-quality/src/gravity.rs", "crates/meta/eliot-memory-quality/tests/gravity.rs"]
minimum_tests = ["repeated popular record can be gravity candidate without support change", "high-utility invariant retained", "minority/counterevidence displacement detected", "negative memory not suppressed for low decision delta alone", "inconclusive denominator yields ObserveMore", "no deletion or applied transition candidate", "permutation deterministic"]
non_goals = ["Context compilation/admission", "memory deletion/support change", "aggregate gravity score", "policy/authority/effect/finish ownership", "Cargo/lib/re-export integration"]
architecture_refs = ["ARCH-CTX-01", "ARCH-MEM-04", "ARCH-META-01"]
implementation_refs = ["I12.21", "I7.11"]
product_pulse = "D4_MEMORY_ECOLOGY_PULSE_01"
stop_condition = "Return a ContractChallenge rather than redefining indexed fields, equating prominence with support, or auto-suppressing without denominators."
proof_ceiling = "PACKAGE_MODULE_PROOF_ONLY_AFTER_RUST_AND_TEST_EXECUTION"
