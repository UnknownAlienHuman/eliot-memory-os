schema_version = 2
authority = "eliot-cognitive-function-design-rev12"
order = 77
cell = "meta.memory.gravity_assessment"
physical_crate = "eliot-memory-quality"
primary_property = "Detect Context dominance disproportionate to evidence and outcome utility, then propose reversible exposure changes only."
field_contract = "crates/meta/eliot-memory-quality/quality.contract.toml"
public_functions = ["pub fn assess_memory_gravity(request: &MemoryGravityAssessmentRequest, exposures: &[MemoryContextExposure]) -> Result<MemoryGravityAssessment, MemoryGravityAssessmentError>"]
public_types = ["MemoryGravityAssessmentRequest", "MemoryContextExposure", "MemoryGravityAssessment", "MemoryGravityDimension", "MemoryGravityDimensionAssessment", "MemoryGravityClassification", "GravityIntervention", "GravityInterventionCandidate", "MemoryQualityCoverage"]
write_scope = ["crates/meta/eliot-memory-quality/src/gravity.rs", "crates/meta/eliot-memory-quality/tests/gravity.rs"]
algorithm = ["validate target, Context recipe, route, task family, protected roles, fence and exposure denominator", "measure placement, repetition, rendered share, protected-role displacement, activation precision, decision delta, outcome utility, source-support alignment, counterevidence displacement and cost independently", "distinguish dominance, benign prominence, insufficient evidence, false-gravity diagnosis and no material gravity", "return only Narrow/Reorder/HandleOnly/Suppress/Split/ObserveMore/NoChange candidates with expiry and rollback"]
minimum_tests = ["popular repeated record can be gravity candidate", "prominence cannot raise support", "high-utility invariant retained", "negative memory not suppressed solely for low decision delta", "minority displacement detected", "inconclusive denominator permits only ObserveMore/NoChange", "no Delete or transition receipt"]
non_goals = ["Context compilation/admission", "canonical read/write", "support/lifecycle mutation", "model call", "aggregate gravity/utility score", "Cargo/lib/re-export integration"]
architecture_refs = ["ARCH-CTX-01", "ARCH-MEM-04", "ARCH-META-01"]
implementation_refs = ["I12.21", "I7.11"]
product_pulse = "D4_MEMORY_ECOLOGY_PULSE_01"
stop_condition = "Return a ContractChallenge rather than deleting memory, changing support, compiling Context, or forcing a diagnosis without a stable exposure/outcome denominator."
proof_ceiling = "PACKAGE_MODULE_PROOF_ONLY_AFTER_RUST_AND_TEST_EXECUTION"
