schema_version = 1
authority = "eliot-cognitive-function-design-rev12"
order = 68
cell = "smart.memory.projection.contracts"
physical_crate = "eliot-memory-projection-contracts"
primary_property = "Own provider-neutral bounded canonical-memory read, candidate, selection and context-projection contracts without defining storage or lifecycle."
public_functions = ["pub fn validate_memory_read_snapshot(snapshot: &CanonicalMemoryReadSnapshot) -> Result<(), MemoryProjectionContractError>", "pub fn validate_memory_candidate_set(set: &MemoryCandidateSet) -> Result<(), MemoryProjectionContractError>"]
write_scope = ["crates/smart/eliot-memory-projection-contracts/src/contracts.rs", "crates/smart/eliot-memory-projection-contracts/tests/contracts.rs"]
minimum_tests = ["empty-complete differs from unavailable", "support/accessibility/influence remain separate", "duplicate handle rejected", "record order does not change digest"]
non_goals = ["canonical write or lifecycle transition", "policy, authority, scheduling, effect or finish ownership", "public type redefinition", "Cargo/lib/re-export integration", "proof above package-local design/implementation evidence"]
architecture_refs = ["ARCH-SEC-02", "ARCH-MEM-04", "ARCH-CTX-04"]
implementation_refs = ["I5.20 named reads", "I12 memory projection", "I7.19 reactive context"]
product_pulse = "D3B_CANONICAL_PROJECTION_PULSE_01"
stop_condition = "Return a ContractChallenge if the declared API requires another owner, hidden I/O/model work, an oracle change, or a wider mutable scope."
proof_ceiling = "STATIC_FUNCTION_DESIGN_ONLY until Rust implementation and package proof exist"
