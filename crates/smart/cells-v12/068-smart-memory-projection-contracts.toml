schema_version = 2
authority = "eliot-cognitive-function-design-rev12"
order = 68
cell = "smart.memory.projection.contracts"
physical_crate = "eliot-memory-projection-contracts"
primary_property = "Implement the frozen store-neutral memory read/query/selection/projection wire contract and validators without storage or lifecycle ownership."
field_contract = "crates/smart/eliot-memory-projection-contracts/projection.contract.toml"
public_functions = ["validate_memory_read_snapshot", "validate_memory_query_intent", "validate_memory_selection_policy", "validate_memory_candidate_set", "validate_memory_context_projection_request"]
write_scope = ["crates/smart/eliot-memory-projection-contracts/src/contracts.rs", "crates/smart/eliot-memory-projection-contracts/tests/contracts.rs"]
minimum_tests = ["Complete without denominator rejected", "wrong-scope record rejected", "searched state without receipt rejected", "Unavailable/Blocked without owner reason rejected", "existence/support/accessibility/influence remain separate", "record order does not change digest"]
non_goals = ["canonical read implementation", "canonical write or lifecycle transition", "selection algorithm", "Context admission", "Cargo/lib/re-export integration", "proof above package-local implementation evidence"]
architecture_refs = ["ARCH-MEM-04", "ARCH-CTX-04", "ARCH-EPI-04", "ARCH-SCOPE-01"]
implementation_refs = ["I5.20", "I12.13", "I2.20"]
product_pulse = "D3B_CANONICAL_PROJECTION_PULSE_01"
stop_condition = "Return a ContractChallenge rather than changing the frozen field contract, adding generic JSON, store access, model work or a second owner."
proof_ceiling = "PACKAGE_MODULE_PROOF_ONLY_AFTER_RUST_AND_TEST_EXECUTION"
