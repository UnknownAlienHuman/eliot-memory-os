schema_version = 3
authority = "eliot-cognitive-function-design-rev12"
assignment_authority = "crates/smart/cognitive-rev12-w0-agent-handoff.toml"
field_contract_is_sole_public_type_authority = true
assignment_status = "READY_FOR_SWARM_IMPLEMENTATION_AFTER_PARENT_CONTRACT_MERGE"
order = 28
cell = "smart.context.contracts.view_quality"
physical_crate = "eliot-context-contracts"
primary_property = "Implement the frozen immutable Active Understanding View, reversible omission, dimensional quality, decision-tail and pending-delivery-plan contracts without changing membership or claiming delivery."
field_contract = "crates/smart/eliot-context-contracts/context-compiler.contract.toml"
public_function_selectors = [
  "validate_active_understanding_view",
  "validate_packet_quality",
  "validate_decision_local_tail",
  "validate_pending_context_injection_plan",
]
write_scope = [
  "crates/smart/eliot-context-contracts/src/view_quality.rs",
  "crates/smart/eliot-context-contracts/tests/view_quality.rs",
]
algorithm = [
  "validate admitted-set, recipe, scope, task and State Fence identities",
  "validate whole rendered atoms and exact boundary metadata",
  "partition every admitted decision into one included atom or explicit omission/handle",
  "validate independent quality dimensions and derive blocking dimensions without scalar score",
  "validate decision-tail Safety Floor, do-not-use set, next action, observable, verifier and stop condition",
  "validate pending plan against real integration profile while preserving plan-versus-receipt separation",
  "canonicalize collections and verify all digests",
]
minimum_tests = [
  "renderer cannot alter membership or epistemic/authority ceiling",
  "required omission remains visible and expandable or blocks safe delivery",
  "unknown tokenizer/provider rewrite is Inconclusive, never Pass",
  "one failed quality dimension remains blocking",
  "decision tail missing one load-bearing field is rejected",
  "pending plan exposes no delivery/ack/use/resolution receipt",
]
non_goals = [
  "Context candidate merge or admission",
  "provider projection",
  "tokenizer/provider call",
  "delivery, acknowledgement, use or attention resolution",
  "authority, finish or Product Proof",
  "Cargo.toml/lib.rs/re-export integration",
]
architecture_refs = [
  "ARCH-CTX-01",
  "ARCH-CTX-03",
  "ARCH-CTX-04",
  "ARCH-UND-01",
  "ARCH-EPI-04",
  "ARCH-MOD-03",
]
implementation_refs = [
  "I2.20",
  "I7.10",
  "I7.11",
  "I7.19",
  "I12.13",
  "I12.14",
  "I14.29",
]
product_pulse = "D3B_REACTIVE_CONTEXT_PULSE_01"
stop_condition = "Return a ContractChallenge rather than changing admitted membership, fabricating route/token evidence, weakening the Decision Safety Floor, or adding delivery/authority semantics."
brief_proof_ceiling = "STATIC_AGENT_WORK_UNIT_MATERIALIZATION_ONLY"
implementation_proof_ceiling = "PACKAGE_MODULE_PROOF_ONLY_AFTER_RUST_AND_TEST_EXECUTION"
