schema_version = 2
authority = "eliot-cognitive-function-design-rev12"
order = 80
cell = "meta.tool.surface_assessment"
physical_crate = "eliot-cognitive-quality"
field_contract = "crates/meta/eliot-cognitive-quality/quality.contract.toml"
primary_property = "Assess the actually advertised tool surface and result-use chain without treating registration, tool count or transport completion as utility."
public_functions = ["pub fn assess_tool_surface(request: &ToolSurfaceAssessmentRequest, exposures: &[ToolExposureReceiptView]) -> Result<ToolSurfaceAssessment, ToolSurfaceAssessmentError>"]
write_scope = ["crates/meta/eliot-cognitive-quality/src/tool_surface.rs", "crates/meta/eliot-cognitive-quality/tests/tool_surface.rs"]
minimum_tests = ["registered/advertised/eligible/selected remain distinct", "transport complete with truncated result is not delivered evidence", "rare load-bearing tool retained", "false exposure and loop risk detected", "missing host coverage stays Unknown", "no tool registration/invocation receipt"]
non_goals = ["tool registration/introduction/invocation", "surface mutation", "aggregate tool utility score", "policy/authority/effect/finish ownership", "Cargo/lib/re-export integration"]
architecture_refs = ["ARCH-HELP-01", "ARCH-CTX-01", "ARCH-META-01"]
implementation_refs = ["I7.24"]
product_pulse = "D4_COGNITIVE_QUALITY_PULSE_01"
stop_condition = "Return a ContractChallenge rather than redefining indexed fields or inferring use/outcome from advertisement, selection or transport completion."
proof_ceiling = "PACKAGE_MODULE_PROOF_ONLY_AFTER_RUST_AND_TEST_EXECUTION"
