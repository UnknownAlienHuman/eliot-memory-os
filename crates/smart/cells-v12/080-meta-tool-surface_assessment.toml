schema_version = 2
authority = "eliot-cognitive-function-design-rev12"
order = 80
cell = "meta.tool.surface_assessment"
physical_crate = "eliot-cognitive-quality"
primary_property = "Assess the actually advertised tool surface and result-use chain without treating registration, exposure, selection or transport completion as utility."
field_contract = "crates/meta/eliot-cognitive-quality/quality.contract.toml"
public_functions = ["pub fn assess_tool_surface(request: &ToolSurfaceAssessmentRequest, exposures: &[ToolExposureReceiptView]) -> Result<ToolSurfaceAssessment, ToolSurfaceAssessmentError>"]
public_types = ["ToolSurfaceAssessmentRequest", "ToolExposureReceiptView", "ToolSurfaceStage", "ToolSurfaceStageObservation", "ToolSurfaceDimension", "ToolSurfaceDimensionAssessment", "ToolSurfaceAssessment", "ToolSurfaceAction", "ToolSurfaceActionCandidate", "CognitiveQualityCoverage"]
write_scope = ["crates/meta/eliot-cognitive-quality/src/tool_surface.rs", "crates/meta/eliot-cognitive-quality/tests/tool_surface.rs"]
algorithm = ["validate route, Tool Definition revisions, scope/task family, budget coverage and fence", "separate registered, advertised, eligible, selected, called, transport-complete, result-delivered, expanded, observably-used and outcome-linked states", "assess result completeness, schema/token cost, missing-capability regret, false exposure, loop risk and proof delta independently", "preserve load-bearing but rarely used tools", "return bounded visibility/requalification candidates only"]
minimum_tests = ["advertised but ineligible distinct", "selected/called/transport-complete cannot imply result delivery/use", "partial/truncated result explicit", "rare load-bearing tool retained", "missing host coverage stays Unknown", "loop risk detected", "no tool registration/invocation"]
non_goals = ["tool registry mutation", "tool introduction/invocation", "route execution", "provider SDK", "aggregate surface score", "Cargo/lib/re-export integration"]
architecture_refs = ["ARCH-HELP-01", "ARCH-CTX-01", "ARCH-META-01", "ARCH-EPI-04"]
implementation_refs = ["I7.24", "I2.20"]
product_pulse = "D4_COGNITIVE_QUALITY_PULSE_01"
stop_condition = "Return a ContractChallenge rather than inferring use from transport, hiding missing host coverage, registering/invoking tools, or optimizing tool count."
proof_ceiling = "PACKAGE_MODULE_PROOF_ONLY_AFTER_RUST_AND_TEST_EXECUTION"
