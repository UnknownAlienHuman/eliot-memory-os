schema_version = 2
authority = "eliot-cognitive-function-design-rev12"
order = 69
cell = "smart.memory.candidate_query"
physical_crate = "eliot-memory-projection"
primary_property = "Select a bounded deterministic memory candidate set from one materialized read snapshot, cue result, intent and immutable policy."
field_contracts = ["crates/smart/eliot-memory-projection-contracts/projection.contract.toml"]
public_functions = ["pub fn query_memory_candidates(snapshot: &CanonicalMemoryReadSnapshot, intent: &MemoryQueryIntent, cues: &CueActivationResult, policy: &MemorySelectionPolicy) -> Result<MemoryCandidateSet, MemoryCandidateQueryError>"]
write_scope = ["crates/smart/eliot-memory-projection/src/candidate_query.rs", "crates/smart/eliot-memory-projection/tests/candidate_query.rs"]
minimum_tests = ["exact cue precedes broad candidate", "negative memory cannot be displaced", "minority/counterevidence retained", "partial denominator visible", "all considered records traced", "permutation deterministic", "no delivery/use/support mutation"]
non_goals = ["storage read", "Context admission", "lifecycle transition", "policy ownership", "Cargo/lib/re-export integration"]
architecture_refs = ["ARCH-CTX-02", "ARCH-CTX-04", "ARCH-MEM-04"]
implementation_refs = ["I5.20", "I12.14", "I7.19"]
product_pulse = "D3B_CANONICAL_PROJECTION_PULSE_01"
stop_condition = "Return a ContractChallenge if the frozen types are insufficient or implementation requires I/O, model work, mutable session state or scalar confidence."
proof_ceiling = "PACKAGE_MODULE_PROOF_ONLY_AFTER_RUST_AND_TEST_EXECUTION"
