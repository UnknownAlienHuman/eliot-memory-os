schema_version = 2
authority = "eliot-cognitive-function-design-rev12"
order = 85
cell = "meta.common_ground.assessment"
physical_crate = "eliot-understanding-assessment"
primary_property = "Assess public causal inheritance across route/model/harness replacement by preserving goals, decisions, invariants, rivals, unknowns, commitments and action consequences—not summary similarity."
field_contract = "crates/meta/eliot-understanding-assessment/understanding.contract.toml"
public_functions = ["pub fn assess_common_ground(request: &CommonGroundAssessmentRequest, source: &PublicInheritanceSnapshot, target: &ReconstructedInheritanceSnapshot) -> Result<CommonGroundAssessment, CommonGroundAssessmentError>"]
public_types = ["CommonGroundAssessmentRequest", "PublicInheritanceItem", "PublicInheritanceSnapshot", "ReconstructedInheritanceItem", "ReconstructedInheritanceSnapshot", "CommonGroundDimension", "CommonGroundDimensionResult", "CommonGroundDimensionAssessment", "CommonGroundGap", "CommonGroundDisposition", "CommonGroundAssessment", "UnderstandingCoverage"]
write_scope = ["crates/meta/eliot-understanding-assessment/src/common_ground.rs", "crates/meta/eliot-understanding-assessment/tests/common_ground.rs"]
algorithm = ["validate source/target route fingerprints, task, product, public inheritance revision, denominator and fences", "compare stable terminology/identity, goal/acceptance, decisions/rationale, invariants/negative memory, rivals/counterevidence, unknowns/inquiries, commitments and predicted action consequences independently", "classify exact preservation, compatible reformulation, omission, contradiction, stale dependency or untested competence", "emit material gaps with expansion handles and discriminative requalification probes", "derive AdequateForScope/Partial/Incompatible/Stale/Untested without authority transfer"]
minimum_tests = ["matching summary with lost invariant fails", "lost rival/unknown/commitment visible", "action consequence mismatch detected", "native continuation state rejected", "Complete without denominator rejected", "required/protected omission blocks adequacy", "no route switch/authority transfer receipt"]
non_goals = ["route execution/switch", "native-state transfer", "authority/task-finish transfer", "model call", "summary-similarity score", "Cargo/lib/re-export integration"]
architecture_refs = ["ARCH-PORT-01", "ARCH-SELF-01", "ARCH-UND-01", "ARCH-EPI-04"]
implementation_refs = ["I7.15", "I12.23", "I2.20"]
product_pulse = "D4_UNDERSTANDING_TRANSFER_PULSE_01"
stop_condition = "Return a ContractChallenge or Untested/Partial rather than accepting summary similarity, native model state, lost causal items, or tacit competence/authority transfer."
proof_ceiling = "PACKAGE_MODULE_PROOF_ONLY_AFTER_RUST_AND_TEST_EXECUTION"
