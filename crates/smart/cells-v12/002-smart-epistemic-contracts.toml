schema_version = 3
authority = "eliot-cognitive-function-design-rev12"
assignment_authority = "crates/smart/cognitive-rev12-w0-agent-handoff.toml"
field_contract_is_sole_public_type_authority = true
assignment_status = "READY_FOR_SWARM_IMPLEMENTATION_AFTER_PARENT_CONTRACT_MERGE"
order = 2
cell = "smart.epistemic.contracts"
physical_crate = "eliot-epistemic-contracts"
primary_property = "Implement the frozen provider-neutral Current Epistemic Position algebra with explicit observations, support, assumptions, rivals, structured conflicts, stale/superseded state, unknowns and investigation requirements."
field_contract = "crates/smart/eliot-epistemic-contracts/current-position.contract.toml"
public_function_selectors = [
  "validate_position_request",
  "validate_current_epistemic_position",
  "current_position_digest",
]
write_scope = [
  "crates/smart/eliot-epistemic-contracts/src/position.rs",
  "crates/smart/eliot-epistemic-contracts/tests/position.rs",
]
algorithm = [
  "validate question/scope/task/fence and exact denominator",
  "validate every claim role, provenance closure, dependency class, assertability, freshness and verifier applicability",
  "validate each conflict locally with participants, independent/shared lineage, unresolved aspects and revision conditions",
  "validate each inquiry as advisory with discriminator, falsifier-equivalent observable, stop and revision conditions",
  "canonicalize claims, conflicts, inquiries and provenance closure and verify digest",
]
minimum_tests = [
  "absence of refutation cannot create ExplicitAssumption or Supported",
  "Observed cannot silently become Supported or Assertable",
  "dependent duplicate evidence does not count as independent corroboration",
  "one local conflict does not conflict every proposition",
  "stale and superseded claims retain exact lineage",
  "InvestigationRequirement exposes no job/tool/effect execution",
  "input permutation is deterministic",
]
non_goals = [
  "evidence acquisition",
  "support admission/promotion",
  "Context admission",
  "investigation execution",
  "canonical write",
  "Cargo.toml/lib.rs/re-export integration",
]
architecture_refs = [
  "ARCH-EPI-01",
  "ARCH-EPI-03",
  "ARCH-EPI-04",
  "ARCH-UND-01",
  "ARCH-MOD-03",
]
implementation_refs = [
  "I2.20",
  "I12.11",
  "I12.12",
  "I12.18",
]
product_pulse = "D3A_ORIENTATION_PULSE_01"
stop_condition = "Return a ContractChallenge rather than inferring assumptions/support, flattening conflict, discarding stale/minority evidence, or adding investigation execution."
brief_proof_ceiling = "STATIC_AGENT_WORK_UNIT_MATERIALIZATION_ONLY"
implementation_proof_ceiling = "PACKAGE_MODULE_PROOF_ONLY_AFTER_RUST_AND_TEST_EXECUTION"
