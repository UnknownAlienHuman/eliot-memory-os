schema_version = 2
authority = "eliot-cognitive-function-design-rev12"
order = 74
cell = "smart.context.provider_registry_contracts"
physical_crate = "eliot-context-contracts"
primary_property = "Implement the exact seven-class provider registry, expected-provider denominator and whole ProviderContribution contracts."
field_contract = "crates/smart/eliot-context-contracts/provider-contribution.contract.toml"
public_functions = ["validate_context_provider_registry", "validate_expected_provider_set", "validate_context_provider_request", "validate_provider_contribution", "provider_contribution_digest"]
write_scope = ["crates/smart/eliot-context-contracts/src/provider_registry.rs", "crates/smart/eliot-context-contracts/src/provider_contribution.rs", "crates/smart/eliot-context-contracts/tests/provider_registry.rs", "crates/smart/eliot-context-contracts/tests/provider_contribution.rs"]
minimum_tests = ["every legacy/extra provider class rejected", "duplicate provider rejected", "missing mandatory provider is Blocked", "Complete without denominator rejected", "role overclaim rejected", "unsafe representation rejected", "single generic request type", "contribution contains no admission/authority field", "order-independent digest"]
non_goals = ["provider implementation", "Context admission or merge", "delivery/use receipt", "authority", "Cargo/lib/re-export integration"]
architecture_refs = ["ARCH-MOD-03", "ARCH-CTX-01", "ARCH-CTX-04", "ARCH-EPI-04"]
implementation_refs = ["I12.13", "I7.11", "I2.20"]
product_pulse = "D3B_CANONICAL_PROJECTION_PULSE_01"
stop_condition = "Return a ContractChallenge rather than extending the closed provider class set, duplicating request types or weakening denominator semantics."
proof_ceiling = "PACKAGE_MODULE_PROOF_ONLY_AFTER_RUST_AND_TEST_EXECUTION"
