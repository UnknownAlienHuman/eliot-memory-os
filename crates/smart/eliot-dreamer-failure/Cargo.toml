[package]
name = "eliot-dreamer-failure"
version = "0.1.0"
edition = "2024"
rust-version = "1.94"
license = "MIT"
publish = false
description = "Propose one exact negative-memory fingerprint with failed action, outcome, invariant, reopen and extinction conditions."

[lib]
path = "src/lib.rs"

[package.metadata.eliot]
prototype = false
source_status = "IMPLEMENTED"
workspace_admission = "admitted via #966 (T8-AS2) root workspace membership"
functional_cell = "smart.dreamer.failure"
source_layer = "C1"
runtime_layer = "R6"
runtime_bundle = "eliot-dreamer"
agent_order = 26

[dependencies]
eliot-dreamer-contracts = { path = "../eliot-dreamer-contracts" }
eliot-receipts = { path = "../../foundation/eliot-receipts" }
schemars = { version = "1.2.1", features = ["derive", "uuid1"] }
serde = { version = "1.0.228", features = ["derive"] }
serde_json = { version = "1.0.150" }

[dev-dependencies]
eliot-contracts = { path = "../../foundation/eliot-contracts" }
eliot-evidence = { path = "../../foundation/eliot-evidence" }

[lints]
workspace = true
