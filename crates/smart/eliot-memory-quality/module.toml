schema_version = 4
module_id = "smart.memory.quality"
crate = "eliot-memory-quality"
status = "PROTOTYPE"
wave = "cognitive-micromodules-wave-09"
agent_order = 76
plane = "Smart"
runtime_layer = "R5"
delivery_depth = "D3b"
source_layer = "C1"
runtime_bundle = "eliotd"
state_class = "stateless"
hot_path = false
lifecycle_owner = "smart.memory.quality"
loc_soft_ceiling = 10000
loc_policy = "review ceiling only; causal cohesion, proof, owner, build/test closure and replacement boundary decide topology"
product_objective = "Memory ecology, gravity, and lifecycle economics are assessed from immutable projections with exact denominators, never from retrieval or popularity."
causal_property = "memory ecology and memory gravity assessment"
old_failure_or_missing_capability = "memory items and clusters are judged by corpus counts, retrieval repetition, or recency with no independent denominator or gravity/cost fields (CC-008)."
discriminator = "every assessment carries its exact independently recheckable denominator with horizon, workload/exposure, fence, and exclusions; low use never reduces support"
primary_responsibility = "Assess one bounded memory scope into a MemoryEcologyAssessment with gravity, economics, maintenance, and counter-metric sections."
owned_mutable_state = []
allowed_effects = []
inputs = ["immutable MemoryProjectionRecord read set by handle (CC-008)", "immutable admitted task/continuity/safety projections by handle (CC-004)", "advisory HarnessActivationReceipt candidates by handle"]
outputs = ["MemoryEcologyAssessment"]
depends_on = ["eliot-memory-projection-contracts", "eliot-contracts", "eliot-evidence"]
providers = ["memory projection provider", "Governor/Memory OS canonical read owner"]
consumers = ["Governor disposition path", "eliot-dreamer-accessibility (gravity/ecology inputs)"]
non_goals = ["store queries", "read-set expansion", "support/influence promotion", "aggregate intelligence score", "universal fixed thresholds"]
invariants = ["consume immutable owner-produced projections only", "low use never reduces support", "retrieval/repetition/agreement never reinforce support", "every Complete state requires the exact independently recheckable denominator", "no aggregate intelligence/confidence/utility score"]
failure_behavior = "Return a typed error on invalid input or missing denominator; per-item gaps become explicit unknown exclusions, never silent drops."
proof_surface = ["stale/conflicted/superseded cases", "false-activation history", "valid-near-match positive controls", "cold-capture ratios with horizon", "weak-claim rate with denominator"]
proof_ceiling = "STATIC_FIELD_CONTRACT_ONLY"
product_pulse = "W9_MEMORY_QUALITY_PULSE_01"
replacement_boundary = "implementation behind the declared public contract; package ownership creates no runtime or semantic authority"
merge_rejoin_condition = "merge back when no independent consumer, proof, dependency or replacement seam remains"
split_trigger = "a second causal responsibility, mutable owner, independent proof surface or independent rollback boundary appears"

[[donor]]
path = "docs/architecture/I12-21-memory-ecology-residual-experience-and-transfer.md"
disposition = "authoritative contract"

[[donor]]
path = "docs/architecture/I12-34-cognitive-proof-ladder-and-ecological-field-proof.md"
disposition = "authoritative proof ceiling"

[agent_task]
objective = "Freeze the owner-neutral memory ecology and gravity field contract with exact denominator; no implementation in this work unit."
write_scope = ["crates/smart/eliot-memory-quality/**"]
deliverables = ["frozen field contract entry", "denominator recheck rule", "affected edge fixtures (contract_only)"]
acceptance = ["one declared responsibility only", "bounded deterministic behavior", "visible local degradation"]
forbidden = ["canonical writes outside the named Governor transition path", "truth/support/authority/policy/finish/effect promotion", "provider or model SDKs in pure cognitive cores", "vendor or storage types in public contracts", "unbounded synchronous work", "silent truncation", "oracle or verifier weakening in the same work unit", "implicit scope widening", "active-generation self-modification"]
stop_condition = "Stop and return ContractChallenge when the work needs another owner, store access, model work or hidden state."
challenge_path = "return ContractChallenge to the integration owner when owner, discriminator, proof ceiling or dependency boundary is wrong"
integration_owner = "cognitive-wave-integrator"
implementation_language = "Rust 2024"
required_verification = ["module/package proof at the declared proof ceiling", "real provider/consumer fixture for every changed edge", "the named Product Pulse before promotion", "exact donor reuse/rewrite/reject trace"]
writeback = "return implementation artifact, exact proof evidence, donor disposition, unresolved ContractChallenges and merge/remove recommendation"
workspace_admission = "forbidden until implementation, package proof, affected edge proof, current-main rebase and integration-owner review"

# Acceptance is decided by the owning contract and independent review
# (I0.10: line/test/type counts are not goals). `scripts/verify-work-unit.py`
# is a retired legacy diagnostic that always reports completion=NOT_VERIFIED
# and never accepts a work unit. This table names the concrete contract and
# the required exports/tests plus small behaviour checks the reviewer verifies.
[acceptance]
require_deny_unknown_fields = true
forbid_unsafe = true
required_exports = []
required_tests = []
forbidden_patterns = ['serde_json::Value', 'unimplemented!', 'todo!']
