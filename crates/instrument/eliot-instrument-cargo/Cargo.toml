[package]
name = "eliot-instrument-cargo"
version = "0.1.0"
edition = "2024"
rust-version = "1.89"
license = "MIT"
description = "Governed Cargo instrumentation adapter over the production process port"

[dependencies]
eliot-instrument-api.workspace = true
eliot-process.workspace = true
serde = { version = "1.0.228", features = ["derive"] }
serde_json = "1.0.150"
thiserror = "2.0.18"

[lints]
workspace = true
