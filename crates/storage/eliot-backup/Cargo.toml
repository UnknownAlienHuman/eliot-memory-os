[package]
name = "eliot-backup"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Governed ECXF logical backup and isolated restore contracts"

[package.metadata.eliot]
workspace_admission = "admitted bounded non-runtime product surface per #1873 (operator CLI previews/coverage/issuance/isolated runs plus isolated rehearsal runner; Kernel-owned governed capture/restore coordination per #959/#960; #1141 disposition; governed export path: #1871); production execution/cutover stays with #960/#961 owners; not a selectable storage fallback"

[dependencies]
aes-gcm = { version = "=0.10.3", default-features = false, features = ["aes", "alloc", "zeroize"] }
eliot-blob-api.workspace = true
eliot-contracts.workspace = true
eliot-ecxf = { path = "../eliot-ecxf", version = "0.1.0" }
eliot-platform-windows.workspace = true
eliot-security-contracts.workspace = true
eliot-store-api.workspace = true
getrandom = "=0.2.17"
zeroize = "=1.9.0"
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
blake3.workspace = true

[dev-dependencies]
eliot-store-memory = { path = "../eliot-store-memory", version = "0.1.0" }
toml.workspace = true

[lints]
workspace = true
