[package]
name = "eliot-controlboard"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Provider-neutral role-filtered ELIOT ControlBoard contract (bounded reference fixture; no production consumer)"

[package.metadata.eliot]
work_id = "A-08"
functional_cell = "controlboard"
lifecycle_owner = "A-08"
source_layer = "C3"
proof_entrypoint = "cargo test -p eliot-controlboard --all-targets --all-features"
# Fixture disposition (#1213 MGR01 half): bounded reference fixture with no
# production consumer in this lane. The `eliot-cli` catalogue edge is severed
# to an inlined `PLAN_GAP` literal; the only remaining reverse dependency is
# `bins/eliotd` test-only adapters. Full delete follows with the eliotd lane.
# This crate is frozen: no board extension, owner bindings, authority, or
# currentness.
fixture_status = "bounded-reference-fixture"
production_consumers = "none"
remaining_reverse_dependency = "bins/eliotd test-only adapters"
follow_up = "full delete with eliotd lane"

[dependencies]
eliot-agent-coordinator.workspace = true
eliot-contracts.workspace = true
eliot-evaluation-contracts.workspace = true
eliot-evidence.workspace = true
eliot-observation-contracts.workspace = true
eliot-protocol.workspace = true
eliot-receipts.workspace = true
eliot-security-contracts.workspace = true
eliot-skill.workspace = true
serde.workspace = true
serde_json.workspace = true
sha2.workspace = true
thiserror.workspace = true

[lints]
workspace = true
