[package]
name = "eliot-authority"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
description = "Pure G-01 authority contracts and deterministic validation"

[dependencies]
eliot-receipts.workspace = true
eliot-runtime-contracts.workspace = true
eliot-security-contracts.workspace = true

[dev-dependencies]
# PLAN_GAP(G-01): the admitted providers use, but do not re-export, the
# canonical StateFence/AuthorityEpoch constructors needed by fixtures.
eliot-contracts.workspace = true

[lints]
workspace = true
