[package]
name = "eliot-memory-projection-provider"
version = "0.1.0"
edition = "2024"
rust-version = "1.94"
license = "MIT"
publish = false
description = "Build bounded, fenced canonical-memory projections through Governor-owned named read ports."

[lib]
path = "src/lib.rs"

[package.metadata.eliot]
prototype = true
source_status = "NOT_IMPLEMENTED"
workspace_admission = "pending_contract_package_real_store_edge_and_integration_proof"
functional_cell = "governor.memory.projection_provider"
source_layer = "C2"
runtime_layer = "R3"
runtime_bundle = "eliotd"
agent_order = 46

[dependencies]
eliot-contracts = { path = "../../foundation/eliot-contracts" }
eliot-evidence = { path = "../../foundation/eliot-evidence" }
eliot-memory-projection-contracts = { path = "../../foundation/eliot-memory-projection-contracts" }
