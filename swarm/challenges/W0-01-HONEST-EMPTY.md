# W0-01 contract challenge — accepted

Status: `ACCEPTED_BY_ROOT`

The frozen and current censuses contain zero tracked Rust `todo!` or
`unimplemented!` sites. Padding `docs/UNIMPLEMENTED.md` with a fabricated row,
or relabelling the 24 typed `PlanGap` catalogue entries as source-level
`Unimplemented`, would violate the truth hierarchy and destroy the distinction
W0-01 is required to establish.

Root disposition:

- `docs/UNIMPLEMENTED.md` must be a nonempty generated document, not a registry
  with an artificially nonzero row count;
- `Row count: 0` plus `Status: HONEST_EMPTY` is the only accepted current
  projection;
- any tracked Rust `todo!` must use `todo!("<A/I anchor>: <work>")` and resolve
  to a real heading in the normative pair;
- `unimplemented!` is rejected;
- CLI `Unimplemented` remains a typed variant distinct from `Unavailable` and
  its architecture anchors are checked against the same normative heading
  rule;
- W1 refusal inventory remains a separate domain and must not be copied into
  this registry.

This disposition resolves the W0-01 challenge without modifying the immutable
recovery-program input.
