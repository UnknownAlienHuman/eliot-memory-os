# ROOT-DIRECTIVE v1.5 — продукт не недописан, он не собран

**Основание:** внешняя ревизия дерева от 2026-08-26 на `d5f0fdc` + рабочий оверлей.
**Продолжает:** `ROOT-DIRECTIVE-v1.4.md`. Действующая копия программы — `docs/tasks/RECOVERY_PROGRAM_v1.md` (v1.3).
**Авторитетность:** программная. Не Architecture, не Implementation, не Product Proof, не cutover.
**Отменяет:** hosted-прогон как условие любого гейта до восстановления квоты (§6); универсальный порог «100k STU на файл» как противоречащий `I2.16` (§3.3); утверждение программы, что W4-01 блокирован W3-04 (§3.1); запуск Ox OpenCode в профиле `plan` (§7).
**Не отменяет:** ничего из принятого в W0/W1, решение по cutover (Option A), решение по `AdmittedSwarmPlan`.

---

## 0. Что показала проверка

> **Снимок на 2026-08-26.** Числа этого раздела — замер того дня и с тех пор изменились; они сохранены как основание выводов, а не как текущее состояние. Действующие величины — только в §0.1.

**Первое.** Из 119 отказов класса `Unimplemented` **111 — типизированный `PlanGap`**, и **64 называют блокировщик дословно**:

```
CommandAvailability::PlanGap { missing_work_id: "A-06", dependency: "eliot-mcp" }   ×21
WorkerError::PlanGap { detail: "P-03 ProcessExecutor was not injected" }
WorkerError::PlanGap { detail: "G-01-facing admission provider was not injected" }
TransportError::PlanGap { dependency: "eliot-platform-windows", reason: "…не скомпонован" }
… "durable replay provider was not injected", "verification port is missing"
```

Ключевые слова: **`was not injected`**, **`port is missing`**, **`dependency: <крейт, который существует>`**.

`eliot-mcp` — **1 894 строки** рабочего кода, ноль потребителей, блокирует 21 команду CLI. `eliot-platform-windows` — 703 КБ. `ProcessExecutor` живёт в `eliot-process`, на который ссылаются 24 манифеста.

**Продукт не недописан. Он не собран.** Реализации есть и недостижимы, потому что их никто не композирует. Отсюда же ноль вызывающих у `commit_canonical` и 40 библиотек с нулём потребителей. Продукт уже написал список работ о себе — типизированно, с указанием блокировщика. Его надо **доделать**, а не мерить термометром.

**Второе, и это блокирует всё остальное.** `cargo test --locked --workspace` красный. На момент написания директивы:

```
test result: FAILED. 323 passed; 1 failed; 2 ignored     ← D0, ЗАКРЫТ 2026-08-26
host_runtime::supervised_process::tests::supervised_process_timeout_forces_job_and_proves_zero_orphan
```

После закрытия D0 прогон прошёл дальше и открыл следующее красное — D5 (§5). Значит `verify.ps1` шаг `workspace-tests` по-прежнему не проходит, и **ни один гейт закрыть нельзя**. Это не неудача починки: гейт, научившийся краснеть, показывает следующее красное. Текущее состояние — в §0.1.

**Третье — первопричина неэффективности агентов, и она измерена.** Между нормативной парой (24 251 строка, 1.17 МБ) и кодом (546 файлов, 7.0 M STU) **нет навигации ни в одну сторону**:

```
ARCH-* якорей в книгах            58
ARCH-* якорей в коде               1   (ARCH-SEC-01, единственный на весь workspace)
файлов с любым якорем              5   из 546
refusals.csv: normative_anchor   UNKNOWN во всех 751 строках
conformance.toml: owner          UNKNOWN в 55 из 58, support UNKNOWN во всех 58
```

`reports/audit/ELIOT_MODEL_EFFICIENCY_20260814.md` называет цену прямо: после отключения **всех** MCP реальные многошаговые задачи всё равно накапливали `76 917 – 118 856` cached input tokens, и источник — «агенты читали почти мегабайтную Implementation, прочие authority-документы, исходники и вывод Cargo».

Агент читает книгу целиком не от недисциплинированности. **Он читает её потому, что искать негде.** W0-10 доказал механизм на одном примере: нормативные имена `BootstrapBriefCompiler` и `CurrentSystemEvidenceCompiler` давали **ноль** вхождений, а способность была написана под именем `compile_provider_gap_brief`. Формулировка v1.4 дословно: *«следующий исполнитель не найдёт уже написанного — механизм, которым прошлая кампания делала „правильные“ реализации недостижимыми»*.

Там же v1.4 отложила общий случай: *«связывание всех нормативных имён с кодом… решается отдельным WorkItem после закрытия W0»*. **W0 закрыт. WorkItem не открыт.** Это и есть старая работа, которую надо доделать, — см. §2.1.

**Правило самоограничения.** Пункт, который нельзя закрыть за одну сессию одного исполнителя, — не пункт, а `ContractChallenge` к этой директиве. Директива, породившая новую волну, недействительна.

---

## 0.1. Ведомость исполнения на 2026-08-26

Директива — снимок на момент написания. Дерево ушло вперёд на 21 коммит. Ниже — что закрыто, чтобы следующий исполнитель не переделывал сделанное и не действовал по устаревшему факту. Проверено в дереве, а не по сообщениям коммитов.

| Шаг | Состояние | Проверка |
|---|---|---|
| 1 коммит дерева | **ЗАКРЫТ** | продуктовое дерево чистое; 12 M + 1 ?? остались только в `research/`, вне периметра |
| 2 D0 | **ЗАКРЫТ** | `AtomicUsize`-nonce + PID в `operation_id` фикстуры; регрессионный guard `supervised_process_fixtures_never_share_a_named_job` проходит; продуктовый `job_object_name` не тронут — верно, ему нужна детерминированность для re-attach |
| — D5 | **ОТКРЫТ, блокирует гейт** | открылся из-под D0; см. §5 |
| 3 `ci.yml` | **ЗАКРЫТ** | `on: workflow_dispatch`; `-ne 126`; `cargo metadata` = 126; `PROJECT_MAP` выровнен |
| 4 D1–D4 | **ЗАКРЫТ** | `let _ = worktree.cleanup()` и `let _ = runtime.cleanup()`; фикстурный репозиторий изолирован от живого |
| 5 гейт W2 | **ЗАКРЫТ** | `swarm/gates/W2.md`: состояние №1, `W2-04 OPEN_MANUAL`, `W2-01 REJECTED_ZERO_TOOL_BUDGET`, граница утверждения записана дословно |
| 6 ребро mcp | **ЗАКРЫТ** | `eliot-cli` зависит от `eliot-mcp` и `eliot-controlboard`; 29 сайтов сменили блокировщика на настоящий |
| 7 индекс §2.1 | **~7 %** | 4 привязки из 58; `owner UNKNOWN` 55→54; `normative_anchor UNKNOWN` 751→744 |
| 8 провайдер №1 | **в работе** | `R13_1A_STATIC_CONTRACTS_COMPLETE_DYNAMIC_ADMISSION_BLOCKED`; серверный обработчик `eliot.agent-bridge.activate` в дереве отсутствует — строка встречается только в клиенте (`bins/eliot-agent-bridge/src/lib.rs:195`) |
| Приложение P1–P6 | **ЗАКРЫТ** | `ROOT-DIRECTIVE-v1.5-APPENDIX-P-SOL-DECISIONS.md`, все шесть с потолком, триггером, откатом и носителями |
| §3.3 STU | **ЗАКРЫТ** | классы из `I2.16:3009`, исключения generated/fixtures, явный `NO_SPLIT_AUTHORITY` |

**Незакрытое, по убыванию:** D5 → индекс §2.1 (7 %) → провайдер №1 (шаг 1A из четырёх: transport admission → reachable server operation → semantic resolution → session/fence issuance).

### Дозамер 2026-08-27

| Величина | 26.08 | 27.08 | Направление |
|---|---:|---:|---|
| `package_staging` красных | 3 | **13** | хуже |
| `bins/eliot-kernel/src/lib.rs` | 15 861 | **15 976** | растёт, §2.2 не исполнялась |
| `pub` в ядре / тестов | 21 / 39 | **21 / 39** | контракт не менялся |
| `refusals.csv` строк | 751 | **766** | Unimplemented держится на 119 |
| `ARCH-*` якорей в коде | 1 | **5** | +4 |
| `owner UNKNOWN` в conformance | 55 | **54** | −1 |
| библиотек-сирот | 40 | **37** | −3 |
| `cargo check --workspace` | падал | **0 ошибок** | лучше |
| `--locked` | неисполним | **работает** | лучше |

Читается так: сборка и топология выправлены, **фронт не двинулся**. Ядро растёт при неизменном публичном контракте — то есть прибывает ровно приватная глубина, которую запрещает `A2.3`. `package_staging` краснеет быстрее, чем чинится: за трое суток +10 падений при +2 проходящих.

**Приоритет не меняется:** сначала D5, потому что без него не закрывается ни один гейт; §2.2 — до следующей способности в ядре, иначе четвёртые сутки повторят третьи.

### Два дефекта дисциплины, вскрытые той же проверкой

**Д-1. Двадцать один коммит работы не породил ни одного структурного результата.** В `swarm/results/` за эти коммиты добавились только файлы, которые уже лежали неотслеживаемыми (W3-01/02/03, логи прогонов, бриф W2-01). Для D0–D4, ребра mcp, индекса, `measure-stu` и R13-1A результата нет ни одного. Это расходится с §2.3 раскладки программы (`results/<work-item-id>.json`), с §2.6 (каждая единица получает disposition) и с R9 (результат — структура, не проза).

**Причина частично моя, и я её снимаю.** §8.9 запрещает заводить новую `schema_version`. Это читается как «не создавать результаты вовсе». Запрет означает **не плодить версии конверта**, а не «не писать результат». Единица работы пишет результат в **уже существующем** `eliot.bootstrap-work-result.v1` — новой схемы для этого не требуется.

**Д-3. Ядро выросло в 92 раза за 12 дней, и причина та же.** Замерено по истории `bins/eliot-kernel/src/lib.rs`:

```
15 авг     172 строки   Add Kernel production composition root   ← так и надо: корень композиции
16 авг   1 449          Store-to-Kernel bootstrap
17 авг   3 112          process validation to canonical
19 авг   5 012          durably activate kernel candidates
20 авг   9 000          close Pulse 4 Store rebind production
20 авг  10 918          close remaining Store rebind crash/fence   ← +5 906 за один день
26 авг  13 735          carry agent bridge admission
сейчас  15 861          76 коммитов правили этот файл
```

Что внутри 15 861 строки: **2 модуля, 21 публичный элемент, 39 тестов**. Упоминаний `process` — 797, `store` — 465, `session` — 389, `candidate` — 186, `activation` — 169, `fence` — 128.

Проверено поимённо, чтобы не судить по частотам: `HostStateJournal`, `HostInstallationEpoch`, `ApprovedBuild`, `TaskContract` в ядре **отсутствуют** — чужого владения по именам нет. Дефект в другом: **21 публичный элемент на 15 861 строку**. Публичный контракт крошечный, приватная реализация — весь остальной файл. `A2.3`: *«Kernel поддерживает все четыре функциональные плоскости, **но не содержит их глубину**»*; `ARCH-MOD-01 — Small living Kernel`.

**Механизм тот же, что и в §0 третьей находки.** `eliot-process` существует и имеет 24 потребителя — но агент, правящий `eliot-kernel/src/lib.rs`, его не находит и пишет заново там, где уже стоит курсор. Раздувание ядра и отсутствие индекса — **одна болезнь, не две**. Пока индекса нет, каждая следующая способность будет садиться в тот же файл.

Отсюда запрет §8.16.

**Д-2. Ни один из 21 коммита не несёт идентичности исполнителя.** Все от `d01-temp <d01-temp@example.invalid>`, без co-author и без provider/model в теле. `swarm/README.md` требует проверять model/provider identity перед приёмом результата; 17 артефактов с идентичностью — все от прошлой кампании (`opencode-go/ox-alpha-free` ×10, `openrouter/stealth/ox-alpha` ×7). Кто именно исполнял шаги 1–8, из дерева установить нельзя.

---

## 1. Единственное открытое утверждение

> **Типизированные отказы продукта перестают называть отсутствующим то, что уже написано.**

Мера — не число прогонов и не цвет значка.

**Поправка от 2026-08-26, по факту исполнения шага 6.** Первая редакция назначала метрикой падение счётчика `classification=Unimplemented` в `refusals.csv`. Это неверно, и проверка это показала: после подключения `eliot-mcp` и `eliot-controlboard` счётчик остался **119**, потому что 21 сайт переехал *внутри* класса — с ложного блокировщика на настоящий:

```
было   21 сайт   dependency: "eliot-mcp"                                    ← крейт существует
стало  23 сайта  dependency: "no admitted Kernel/Governor provider is injected"
```

Это и есть прогресс — фронт сдвинулся на один честный шаг, — но выбранный счётчик его не видит. Действующая метрика:

```
СНИМАЕТСЯ: число сайтов, чей блокировщик — существующий в дереве крейт   29 → 0
ОСТАЁТСЯ:  число сайтов, чей блокировщик — неинъектированный провайдер   → падает только по R13
```

Первая величина измеряет ложные метки, вторая — настоящую сборку. Обе считаются по `refusals.csv` на перегенерации. Смешивать их в один счётчик нельзя: первая падает от рёбер, вторая — только от провайдеров.

---

## 2. Фронт работ, который продукт назвал сам

| Блокировщик | Сайтов | Состояние в дереве | Кому |
|---|---:|---|---|
| ~~`eliot-mcp`~~ | ~~21~~ → **0** | ЗАКРЫТО 2026-08-26: ребро есть, сайты назвали настоящий блокировщик | — |
| `eliot-user-broker-core` | 18 | есть | после провайдера |
| `eliot-blob` | 17 | есть, 5 манифестов ссылаются | после провайдера |
| ~~`eliot-controlboard`~~ | ~~8~~ → **0** | ЗАКРЫТО 2026-08-26 тем же способом | — |
| `eliot-wasm-runtime` | 5 | есть | ждёт |
| Порты и инъекции: `ProcessExecutor` P-03, durable replay, admission G-01, evidence sink, verification ports | ~23 | реализации есть, инъекции нет | **только Sol** — authority seam, §7.4 |
| Без названного блокировщика | 55 | внутри тех же крейтов | не трогать, §4.1 |

**Порядок §5.1 (R13) не переставляется.** Пока провайдера нет, «не инъектирован провайдер» — честный отказ, и ни один потребитель ниже по цепи не закрывается по-настоящему. Обратный порядок уже произвёл `A-01/A-03/A-05`.

**Уточнение принято 2026-08-26 и имеет приоритет над этим параграфом.** Директива называла провайдером №1 серверную строку `eliot.agent-bridge.activate`. `swarm/decisions/R13-PROVIDER-1-ADMISSION-AND-RESOLUTION.md` (`ACCEPTED_SOL_BOUNDARY`) это опровергает по нормативу: строка сама по себе провайдером не является — провайдер достигнут, когда реальный процесс моста проходит границу transport admission, шлёт identity-bound запрос, доходит до закрытого селектора Kernel и получает один типизированный ответ. Внутреннее разбиение на причинные предпосылки принято; порядок потребителей после R13 не меняется. Действующее состояние — `R13_1A_STATIC_CONTRACTS_COMPLETE_DYNAMIC_ADMISSION_BLOCKED`.

Отсюда распределение, а не лозунг: **фронт — Sol-only.** У swarm на нём работы нет. Поэтому swarm занимается D0–D4, рёбрами, коммитом и §2.1 — а не новыми разрезами.

### 2.1. Индекс якорь↔символ — единственная новая единица, и она отменяет работу

Отложенный в v1.4 WorkItem открывается сейчас, потому что он единственный сокращает стоимость всех остальных. Это **не новая инвентаризация — это join уже существующих артефактов**:

```
58 якорей               → уже извлечены gen-conformance.ps1 в docs/conformance.toml
граф кода               → уже есть: .codebase-memory, 54 134 узла, 268 208 рёбер
крейты и fan-out        → уже есть: swarm/inventory/modules.json
блокировщики и сайты    → уже есть: swarm/inventory/refusals.csv
```

Выход — заполненная колонка `owner` в `docs/conformance.toml` **со стороны кода**, а не из Appendix H, плюс обратное направление: по символу находится якорь. Один генератор и один верификатор по образцу тех семи, что уже написаны и приняты.

Правило поддержания, не требующее процесса: **lane, который трогает сайт, записывает его якорь.** Пустой `owner` перестаёт расти сам.

Пока индекса нет, правило R3 («бриф без `architecture_and_implementation_refs` не выдаётся») исполнимо только на бумаге: подставить якорь неоткуда. Это объясняет, почему `normative_anchor` во всех 751 строке `UNKNOWN`, и почему это не лень исполнителя.

### 2.2. Вынести из ядра лишнее — по владению, не по размеру

Критерий задаёт `I1.2 §2`, а не объём файла. Kernel **владеет** ровно этим:

```
local front-door IPC · principal/session binding · Authority Epochs и fencing ·
Operational Recovery State · Control Reserve · Module/daemon generation routing ·
canonical transition gateway · minimal health и Recovery View ·
startup/drain orchestration · connection to store bridge
```

и **не выполняет**: semantic curation, Dreamer jobs, code graphs, UI, full task planning, broad retrieval.

Порядок работы, один проход по файлу:

```
1. каждый приватный блок сопоставить с десятью пунктами I1.2 §2;
2. не сопоставился             → уходит из ядра;
3. сопоставился и имеет
   исполнимый contract/test/
   context seam (I2.2)         → отдельный крейт через CrateExtractionDecision (I2.23);
4. сопоставился, seam'а нет    → остаётся обычным модулем — I2.2 прямо это разрешает;
5. размер решением не является → I2.16: «never force a Module split by themselves».
```

`CrateExtractionDecision` берётся дословно из `I2.23` — тринадцать полей, включая `first_real_consumer_or_time_bounded_migration_facade` и `counter_risks_merge_or_rejoin_condition`. **Крейт без реального потребителя или test seam отклоняется** (`I2.2`); crate-per-file и crate-per-type целью не являются.

Семейство для вынесенного выбирается из девяти в `I2.23`, а не придумывается.

Мера успеха — **не число строк**, а: у ядра появились типизированные порты вместо приватных вызовов, и `cargo metadata` показывает у вынесенного хотя бы одного потребителя. Если после прохода не выносится ничего — это годный результат с записью, а не провал.

---

---

## 3. Четыре утверждения программы, опровергнутые кодом

**3.1. W4-01 не блокирован W3-04.**
Программа пишет: «подключить `eliot-mcp` как потребителя выделенного в W3-04 `mcp_stdio`». Манифесты говорят иначе:

```
eliot-mcp зависит от: eliot-protocol, eliot-receipts, eliot-security-contracts, serde/schemars/sha2/thiserror
eliot-cli уже несёт:  eliot-protocol, eliot-receipts
```

Ни `eliot-app`, ни `mcp_stdio` в цепочке нет. Крейт спроектирован под инъекцию: трейт `KernelGovernorPort`, null-объект `NoProviderPort`, типизированный `PortFailure`. **Ребро `eliot-cli → eliot-mcp` свободно сегодня, цикла не создаёт, W3-04 не ждёт.**

Честный результат: 21 сайт перестаёт называть блокировщиком существующий крейт и начинает называть настоящий — «не инъектирован admitted Kernel/Governor провайдер». Фронт сдвигается на один шаг и становится виден. Ложная метка «блокирован `eliot-mcp`» — ровно та болезнь, ради которой программа написана.

Объём склейки за манифестом lane измеряет и докладывает. Больше одной сессии — `ContractChallenge`, а не тихое расширение.

**3.2. R1 в форме `todo!()` неприменим, и это правильно.**
`todo!()` паникует. 111 сайтов отказывают типизированно и несут `dependency`/`detail` — это **строго лучший** маркер. Ставить туда `todo!()` значит менять graceful refusal на панику.

R1 амендится:

```
канонический маркер незавершённого := типизированный вариант PlanGap / *::Unimplemented
реестр незавершённого              := swarm/inventory/refusals.csv (принят, генерируем, верифицируем)
docs/UNIMPLEMENTED.md              := биекция по todo!-маркерам; честно пуст, таким и остаётся
```

Ноль правок продукта, ноль правок скриптов. Отмечено отдельно, потому что напрашивается неверное решение: **расширять источник `gen-unimplemented.ps1` строками CSV нельзя** — независимый оракул `verify-unimplemented.ps1` требует, чтобы якорь был реальным нормативным заголовком, а строки CSV несут `normative_anchor: UNKNOWN`. Пара генератор/верификатор сломается. Чинится правило, а не механизм.

**3.3. DoD W3 ссылается на несуществующий гейт.**
Причина глубже отсутствия механизма: **гейта не должно быть в такой форме, он противоречит нормативу.**

`I2.16` (`ELIOT_IMPLEMENTATION.md:3081`) дословно:

> *«STU, byte ratios and historical token averages are planning fallbacks only. They never prove that a Decision Safety Floor fits, never authorize truncation and **never force a Module split by themselves**.»*

И рядом, `:2971`: *«The receipt prevents a planning number from becoming an invisible law.»* Произошло ровно то, от чего эта строка предостерегает.

`A2.3` подтверждает с другой стороны: *«Architecture не задаёт максимальный размер, количество строк, tokens, files, packages или Modules… Split/merge решается по Effective Context Profile, dependency fan-out, build/test cost, failure isolation, replacement cost и Product Pulse»*. W3 резал по размеру при неработающем Pulse — без одного из названных архитектурой входов решения.

**Это нашли раньше меня.** `swarm/results/W0-03-independent.json`, OpenRouter, вердикт `ACCEPTED_WITH_STALE_WORKFLOW_SNAPSHOT`: *«I2.16 defines STU as a deterministic planning fallback and says a numeric band alone records evidence rather than failing or splitting a crate»*. Находка **принята** — и W3 всё равно отработала разрезом по размеру, а DoD W3 до сих пор ссылается на несуществующий гейт. Это первый образец дефекта, который §4.5 закрывает как класс.

Три ошибки прочтения, все исправимы без нового кода:

```
единица   норматив меряет КРЕЙТ (Loaded Crate Slice), гейт мерил ФАЙЛ
семантика норматив даёт review band, гейт объявлял fail
область   норматив считает human-authored source + ordinary tests и прямо исключает
          generated code, golden corpora, vendor source и raw fixtures;
          measure-stu.ps1 считает все tracked .rs подряд
```

Правильный инструмент **уже есть в нормативе** — таблица по классам крейта (`:3009`): primitives/contracts 5–15k при review 25k; control/service implementation 30–50k при review 70k и legacy-band 100k; facade/composition/binary 5–15k при review 25k. Это применимо к `swarm/inventory/modules.json` сегодня: там уже есть per-crate STU.

**Задание:** `measure-stu.ps1` переводится с файлов на крейты по классам из `:3009`, с исключением generated/fixture по `:3005`, и остаётся **review**, а не fail. Универсальный порог «100k на файл» снимается как противоречащий `I2.16`. Ни одного разреза этим не назначается.

**3.4. W3 отчитался разрезом на cells, а произвёл разгрузку контекста. Это разные утверждения.**
`A2.3` требует от micro-module: одна причинная ответственность и один lifecycle owner, **явный public contract**, typed dependency ports, one-way направление, **независимая proof surface**, границы failure/replacement/migration/removal.

Факт на дереве:

```
eliot-installation: 8 модулей объявлены как private `mod X;` (не `pub mod`);
                    7 из 8 содержат ноль #[test]; ни одного нового крейта;
eliot-host:         26 private `mod`, часть объявлена посреди файла (:448, :513, :833, :3958);
DoD W3 гласит:      «каждый новый крейт имеет ModuleTestCapsule с ненулевым числом тестов».
```

W3-02 не создал ни одного крейта, поэтому этот DoD к нему неприменим — и не выполнен. Предъявленное доказательство корректности разреза — совпадение SHA-256 списка тестов до и после — **одновременно является доказательством того, что независимая proof surface не появилась**. Мера успеха и мера провала здесь одно и то же число.

Единственный артефакт W3, отвечающий `A2.3`, — `eliot-host-control-endpoint`: настоящий крейт с тремя собственными тестами.

Это **не обесценивает сделанное**. `A2.3` прямо называет разгрузку контекста текущим реальным драйвером: *«Потребность в меньших causal worksets сейчас следует из ограничений model context»*. Падение `eliot-installation` с 1 351 821 до 569 689 байт — настоящая польза. Требуется одно: называть её тем, что она есть. Записать в результатах W3 `CONTEXT_RELIEF`, а не `cells`, и не наследовать `FunctionalCapabilityCell`/`ModuleTestCapsule` без крейта, публичного контракта и собственных тестов.

---

## 4. Закрывается объявлением, а не работой

**4.1. 514 `Unknown` и 55 безымянных `PlanGap` — не задолженность.** 490 `Unknown` имеют одну причину: runtime-доступность статически неотличима от отсутствия работы. 55 безымянных живут внутри тех же пяти крейтов из §2 и разрешатся побочно при их сборке. WorkItem'а нет и не будет; переклассификация — только попутно.

**4.2. `AdmittedSwarmPlan`.** Решение Sol в `challenges/BOOTSTRAP-PLAN-ADMISSION.md` верно: рукописный admitted-JSON подделал бы восстанавливаемую границу. Ратифицируется, не переоткрывается до W7.

**4.3. W1 закрыт.** Принятый `INTEGRATED_EVIDENCE_ONLY` артефакт не пересматривается. Замечания к форме — `NOTED_NON_BLOCKING`, в файл, без итерации.

**4.4. Периметр.** Linux, hosted, подпись, установка, SCM — вне периметра до W6. Упоминание в брифе — дефект брифа.

**4.5. Принятая находка обязана иметь последствие. `EVIDENCE_ONLY` — потолок доказательства, а не урна.**

Измерено: 18 внешних артефактов, **29 принятых findings**, и за всё время **одно** изменение плана (W1-06 опроверг посылки A2/A3 → ревизия v1.4 §3). Находка по STU принята и не исполнена — §3.3. Так `accepted_findings` превращается в способ согласиться, ничего не поменяв.

С этого момента каждая принятая находка получает ровно одно из трёх, в той же сессии, что и приём:

```
EXECUTED         — план/контракт/код изменён; ссылка на изменение обязательна
DEFERRED_WITH_TRIGGER — не исполняется сейчас; названо условие, при котором исполняется,
                   и волна, в которой оно наступает
DECLINED_WITH_REASON  — не будет исполнено; причина названа существом, а не потолком
                   («EVIDENCE_ONLY» причиной не является)
```

`accepted_findings` без одного из трёх — незакрытая единица работы по §2.6 программы. Луна-B не принимает результат, в котором есть принятая находка без последствия; Sol не объявляет гейт при незакрытых находках предыдущей волны.

Ретроспективно это применяется один раз и списком: 29 существующих findings получают по строке. Это работа на одну сессию по уже написанному — новых артефактов не создаётся.

---

## 5. Обвязка: семь дефектов, производящих ложные показания

Ни один из них не «стабилизируется». Все пять — общий глобальный ресурс без изоляции. Чинятся именно как это, и ни шага больше.

| # | Дефект | Место | Починка |
|---|---|---|---|
| **D0** | **Имя Job Object глобально и не уникально.** `format!("Eliot-{sanitized}-g{generation}")` — ни PID, ни nonce; тесты используют `generation: 1`. Параллельный прогон бинаря сталкивает два теста на одном именованном объекте ядра, `spawn_named` падает. Изолированно тест проходит 3/3 | `crates/eliot-app/src/host_runtime/supervised_process.rs:1730`, ошибка всплывает на `:1081` | уникальный `operation_id` на тест **либо** сериализация группы supervised-process. **Третий раз «стабилизировать» таймингами запрещено** — это уже делали `7a18ece` и `e71d385`, и оно не про тайминги. Отдельно оценить, не та же ли коллизия возможна между поколениями в проде |
| D1 | **Результат уборки становится вердиктом.** Windows держит хэндл — выполненный цикл пишется как FAIL | `crates/eliot-app/tests/first_working_loop.rs:1310` `worktree.cleanup()?;` и `:1367` `runtime.cleanup()?;`, при том что `Drop` (`:1734`) ту же ошибку уже глотает | снять `?` на обоих сайтах; вердикт — **до** уборки, уборка — отдельным наблюдением |
| D2 | `git commit` зависит от ambient-конфига машины | `:1654` | `-c user.name=… -c user.email=… -c commit.gpgsign=false`, `--no-verify` |
| D3 | `nothing to commit` — коммит вызван, когда сталкивать нечего | `:1638` | утверждать непустой staged-diff **до** коммита |
| D4 | Worktree и `git branch -D` — в **живом репозитории**; демон и cargo держат в нём хэндлы | `:1612`, `:1718` | фикстурный репозиторий в TEMP; рабочая копия тестом не мутируется |
| **D6** | **Гейт не видит дальше первого красного крейта.** `verify.ps1:69` запускает `cargo test --locked --workspace` без `--no-fail-fast`; cargo останавливается на первой упавшей цели. Так D0 три дня скрывал D5, а сейчас D5 скрывает весь воркспейс за собой. Пока это не снято, «дерево красное» — единственное, что гейт способен сказать, и стоимость каждой починки узнаётся только после неё | `scripts/verify.ps1:69` | добавить `--no-fail-fast` в шаг `workspace-tests`. Одно слово. `run-isolated-tests.ps1:559` уже так делает — образец в дереве есть |
| **D5** | **Открыт 2026-08-26 после закрытия D0, и растёт: `package_staging` 3 → 12 → 13 красных за трое суток (39 passed / 13 failed на 27.08).** Падают детерминированно, в том числе изолированно — это **не** болезнь D0. Второй вызов `create_destination_file` не возвращает `GenerationExists`. Перезаписи байтов при этом нет: `CREATE_NEW` гарантирует отказ на уровне ОС, и в коде нет ветки, где существующие байты переписываются, — отказывает **типизированная классификация**, не свойство безопасности. Отдельно: escape-hatch `security_fixture_unavailable` (`:4458`) стоит только на **первом** вызове, второй идёт голым `assert!` — на машине без привилегированного токена тест падает там, где автор уже предусмотрел пропуск | `crates/kernel/eliot-platform-windows/src/package_staging.rs:5482`, классификатор `:4458`, путь `:3883–3931` | владельцу `eliot-platform-windows`: установить, что возвращает второй вызов, и либо починить классификацию, либо симметрично закрыть escape-hatch. Свойство «create-new не перезаписывает» под сомнение **не** ставится |

**D5 не регрессия от свежих коммитов.** `package_staging.rs` последний раз правился в `2616470` — до baseline `d5f0fdc`. На baseline `cargo test --workspace` обрывался на `eliot-app` из-за D0, поэтому `eliot-platform-windows` вообще не запускался. **Эти три теста были красными всё время и были скрыты за D0.**

Это первый случай, когда гейт видит так далеко, и ровно то, ради чего D0 чинился: гейт, научившийся краснеть, показал следующее красное. Ожидать зелёного дерева сразу после D0 было неверно — верно ожидать, что каждое снятое ложное показание открывает настоящее.

D0 блокирует `verify.ps1`, поэтому идёт первым. D1: падения 8, 9, 11 произошли в уборке дословно, 10 и 14 — `os error 32` из той же фазы; пять из четырнадцати снимаются снятием двух вопросительных знаков.

**Pulse #1 — термометр, а не гейт.** Он идёт через контур-донор A и ассертит `ELIOT_DISABLE_REAL_PROVIDER=1` (`:629`); программа сама пишет, что он «не доказывает D1-ingress». **Требование «20 прогонов подряд» из черновика этой директивы изъято** — это была новая работа на обвязке вокруг несобранного продукта, и она противоречила §0. Проходимость записывается по тем запускам, которые случатся сами.

W2 закрывается одним из двух способов, и выбор пишется в `swarm/gates/W2.md`: либо Pulse ручной и W2-04 не закрыт — так и записать; либо `#[ignore]` снят вместе с провижинингом runtime в `verify.ps1`. Сейчас действует необъявленное третье состояние: тест назван Pulse'ом, гейтом не является, и это нигде не написано. Заявление фиксируется в его границах: контур-донор, реальный провайдер выключен.

**Дрейф W2 прекращается.** `swarm/briefs/W2-01.json` несёт `max_model_calls: 0`, `max_tool_calls: 0`, verifier `source-checked-no-run`. Это работа по форме контракта, а не `OperationalSpineProofBrief` по `I17.6`. W2-артефакт с нулевым бюджетом инструментов отклоняется.

---

## 6. CI и потолок доказательства

Квота GitHub исчерпана. Гонять на раннере час то, что локально идёт 16 минут, запрещено прямо. Но заметить нужно точно, **что** покупал hosted-прогон: не скорость и не значок, а независимость от ambient-состояния одной машины. Это воспроизводится локально:

```
CLEAN_CLONE_ATTESTED :=
  клон закоммиченной ревизии в изолированный корень вне рабочей копии
  + свой CARGO_TARGET_DIR + свой TEMP + без ProgramData/демона/тёплого target
  + pwsh -NoProfile -File scripts/verify.ps1 → exit 0
  + записаны: ревизия, дайджест дерева, время, вывод одиннадцати шагов
```

Один прогон **на гейт волны**, не на WorkItem; цена — одна холодная сборка. Luna-C повторяет из своего клона: это и есть независимость, которой добивались от раннера. Внутри lane достаточно тёплого прогона по своему крейту. **До починки D0 такой прогон невозможен — он упадёт на `workspace-tests`.**

Две правки, по строке:

- `.github/workflows/ci.yml:70` — `-ne 125` → `-ne 126` (текст throw на `:71`). Воркспейс уже 126 после `eliot-host-control-endpoint`; сейчас workflow лжёт и упадёт на первом push. Крейт добавляется — число правится тем же коммитом.
- Блок `on:` (`:7–13`) сводится к `workflow_dispatch`. Тогда `[skip ci]` не нужен как дисциплина и квота не тратится случайно; после этого `[skip ci]` в сообщениях не ставится.

---

## 7. Роли: маршрутизация была инвертирована

Измерено по артефактам, а не по плану:

```
внешних артефактов                18
из них agent_profile              plan — все 18
opencode-go/ox-alpha-free         10
openrouter/stealth/ox-alpha        7
кода от внешних агентов в дереве   0
provider_id в результатах W1-*/W3-*  отсутствует — их делали лейны Root/Sol
```

Программа §7.4 назначает Ox OpenCode механические единицы: генераторы, инвентаризацию, вынос тестов, split по готовым границам. Фактически Ox работали в `plan`-профиле и производили суждение, а семь генераторов, восемь верификаторов и разрезы W3 сделали дорогие лейны.

**Дешёвые агенты делали дорогую работу, дорогие — дешёвую.** Это ровно инверсия матрицы назначения, и это объясняет и стоимость, и четыре брака из восемнадцати (`MALFORMED_EXTERNAL_RESPONSE_RETRYING`, `REJECTED_PRE_CORRECTION_CANDIDATE`, `REJECTED_WITH_EVIDENCE`, `REJECTED_V1_ORACLE_FIXED_IN_V2`), после которых работу переделывали внутри.

**Ox OpenCode больше не запускаются в `plan`-профиле.** Профиль `plan` — это native Luna и OpenRouter. Ox получают write-lane с точным списком файлов, иначе не запускаются вовсе.

| Кто | Точное задание сейчас | Proof | Чего не делает |
|---|---|---|---|
| **Sol** | Провайдер №1 по R13: серверная `eliot.agent-bridge.activate` в Kernel. Затем инъекционные порты §2. Шесть решений Приложения — записью. Каждый бриф несёт границу утверждения: что доказывается **и при каких выключенных провайдерах** | запись решения + бриф с якорем | не выдаёт брифов вне §2/§2.1/§5; не открывает Mechanism Review по принятому; не объявляет гейт при незакрытых находках (§4.5) |
| **Luna-A** | Коммит существующего дерева **до** любой правки. Затем ребро `eliot-cli → eliot-mcp`, затем `eliot-controlboard`. Один `CLEAN_CLONE_ATTESTED` на гейт — после D0 | коммит; счётчик сменивших блокировщика сайтов | не мержит без disposition; результат уборки lane на disposition не влияет |
| **Luna-B** | Раздача write-lane по таблице ниже. Отклоняет бриф без границы утверждения, бриф с нулевым бюджетом инструментов и результат с принятой находкой без последствия (§4.5) | реестр lane | не передаёт в работу ни одного нового split |
| **Luna-C** | Проверка из **своего чистого клона**. Подсовывает каждому гейту заведомо ломающую мутацию: гейт, её не поймавший, объявляется несуществующим | mutation-fixture отчёт | не участвует в исполнении волны |
| **Ox-1 OpenCode** | **D0**: `supervised_process.rs:1730` — уникальность имени Job Object либо сериализация группы. Файл один | `cargo test --locked --workspace` зелёный | таймингами не «стабилизирует» |
| **Ox-2 OpenCode** | **D1–D4**: `crates/eliot-app/tests/first_working_loop.rs`, строки 1310, 1367, 1638, 1654, 1612, 1718. Файл один | тест перестаёт падать в уборке | не трогает продуктовый код |
| **Ox-3 OpenCode** | **`ci.yml`** `:70` 125→126 и `on:`→`workflow_dispatch`; **`PROJECT_MAP.md`** 125→126. Две строки и одна | `cargo metadata` = ci.yml | ничего сверх чисел |
| **Ox-4 OpenCode** | **§3.3**: `measure-stu.ps1` с файлов на крейты по классам `I2.16:3009`, исключение generated/fixture по `:3005`, семантика review | новый отчёт против `modules.json` | не назначает ни одного разреза |
| **Ox-5 OpenCode** | **§2.1**: генератор и верификатор индекса якорь↔символ по образцу семи принятых | `owner` в `conformance.toml` заполнен со стороны кода | не выдумывает якоря там, где их нет — пишет `UNKNOWN` |
| **Ox OpenRouter ×2** | Опровергать §2 и §4.5: «блокировщик назван неверно», «крейт не готов к потреблению», «эта находка принята и не исполнена». Профиль `plan` — их штатный | challenge с evidence | не видит результатов siblings до собственной submission |
| **Antigravity** | Read-only. Перегенерация инвентарей **один раз, после коммита**. Ретроспективный проход §4.5 по 29 существующим findings | перегенерированные артефакты + 29 строк | ничего не пишет в основную ветку |

Пять write-lane не пересекаются по файлам, поэтому идут параллельно и не требуют потолка WIP из §2.5 программы: он ограничивает **mutating lane на общий scope**, а здесь scope у каждого свой.

---

## 8. Запрещено

1. **`git reset --hard` на `main` и работа на `main` с грязным деревом.** Один WorkItem — одна ветка `work/<id>`, один worktree, один писатель (программа §2.4). `2026-08-27 00:32:59` — `reset: moving to HEAD` уничтожил ~21 700 незакоммиченных строк; тремя коммитами вернулось ~3 900, то есть **≈18 %**. Дисциплина веток была снята 23 августа, и вместе с ней страховка: на тематической ветке reset стоит одну задачу, на грязном `main` — всё сразу.
2. Hosted-прогоны до восстановления квоты; `[skip ci]` после §6.
3. Стабилизировать обвязку вокруг несобранного продукта: кампании прогонов, retry-петли, «зелёный с N-й попытки», третья попытка «стабилизировать» supervised-process таймингами.
4. Открывать W3-05 и любой новый разрез, не снимающий ни одного сайта из §2.
5. Брать потребителя раньше его провайдера по R13.
6. Ставить `todo!()` туда, где сейчас типизированный отказ.
7. Пересматривать принятое в W0/W1 и решение по cutover.
8. Мутировать живую рабочую копию из теста.
9. Оставлять единицу работы без disposition.
10. Заводить новую `schema_version` для результата. Их уже десять и ни одной с владельцем в Rust. **Это запрет на новые версии конверта, а не на результаты:** результат пишется в существующем `eliot.bootstrap-work-result.v1` (§0.1 Д-1).
11. Закрывать единицу работы без структурного результата в `swarm/results/` и без идентичности исполнителя (§0.1 Д-1, Д-2).
12. Наследовать `FunctionalCapabilityCell` или `ModuleTestCapsule` модулю без крейта, публичного контракта и собственных тестов (§3.4).
13. Принимать находку без последствия по §4.5. «Принято как `EVIDENCE_ONLY`» — потолок доказательства, а не disposition.
14. Запускать Ox OpenCode в профиле `plan`. Нет точного списка файлов — lane не выдаётся.
15. Назначать разрез по числу STU: `I2.16` — «never force a Module split by themselves».
16. **Класть в `bins/eliot-kernel/src/lib.rs` то, чем ядро не владеет по `I1.2 §2`.** Новая способность проходит §2.2: сопоставление с десятью пунктами, затем `CrateExtractionDecision` по `I2.23`, если есть seam.
17. Реализовывать заново то, что уже есть в workspace. Перед новым кодом — `cargo metadata`: `eliot-process` с 24 потребителями переписан внутри ядра именно потому, что этого не сделали.

---

## 9. Порядок

```
шаг  владелец      что                                             блокирует
──────────────────────────────────────────────────────────────────────────────
 1   Luna-A        коммит дерева как есть: 82 M + 56 ??            всё
                   красное дерево в коммите честнее его потери
 2   Ox-1          D0 — ЗАКРЫТО 2026-08-26, проверено                —
 2b  владелец      D5 — открыт вместо него; см. §5                   любой гейт
     platform-win
 3   Ox-3          ci.yml 125→126, on:→workflow_dispatch,           —
                   PROJECT_MAP 125→126
 4   Ox-2          D1, затем D2–D4. Без кампании прогонов           закрытие W2
 5   Sol           swarm/gates/W2.md: статус Pulse и граница        закрытие W2
                   утверждения — явно
 6   Luna-A        ребро eliot-cli → eliot-mcp; замерить,           —
                   сколько из 21 сайта сменили блокировщика
 7   Ox-5          §2.1 индекс якорь↔символ                         R3
 8   Sol           провайдер №1 по R13                              счётчик §1
```

Шаги 1–4 идут параллельно: пересечения по файлам нет. Шаги 3, 4, 7 не ждут шага 2 — его ждёт только гейт.

Параллельно и целиком записью, без кода: Ox-4 — §3.3; Antigravity — ретроспектива §4.5 по 29 findings; Sol — шесть решений Приложения.

Шаги 1 и 3 — минуты. Шаг 2 — условие существования любого гейта. Шаги 5–6 делают фронт видимым. Шаг 7 — единственный, который окупается на каждом следующем брифе. Шаг 8 — собственно продукт.

---

## 10. DoD директивы

```
дерево закоммичено, рабочая копия чистая;
cargo test --locked --workspace → зелёный (D0 закрыт 2026-08-26; остаётся D5);
ci.yml не противоречит cargo metadata; hosted не запускался;
статус Pulse #1 и граница его утверждения записаны явно;
ребро eliot-cli → eliot-mcp существует, измерено, сколько сайтов сменили блокировщика;
сайтов с блокировщиком-существующим-крейтом: 0 (было 29) — ЗАКРЫТО 2026-08-26;
сайтов с блокировщиком-непровайдером: падает после R13 шага 1;
docs/conformance.toml: колонка owner заполнена со стороны кода, UNKNOWN < 55;
все 29 существующих accepted_findings имеют disposition по §4.5;
measure-stu меряет крейты по классам I2.16, семантика review, generated исключены;
шесть противоречий Приложения получили запись-решение;
ни один Ox не запускался в профиле plan;
ни один новый WorkItem вне §2, §2.1 и §5 не открывался.
```

Последние две строки — главные. Первая говорит, что продукт собирается. Вторая — что директива не стала новой работой.

---

## Приложение К. Сверка с якорями Architecture

Проверено 2026-08-27 по 54 якорям `ARCH-*`, имеющим формулировку в `ELIOT_ARCHITECTURE.md`. **Механически проверяемы 11.** Остальные 43 описывают поведение во время работы и статически не проверяются — их закрывает только живая conformance map `A6.7`, то есть §2.1. Отсутствие проверки здесь **не** означает соответствия.

| Якорь | Требование | Замер | Вердикт |
|---|---|---|---|
| `ARCH-MOD-01` | Small living Kernel | 15 976 строк, 21 pub, 2 модуля, 39 тестов | **нарушен** → §2.2 |
| `ARCH-DEV-01` | Working system before broad hardening | первый пункт spine `A0.8` — «один реальный agent bridge» — возвращает безусловный `Err`; W0 и W3 (линты, STU, проекция, разрезы) выполнены раньше | **нарушен** → §2 |
| `ARCH-SEC-02` | One canonical transition path | `commit_canonical`: **1 определение, 0 вызовов** | **не действует**: путь объявлен и не используется |
| `ARCH-SWM-02` | Координация durable, idempotent, epoch-fenced; **process не единственный носитель результата** | `reset --hard` 27.08 уничтожил ~21 700 строк; носителем был рабочий каталог | **нарушен** → §8.1 |
| `ARCH-SKL-01` | Instructions are intent-dense. «Мало слов, одно значение» | 10 документов инструкций в `swarm/decisions/`, **0 помечены `superseded`**; суммарная поверхность ≈1.35 МБ; эта директива — 503 строки | **нарушен, в том числе мной** → §К.1 |
| `ARCH-CTX-01` | **Decision sufficiency before size optimization** | W3 резала по STU; `I2.16` запрещает размерный разрез независимо | **нарушен** → §3.3, уже снято |
| `ARCH-ORD-01` | Единственный owner конфликтующего state | работа на `main` с грязным деревом с 23.08, ветка `work/<id>` не заводится | **нарушен** → §8.1 |
| `ARCH-RES-04` | Degradation is visible and local; обещания уменьшаются раньше, чем неполное выдаётся за полное | `UNIMPLEMENTED.md` = `HONEST_EMPTY` при 119 известных `Unimplemented`; 525 отказов `Unknown` | **нарушен по существу**, формально честен → §3.2 |
| `ARCH-RES-04` (2) | то же — **деградация не локальна в самом гейте** | `verify.ps1:69` — `cargo test --locked --workspace` **без `--no-fail-fast`**: прогон 27.08 остановился на `eliot-platform-windows` (206 passed / 13 failed), и **все цели после него не выполнялись вовсе**. Гейт сообщает первую проблему, а не состояние | **нарушен** → §5 D6 |
| `ARCH-PORT-01` | Органы и contours заменяемы через capability/conformance | 37 библиотек с нулём потребителей; порты не подключены | **не действует** → §2 |
| `ARCH-SELF-01` | ELIOT знает своё назначение и состояние | conformance map: `owner UNKNOWN` 54/58, `support UNKNOWN` 58/58 | **не действует** → §2.1 |
| `ARCH-FIN-01` | Completion is proof-bearing | `DONE_VERIFIED` — 30 вхождений в коде; достижимость без proof **не проверена** | требует проверки |

### К.1. Нарушение, которое внёс этот документ

`ARCH-SKL-01` требует: мало слов, одно значение, ясный следующий шаг. `A:1668` требует, чтобы инструкции несли `status: active | superseded | expired | conflicting` и **не накапливались молча**.

Фактически действуют одновременно: программа v1.3, `ROOT-DIRECTIVE-v1.4`, эта директива, Приложение P, решение R13 и шесть остальных документов в `swarm/decisions/`. Ни один не помечен. Исполнитель обязан сам вычислить, что действует.

**Обязательство:** после закрытия D5 и §2.2 эта директива сокращается до действующего задания, а разбор уходит в `superseded`-носитель. Пока этого нет, `ARCH-SKL-01` остаётся нарушенным, и я это фиксирую, а не обхожу.

### К.2. Чего эта сверка не покрывает

43 якоря из 54 — про поведение: `ARCH-MEM-*`, `ARCH-EPI-*`, `ARCH-UND-*`, `ARCH-DRM-*`, `ARCH-ATTN-*`, `ARCH-LEARN-01`, `ARCH-PRIV-01`, `ARCH-RES-01/02/03/05`, `ARCH-WDG-*`, `ARCH-HUM-01` и прочие. Их нельзя проверить чтением дерева — нужен работающий проход и conformance map. Это и есть содержательная причина, по которой §2.1 стоит выше по приоритету, чем кажется: **без индекса 43 из 54 инвариантов непроверяемы в принципе.**

---

## Приложение. Противоречия, требующие явного решения Sol

Каждое проверено на дереве. Ни одно не закрывается работой — все закрываются записью.

**П1. Гейт W0 закрыт на невыполненном собственном proof.** `ROOT-DIRECTIVE-v1.4` задаёт для `W0-09` proof: *«Зелёный hosted receipt на main»*. Гейт закрыт с `HOSTED_NOT_RUN`. Отклонение записано честно — owner, причина (квота), риск, rollback. Но rollback гласит *«restore the hosted-green W0-09 criterion»*, а квоты нет. **Recoverable Deviation, условие пересмотра которого не может наступить, — не recoverable.** Переобъявить как постоянный пониженный потолок с `CLEAN_CLONE_ATTESTED` вместо hosted; иначе каждый следующий гейт наследует отклонение, которое все считают временным.

**П2. Основание программы отсутствует в дереве.** `docs/audit/` пуст. `CODEX_SWARM_FAILURE_AUDIT_2026-08-24.md` и `ELIOT_CODEX_DEEP_ROOT_CAUSE_AUDIT_20260824.md` — документы, на которые программа ссылается как на своё основание, — недоступны исполнителям. Это прямое нарушение R3 самой программой. Внести или снять ссылку.

**П3. §2.2 программы не исполнена и отменена молча.** Обещание: брифы и результаты сериализуются в типах `eliot-swarm`, «сирота получает первого реального потребителя», «swarm говорит на языке ELIOT с первого дня». Факт: `eliot-swarm` — **ноль потребителей**; в `swarm/results/` **10 разных `schema_version`** (`eliot-external-audit-result-v1` ×15, `eliot.bootstrap-work-result.v1` ×14, `eliot-work-result-v1/v2`, `…-v4` и прочие); **ни один не имеет владельца в Rust** — все собираются literal-хэштейблами внутри семи PowerShell-генераторов. При этом `eliot-bootstrap` содержит настоящие типы (`NormativePair`, `CurrentSystemEvidenceSnapshot`, `BootstrapRuleCatalogue`), которыми swarm не пользуется.

Следствия, которые надо признать вслух: заявленный dogfood не является dogfood; рост версий схем (`v1→v4`) — это и есть механизм переделок, ради которого v1.4 пришлось законодательно объявить «текущие попытки последние»; **область W7 больше, чем записано** — там не «замена файлового транспорта», а перенос семантики, которой в типах ELIOT никогда не было.

**П4. Порядок волн против `ARCH-DEV-01`.** `A0.8` определяет первый полезный vertical spine, и его первый пункт — *«один реальный agent bridge»*. `bins/eliot-agent-bridge` возвращает безусловный `Err(KernelAdmissionRequired)` и выходит с кодом 69. `ARCH-DEV-01`: *«Working system before broad hardening. Сначала создаётся реальный end-to-end cognitive loop; тесты и глубина добавляются по наблюдаемым failure modes»*. Программа применяет это правило явно, чтобы отложить W6, — и не применяет к себе: W0 (lint-политика, STU-измеритель, проекция норматива, verify-профиль) и W3 (разрезы) суть broad hardening, выполненные до существования spine. Это же объясняет, почему §2 настоящей директивы ставит фронтом провайдер №1, а не следующий разрез.

**П5. Conformance недоказуем по построению.** `A0.8`: *«Соответствие подтверждается живой conformance map A6.7»*. `A6.7` требует связи `ARCH-*` с owner, mechanism, failure behavior и observable status. `docs/conformance.toml`: 58 требований, `owner` UNKNOWN в 55, `support` UNKNOWN во всех 58, списки invalidation пусты. Форма карты есть, содержания нет. Закрывается §2.1, другого пути нет.

**П6. Маршрутные документы разошлись с деревом.** `docs/PROJECT_MAP.md` заявляет 125 workspace members, `.github/workflows/ci.yml:70` требует ровно 125, `cargo metadata` даёт **126**. Тот же класс дрейфа, что и в П5: артефакт, который никто не пересчитывает при изменении. Правило из §6 распространяется на PROJECT_MAP.
