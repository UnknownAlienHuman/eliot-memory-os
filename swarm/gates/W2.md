# W2 gate — BOUNDARY_RECORDED / W2-04_OPEN

Observed source revision: `2548d572f9e99807428b5463ded5ef7dda020266`.
Controlling decision: `swarm/decisions/ROOT-DIRECTIVE-v1.5.md`, §5 and §9,
step 5.

## Root decision

Product Pulse #1 remains a **manual** local test. The six tests in
`crates/eliot-app/tests/first_working_loop.rs` remain `#[ignore]`; no runtime
provisioning for them was added to `scripts/verify.ps1`. Therefore `W2-04` is
`OPEN`, W2 is not accepted as Product Proof, and no hosted or release claim is
made.

This is the first of the two states allowed by ROOT-DIRECTIVE v1.5. It replaces
the previous undeclared third state in which the test was called a Pulse but
was neither an automated gate nor explicitly manual.

| Item | State | Boundary |
|---|---|---|
| Pulse #1 execution | `MANUAL_NOT_RUN_AT_THIS_REVISION` | An operator may run the ignored local test only after provisioning its documented runtime prerequisites. This gate update did not run it. |
| `W2-01` | `REJECTED_ZERO_TOOL_BUDGET` | The current brief has `max_model_calls: 0`, `max_tool_calls: 0`, and verifier hash `source-checked-no-run`; it is not an `I17.6` operational-spine proof. |
| `W2-02` | `NOT_ADMITTED_BY_THIS_GATE` | Static startup/readiness source evidence is not a clean-machine runtime receipt. |
| `W2-03` | `NOT_PROVEN` | No accepted natural memory-discovery, restart/resume, changed-action, and terminal-verifier receipt chain exists at this revision. |
| `W2-04` | `OPEN_MANUAL` | The Pulse is still ignored and is not provisioned by the local aggregate verifier. |

## Claim boundary

The existing Pulse exercises donor contour A. It requires
`ELIOT_DISABLE_REAL_PROVIDER=1`, so even a future manual pass of this exact test
may demonstrate only the bounded donor/harness route that the test asserts. It
does **not** prove:

- real-provider execution;
- D1 ingress or `eliot.agent-bridge.activate` server authority;
- a production caller of `commit_canonical`;
- natural causal benefit from remembered material rather than harness-authored
  actions or supplied identifiers;
- clean-machine repeatability, hosted verification, release readiness, or
  installed-product readiness.

No Product Pulse result, `DONE_VERIFIED`, terminal update, or completion receipt
is created by this record.

## Review condition

`W2-04` may change from `OPEN_MANUAL` only in the same accepted change that
removes the relevant `#[ignore]`, provisions the required local runtime through
`scripts/verify.ps1`, and produces the exact admitted receipt. Until then, the
manual/open state and the claim boundary above remain authoritative.
