# W0 gate — OPEN

Baseline: `d40a0a1e03f8a5afabffb669f4a6acfb6ea848f0`.

| Acceptance item | State | Current evidence |
|---|---|---|
| Workspace clippy has zero warnings | `FAILED` | The first effective-policy run exposed 244 focused `eliot-platform-windows` blockers (1 production, 243 test-target). Bounded source repairs are in progress; no lint suppression is accepted. |
| CI covers the workspace and cargo-deny | `IMPLEMENTED_NOT_EXECUTED` | `.github/workflows/ci.yml` now checks 125/6 topology, installs pinned `cargo-deny 0.20.2`, records its identity, and delegates to `scripts/verify.ps1`. No hosted CI receipt exists yet. |
| Cross-platform verify parity | `PARTIAL` | Both entrypoints delegate to the same profile, but the independent W0-04 audit found parser-edge divergences between the standalone normative PowerShell and Bash verifiers. |
| Normative projection hashes | `VERIFIED_LOCAL` | Four projected files match the external canonical files byte-for-byte; both standalone verifiers returned `NORMATIVE_VERIFY: PASS` on current bytes. Mutated-fixture parity remains untested. |
| Generated unimplemented registry | `CONTRACT_CHALLENGE` | Current frozen census is zero `todo!` and zero `unimplemented!`; the historical 1371 claim and a required nonempty registry are unreproducible. W0-01 needs a truthful empty-state/generator contract. |
| Bootstrap brief command | `CONTRACT_CHALLENGE` | The unsound admission candidate was removed. CLI remains `PlanGap` until provider-owned gap catalogue, I17.2 drafts, explicit roots, response/exit contracts, and candidate write effect are owned. |

No W0 completion claim is authorized while any row is not verified. W2 remains
blocked. Luna-C has not run the independent gate.
