# W0 gate — CLOSED_LOCAL_RECOVERABLE_DEVIATION

Original bootstrap baseline: `d40a0a1e03f8a5afabffb669f4a6acfb6ea848f0`.
Product source baseline: `e71d385e6f89d9f1a476bfe37a249c301a0be1c3`.
The closure overlay is exactly `swarm/results/W0-09.json` plus this gate record.

| Acceptance item | State | Current evidence |
|---|---|---|
| Workspace Clippy has zero warnings | `VERIFIED_LOCAL` | Root quiescent `pwsh -NoProfile -File scripts/verify.ps1` completed with exit 0; all eleven profile steps passed, including `workspace-clippy`. The accepted FFI lint-inheritance deviation preserves the exact effective Clippy policy. |
| CI covers the workspace and cargo-deny | `HOSTED_NOT_RUN` | `.github/workflows/ci.yml` remains the hosted verifier, but the Human decision owner explicitly restricted this recovery to local test execution. No green hosted receipt exists or is claimed. |
| Cross-platform verify parity | `RESOLVED_BY_PROGRAM_REVISION` | ROOT-DIRECTIVE-v1.4 makes W0 a Windows-only product gate: `scripts/verify.{sh,ps1}` expose one `eliot-verify-v3` profile through one PowerShell owner. Native Linux is not W0 DoD; no Linux receipt is claimed. |
| Normative projection hashes | `VERIFIED_LOCAL` | Architecture, Implementation, INDEX, and README match the external canonical files byte-for-byte; independent generator/verifier parity and mutation fixtures pass. |
| Generated unimplemented registry | `VERIFIED_HONEST_EMPTY` | The historical nonempty premise is unreproducible. The accepted challenge uses a generated, independently verified `HONEST_EMPTY` registry with zero structured production markers and keeps `Unimplemented` distinct from `Unavailable`. |
| Bootstrap brief command | `VERIFIED_FOCUSED_V2` | The CLI route is locally admitted as a candidate effect. The provider owns its GAP catalogue; drafts are append-only/content-addressed; `NormativePair` v2 contains only Architecture and Implementation; focused strict Clippy and tests pass. No Kernel/MCP or canonical authority is claimed. |
| W0-07 watchdog dependency feature boundary | `VERIFIED_LOCAL` | `eliot-platform-windows.workspace=true` is a normal dependency and dev-dependency with `features=["test-support"]` only on the dev-dependency path. Independent normal-tree proof has four `eliot-platform-windows` entries and zero `test-support` annotations; focused watchdog strict Clippy and 45 tests pass. See `swarm/results/W0-07.json`. |
| W0-08 aggregate Windows verifier | `VERIFIED_LOCAL` | Authorized recoverable cleanup preceded a quiescent root run. `pwsh -NoProfile -File scripts/verify.ps1` (profile `eliot-verify-v3`) exited 0 in 940.5s at snapshot `2026-08-24T21:29:37.0912001-04:00`, with all eleven steps PASS. Independent Luna-C ran the exact command, ACCEPT, exit 0 in 994.4s, with the same eleven PASS steps. Raw full logs were not persisted. See `swarm/results/W0-08.json`. |
| W0-09 local-only closure | `CLOSED_LOCAL_RECOVERABLE_DEVIATION` | The exact local `pwsh -NoProfile -File scripts/verify.ps1` / `eliot-verify-v3` aggregate passes on the product baseline plus this two-file closure overlay. `swarm/results/W0-09.json` records the A0.6 owner, reason, affected scope, hard-boundary checks, review condition, rollback, outcome, and lower proof ceiling. |
| W0-10 normative compiler names | `VERIFIED_LOCAL` | `BootstrapBriefCompiler` and `CurrentSystemEvidenceCompiler` now bind the normative anchors and production consumers; independent searches exited 0 with 8 and 13 hits, respectively, and old free names are absent. Focused bootstrap tests and strict Clippy pass. See `swarm/results/W0-10.json`. |

W0 is closed only for the bounded local recovery sequence under the active
`W0-09-LOCAL-ONLY` A0.6 Recoverable Deviation. The hosted proof surface was not
run and is not claimed. This state is not `CLOSED_HOSTED`, `VERIFIED_COMPLETE`,
release acceptance, or Product Proof.

The historical W0-08 receipt remains evidence for its recorded source. W0-09
adds a fresh exact local aggregate over the current product baseline and the
two-file closure overlay. Root accepts this lower proof ceiling for downstream
local recovery work. Any hosted, release, or cutover claim remains blocked until
its own required evidence is explicitly admitted. No native Linux receipt is
required or claimed under ROOT-DIRECTIVE-v1.4.
