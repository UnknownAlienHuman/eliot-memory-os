# Work unit F-CONF

Owning issue: #744

Exclusive implementation scope is `eliot-conformance-contracts`, its exact root workspace/dependency lines, Cargo-required lockfile delta, and mechanically required generated package-index paths.

D-BUILD starts after this unit. Remove this reservation file before the implementation PR is marked ready.
