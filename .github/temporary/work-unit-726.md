# Work unit F-PIPE

Owning issue: #726

Exclusive implementation scope is the canonical pipe-name module/test plus the exact legacy literal consumer files listed in #726. Freeze any additional same-literal path before editing it.

F-DENY #710 follows this unit and must exclude these paths. Remove this reservation file before the implementation PR is marked ready.
