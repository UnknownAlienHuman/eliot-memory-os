# Work unit F-APPP

Owning issue: #708

Exclusive implementation scope is the nine exact `crates/eliot-types/src/*.rs` files and the Appendix P regression test listed in #708. `ul/cue.rs` is excluded.

Remove this reservation file before the implementation PR is marked ready.
