name: Manual Integration Workflow Fixture write permissions (must be rejected)
# WORK_UNIT_CASE: 915/5 negative fixture. Broad write-all permissions are
# forbidden; effective permissions must remain read-only.
on:
  workflow_dispatch:

permissions: write-all

jobs:
  integration-manual:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false
          fetch-depth: 0
      - run: echo "must never validate"
