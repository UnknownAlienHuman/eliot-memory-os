name: Fixture reusable bypass (must be rejected)
# WORK_UNIT_CASE: 750/4 negative fixture. workflow_call, reusable-workflow
# calls, and dispatch passthrough that selects a weaker profile are forbidden.
on:
  workflow_call:
    inputs:
      profile:
        required: false
        type: string

permissions:
  contents: read

jobs:
  delegated:
    uses: some-org/some-repo/.github/workflows/reusable-verify.yml@main
    with:
      profile: Quick
