{"base":"origin/main ef38df8b","branch":"issue/1788-bootstrap-discovery-W1","checks":[{"caller":"crates/governor/eliot-workscope/src/scanner.rs::run_bootstrap_discovery","command":"cargo check --locked --offline -p eliot-workscope --all-targets (exit 0, compile only; no test execution per owner order)","id":"A1","impl":"A1 acceptance routes through the non-test production caller run_bootstrap_discovery (keyed lease + observation-bound evidence + durable receipt write + real verifier_candidates). Runtime behavior not executed in this fixup.","reason":"runtime-proof-missing: owner order forbids test execution; acceptance runtime demonstration deferred to a later phase with authority to run tests","status":"TEST-PHASE"},{"caller":"crates/governor/eliot-workscope/src/scanner.rs::run_bootstrap_discovery","command":"cargo check --locked --offline -p eliot-workscope --all-targets (exit 0, compile only; no test execution per owner order)","id":"A2","impl":"A2 acceptance routes through the non-test production caller run_bootstrap_discovery with no boundary (SCAN_PRIVACY_BOUNDARY_REQUIRED, no persistence). Runtime behavior not executed in this fixup.","reason":"runtime-proof-missing: owner order forbids test execution; acceptance runtime demonstration deferred to a later phase with authority to run tests","status":"TEST-PHASE"},{"caller":"crates/governor/eliot-workscope/src/scanner.rs::BootstrapScanner::scan via crates/governor/eliot-workscope/src/lib.rs::DiscoveryReadLease::key_matches","command":"cargo check --locked --offline -p eliot-workscope --all-targets (exit 0) + cargo clippy --locked --offline -p eliot-workscope --lib --no-deps -- -D warnings (zero warnings)","id":"W1","impl":"DiscoveryReadLease retains proposer/session/host/fs-identity; issue_discovery_lease populates them; scan calls lease.key_matches and fails closed without consumption on mismatch","status":"MET"},{"caller":"crates/governor/eliot-workscope/src/scanner.rs::deny_forbidden_operations via crate::authorize_operation","command":"cargo check --locked --offline -p eliot-workscope --all-targets (exit 0) + cargo clippy --locked --offline -p eliot-workscope --lib --no-deps -- -D warnings (zero warnings)","id":"W2","impl":"deny_forbidden_operations invokes authorize_operation for all 5 DiscoveryOperations and fails closed if ever admitted; scan runs it before charging","status":"MET"},{"caller":"crates/governor/eliot-workscope/src/scanner.rs::run_bootstrap_discovery + BootstrapScanner::scan","command":"cargo check --locked --offline -p eliot-workscope --all-targets (exit 0) + cargo clippy --locked --offline -p eliot-workscope --lib --no-deps -- -D warnings (zero warnings)","id":"W3","impl":"attested_reads must equal populated classes exactly; evidence bound to live ObservedScopeResources (root/fs-identity/VCS generation); scanner performs no I/O itself per governor boundary","status":"MET"},{"caller":"crates/governor/eliot-workscope/src/scanner.rs::BootstrapScanEvidence::validate + ScanDisclosureReceipt::validate","command":"cargo check --locked --offline -p eliot-workscope --all-targets (exit 0) + cargo clippy --locked --offline -p eliot-workscope --lib --no-deps -- -D warnings (zero warnings)","id":"W4","impl":"no excluded-class intake fields (compile-time); redaction identities must be 64-hex digests at intake and receipt validation, not copied caller strings","status":"MET"},{"caller":"crates/governor/eliot-workscope/src/scanner.rs::BootstrapScanner::scan via crate::ScanDisclosureStore","command":"cargo check --locked --offline -p eliot-workscope --all-targets (exit 0) + cargo clippy --locked --offline -p eliot-workscope --lib --no-deps -- -D warnings (zero warnings)","id":"W5","impl":"scan durably writes receipt through ScanDisclosureStore and reports Completed only with bound ScanReceiptHandle; write impl stays with store owner per governor boundary","status":"MET"},{"caller":"crates/governor/eliot-workscope/src/scanner.rs::run_bootstrap_discovery supplies policy.verifier_refs","command":"cargo check --locked --offline -p eliot-workscope --all-targets (exit 0) + cargo clippy --locked --offline -p eliot-workscope --lib --no-deps -- -D warnings (zero warnings)","id":"W6","impl":"profile verifier_candidates come from owner-registered verifier refs; empty records a verifiers capability gap","status":"MET"},{"caller":"crates/governor/eliot-workscope/src/scanner.rs::ScannerResolverInputs::to_resolution_request + crates/governor/eliot-workscope/src/resolver.rs::ResolutionRequest::governing_source_refs","command":"cargo check --locked --offline -p eliot-workscope --all-targets (exit 0) + cargo clippy --locked --offline -p eliot-workscope --lib --no-deps -- -D warnings (zero warnings)","id":"W7","impl":"ResolutionRequest.governing_source_refs slot added (validated, owned by this issue) and populated from scan inputs; evidence-only tiers unchanged","status":"MET"}],"issue":1788,"worker":"W1"}