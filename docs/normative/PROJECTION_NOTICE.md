# Normative projection notice

This directory is a byte-for-byte projection of four files from the canonical
normative source. It is **not authority** and must not be used to promote a
claim about the current product. The canonical source remains the external
`ELIOT Arhitecture` document set referenced by the repository contract.

`projection-manifest.tsv` is local evidence for the four copied files. The
verifiers also compare the Architecture and Implementation bytes with the
hashes recorded in `docs/ARCHITECTURE_CONTRACT.md`.
