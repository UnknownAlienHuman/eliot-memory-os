# Claude integrations

Current core-repository integration documents:

- `CLAUDE_CODE_PLUGIN.md` — Claude Code plugin packaging/installation surface;
- `CLAUDE_DESKTOP_MCPB.md` — Claude Desktop MCPB packaging surface;
- `CLAUDE_INTEGRATION_SECURITY.md` — credential, transport, and authority constraints.

These documents describe adapters only. They cannot override the canonical
Architecture/Implementation pair, grant runtime support by presence, or define
Claude as an irreplaceable owner.
