# Issue #66 - W1 attempt 2 verification (branch `issue/66-W1-2`)

Method: re-read every MET claim's `impl` AND production `caller` on this
branch after the fix. Verdict per item: CONFIRMED (impl + caller present and
correct on the pushed HEAD) or REFUTED (with exact file:line of the break).
Verifier is the worker (self-verification by direct re-read; no
fresh-context subagent exists in this worker session).

## C - Required contract change

| id | verdict | evidence (branch HEAD) |
|----|---------|------------------------|
| C1 | CONFIRMED | Closed 7-variant `AgentActivationResolutionDisposition` (`crates/foundation/eliot-protocol/src/activation_resolution.rs:241-265`), `AgentActivationResolutionResult:339`; single producer `DaemonComposition::resolve_agent_activation_v2` (`bins/eliotd/src/lib.rs:827`); single caller `start_valid_claim_step` (`bins/eliotd/src/daemon_runtime.rs:803-804`). |
| C2 | CONFIRMED | Only `Resolved` reaches Session mint `activation_response_frame_for_resolution` (`bins/eliot-kernel/src/agent_bridge.rs:1509-1518,1564-`); six negatives deny with no Session (`:1520-1552`); daemon submit carries no authority (`bins/eliotd/src/daemon_runtime.rs:1204-1213`). |
| C3 | CONFIRMED | Submit-once `submit_agent_activation_result` (`agent_bridge.rs:1173`) via `dispatch_agent_activation_result` (`daemon_runtime.rs:1214`) with lost-ack reconcile reusing retained verbatim (`:1229-1247`); idempotency `classify_activation_result` (`bins/eliot-kernel/src/lib.rs:739`) + `submit_against_retained_result` (`agent_bridge.rs:1074-1122`). |
| C4 | CONFIRMED (fixed this attempt) | Transient-only gate `result_phase_for_disposition` (`agent_bridge.rs:791-798`, only `NotReady`->`DeferredNotReady`) + `not_ready_supersede_allowed` (`:757-786`, due-time + changed revision); `claim_at` admits a result-less ticket at most once (`bins/eliot-kernel/src/lib.rs:754-786`, `claim_lease_until_unix_ms.is_some()` skips forever); result-bearing tickets skipped (`:757-759`); deferred re-queue only via gated superseding submit, never admission. Cross-check refutation answered by code change (see below). |
| C5 | CONFIRMED with doc-code naming drift reported | Governor->protocol map `bins/eliotd/src/activation_projection.rs:239-302`; denial codes `agent_bridge.rs:1376-1400`; immediate projection with no expiry wait (`:1412-1451`, `:1465-1553`); agent-facing triple (`bins/eliot-agent-bridge/src/kernel_activation_client.rs:44-64,75-105`): reason + `STALE_OR_CONFLICT` disposition (I7.20 state/conflict group) + candidate-recovery directive. `TASK_SELECTION_REQUIRED` matches doc prose exactly; ambiguity wire string is `SCOPE_AMBIGUOUS`, not doc `AMBIGUOUS_RESULT` (drift reported, wire rename out of scope: stable 6-code vocabulary pinned by roundtrip test `crates/foundation/eliot-protocol/src/lib.rs:4103-4141`, owned by #204 edge). |
| C6 | CONFIRMED | `StaleFence` disposition (`activation_resolution.rs:258-261`); producer `activation_projection.rs:290-296` + `stale_fence_for_resolved_mismatch` (`:326-337`) + resolved-mismatch guard (`bins/eliotd/src/lib.rs:872-898`); Kernel deny arms create no Session (`agent_bridge.rs:1520-1552`); fence re-validation before ledger mutation (`:1499-1502`). |
| C7 | CONFIRMED (anchors corrected this attempt) | `FailedInternal{failure_handle}` (`activation_resolution.rs:262-264,297-300`); unready-Governor -> typed FailedInternal (`bins/eliotd/src/lib.rs:842-863` via `activation_projection.rs:352-361`); mapping-failure fallback (`lib.rs:1741-1758` via `activation_projection.rs:376-386`); bounded diagnostics `ErrorRecord`/`AdmissionRecord` (`lib.rs:912-924`, `activation_projection.rs:304-312`); retained Kernel record is the durable bounded failure evidence (`agent_bridge.rs:1074-1122`, bounded eviction `lib.rs:790-884`); distinct denial code (`agent_bridge.rs:1396-1398`); bridge failure-capsule directive (`kernel_activation_client.rs:95-105`). Old `:717` anchor was finish-closure code after main line drift; real path cited now. |
| C8 | CONFIRMED | Daemon deadline gate pre-resolve (`daemon_runtime.rs:787-798`, returns `None`, never calls resolver); Kernel submit retains replay across deadline (`agent_bridge.rs:1201-1209`) with `Timeout` only when no retained result; `claim_at` skips expired (`lib.rs:772-774`); result-less expiry keeps Kernel-owned `SemanticResolutionUnavailable` (`agent_bridge.rs:1373-1375,1931,2040`). |

## A - Acceptance

| id | verdict | evidence (branch HEAD) |
|----|---------|------------------------|
| A1 | CONFIRMED | `GovernorActivationOutcome::TaskSelectionRequired` (`crates/governor/eliot-governor/src/activation_outcome.rs:78-80`) from `resolve_activation_outcome` via `classify_activation_error` no-binding arm (`composition.rs:4969-4975`); mapped 1:1 (`activation_projection.rs:268-272`); denied immediately (`agent_bridge.rs:1440-1449`). |
| A2 | CONFIRMED (fixed this attempt) | `ScopeAmbiguous{selection}` built from the live selection by `scope_ambiguous_outcome_from_selection` (`composition.rs:4748-4790`): handles `scope:candidate:{work_item_id}` from the actual competing projections, sorted/deduped, bounded by `eliot_protocol::MAX_AGENT_ACTIVATION_CANDIDATES` (32; over-cap truncates with `Partial` coverage), `Complete` otherwise; <2 distinct candidates -> `FailedInternal`, never placeholders, never task selection. Protocol validation enforces >=2 exact candidates (`activation_resolution.rs:176-187`); deny `ScopeAmbiguous` (`agent_bridge.rs:1387-1389`). Cross-check refutation answered by code change. |
| A3 | CONFIRMED (fixed this attempt) | Single resolve per admission (`daemon_runtime.rs:799-824`; resolver called once at `:803-804`); no recompute on reconcile (`:1229-1247`); `claim_at` never re-admits a result-less ticket (`lib.rs:772-778`); supersede only via `not_ready_supersede_allowed` gate (`agent_bridge.rs:757-786,1092-1116`). Cross-check refutation answered by code change. |
| A4 | CONFIRMED | C6 evidence + fence re-validation before any ledger mutation (`agent_bridge.rs:1499-1502`); only `Resolved` builds binding (`:1509-1518`); deny arms `:1520-1552` create no Session. |
| A5 | CONFIRMED (anchors corrected this attempt) | Resolver `Err` fails closed to hard error (`daemon_runtime.rs:803-810`, never silently discarded; `?` at `:741` surfaces visibly); unready/mapping failures answer with terminal `FailedInternal` instead of killing the loop (`lib.rs:827-926,1741-1758`); `FailedInternal` is terminal (`result_phase_for_disposition` -> `AcceptedTerminal`) and retained, never re-resolved (C4). Old `:717` anchor was finish-closure code after main line drift; real resolver/diagnostic path cited now. |
| A6 | CONFIRMED | `classify_activation_result` Commit/ExactReplay/Conflict (`lib.rs:739-750`) + `classify_activation_result_for_entry` (`agent_bridge.rs:1283-1292`); digest-mismatch -> `IdentityConflict` (`:1101-1116`, `:1225-1227`, reconcile `:1272`); ack outcomes (`activation_resolution.rs:617-655`); daemon classify (`daemon_runtime.rs:1269-1344`); restart replay/conflict tests (`bins/eliot-kernel/src/tests/activation.rs`). |
| A7 | CONFIRMED | Lost-ack reconcile reuses retained verbatim, never recomputes (`daemon_runtime.rs:1229-1247`; `agent_bridge.rs:1246-1274`); retained replay survives deadline (`:1201-1209`); agent response consumes retained result (`:1465-1553`). Single-claim change preserves this: crash-before-submit now rests to Kernel-owned result-less expiry (one terminal disposition) instead of re-resolving. |
| A8 | TEST-PHASE | Code path wired (C5/A1 + `SemanticResolutionUnavailable` reserved for result-less cases only, `agent_bridge.rs:1373-1375`); live installed-route Product Pulse proof is a runtime event - genuinely TEST-PHASE. |

## W - Work decomposition

| id | verdict | evidence (branch HEAD) |
|----|---------|------------------------|
| W1 | MET | `activation_resolution.rs` dispositions + `Result::new` + `validate_against` + ack/reconcile types + fixtures (`crates/foundation/eliot-protocol/tests/`). |
| W2 | MET (caller anchor corrected this attempt) | `activation_outcome.rs` typed outcome + `resolve_activation_outcome` (`composition.rs:4709`) + lossless map (`activation_projection.rs:239-302`); real production caller is `bins/eliotd/src/lib.rs:870` inside `resolve_agent_activation_v2` (old `:745` anchor is the `kernel_snapshot` accessor after main line drift - it never invoked the mapper). |
| W3 | MET | `start_valid_claim_step` / `dispatch_agent_activation_result` / reconcile (`daemon_runtime.rs:787-825,1214-1383`) + dispatch wiring tests. |
| W4 | TEST-PHASE | All cited code present and correct (`agent_bridge.rs:757-798,1066-1274,1376-1553`; `lib.rs:703-786`; `host_request_route.rs` consumer); code-complete, only runtime proof left. |
| W5 | TEST-PHASE | Typed onboarding path wired (C5/A1/A8 evidence); v1-removal on main via #2431 confirmed; remaining is live edge run + #11 Pulse proof (#204 OPEN edge remainder). |

## Refutation responses (REMAINING.md, 7 items)

- C4 (was: `claim_at` recycled every result-less ticket after the 1s lease): FIXED. `claim_at` (`lib.rs:754`) admits each ticket at most once; the lease mark is a single-admission record. Reconsideration requires a retained `NotReady` result via the gated superseding submit path (`agent_bridge.rs:757-786,1074-1122`). Test `activation_claim_is_single_admission_without_result_less_recycle` pins the behavior.
- C5 (was: `SCOPE_AMBIGUOUS` + `STALE_OR_CONFLICT` instead of `AMBIGUOUS_RESULT`): functional contract CONFIRMED (immediate, typed, no-expiry-wait projection with reason + I7.20 state/conflict disposition + candidate-recovery directive). The reason-code string drift (`SCOPE_AMBIGUOUS` vs I04-04-01:92 / I7.20-catalogue `AMBIGUOUS_RESULT`) is REPORTED, not renamed here: the 6-code denial vocabulary is stable wire pinned by the roundtrip test, and agent-facing renames belong to #204. See REPORT.md.
- C7 (was: `:717` is finish-closure code): anchor CORRECTED to the real activation failure path (`lib.rs:827` resolver, `:842-863` unready arm, `:1741-1758` mapping fallback, `activation_projection.rs:352-361,376-386`, diagnostics `:912-924`). Failure record = retained Kernel typed result (bounded) + bounded daemon diagnostics + distinct `FAILED_INTERNAL` denial + failure-capsule directive. Not downgraded to ambiguity anywhere (classifier arm for unreadable ambiguity now yields `FailedInternal`, `composition.rs:4984-4993`).
- A2 (was: fixed `scope:candidate:a/b` placeholders): FIXED. Handles now name the actual competing `work_item_id`s from the live coordination selection (`composition.rs:4748-4790`), bounded to 32 with honest `Partial` coverage on truncation; classifier fallback without selection access yields `FailedInternal`. No placeholder handle string remains in production code.
- A3 (was: result-less ticket re-claimed after 1s and re-resolved): FIXED by the same single-admission change (C4). Daemon still resolves exactly once per admission (`daemon_runtime.rs:803-804`).
- A5 (was: `:717` unrelated finish code): anchor CORRECTED (same real path as C7). Resolver errors fail closed visibly; terminal failures are retained, never silently looped.
- W2 (was: `:745` is only the `kernel_snapshot` accessor): caller anchor CORRECTED to `bins/eliotd/src/lib.rs:870` (the `resolve_activation_outcome` invocation inside `resolve_agent_activation_v2`).

Overall: 18 MET, 3 TEST-PHASE (A8, W4, W5 - runtime/acceptance proof only), 0 refuted standing.
